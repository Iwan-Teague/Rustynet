# Cross-Platform Role-Parity Status Snapshot — `--node` engine — 2026-09-03

**Status:** POINT-IN-TIME SNAPSHOT (evidence-grounded 2026-09-03, worktree HEAD `b089a555`). Purpose: plan the next parity work after the macOS-client DNS release-blocker's fix wave (`551efad7`, `c53fb247`, `9cb8d094` — merged; D2 posture-flap fix still in progress). This doc **does not supersede** `CrossPlatformRoleParityRefresh_2026-07-23.md` (the standing status source of truth); it refreshes its matrix against the live ledger and flags drift. **Machine-generated by a delegated edit agent — UNTRUSTED until a human re-verifies the cited run ids against the ledgers.**

**Method.** Every verdict below was derived from the `--node` per-stage ledger `documents/operations/live_lab_node_stage_results.csv` (37,496 stage rows, 2026-07-10 → 2026-09-03), parsed quote-aware (python `csv`), and cross-checked against the run-matrix roll-up `documents/operations/live_lab_node_run_matrix.csv` (255 rows). Per AGENTS.md §12.3, a roll-up column is never taken alone as proof: where a column claims a pass that the per-stage ledger cannot corroborate, this snapshot says so (§5). The frozen bash archive `live_lab_run_matrix.csv` was not consulted; bash-era proofs do not count (Refresh §0).

**Verdict vocabulary.** LIVE-PROVEN = green stage artifacts exist in the per-stage ledger under the named run id(s) (formal §5.4 N-of-N-at-a-single-clean-commit stability is NOT recomputed here; noted where obviously unmet). PARTIAL = real green evidence exists but the cell is incomplete (single-run proof, missing sub-scope, or re-prove pending after a fix). NEVER-PROVEN = no green stage artifact for that cell on that OS. NEVER-RUN = the engine has zero stage rows for the cell (⬛ in the Refresh legend). UNKNOWN is not used: every cell was resolved to one of the above.

---

## 1. Matrix — role × OS on the `--node` engine of record

| Role | Linux (reference) | macOS | Windows |
|---|---|---|---|
| **client** | **LIVE-PROVEN** — `validate_baseline_runtime` 197 pass / 2 fail lifetime; `active_exit` P=132, `exit_handoff` P=135, `live_two_hop_validation` P=27; last pass **2026-09-02** `livelab-1788331859-79ce523d8354` | **PARTIAL** — single-node validators green (`validate_baseline_runtime` last pass 2026-08-25 `livelab-1787664882-e9cc8ad99575`, P=10/F=5; `dns_failclosed_validation` P=9/F=1), but the post-fix re-prove is open (2026-09-03 `livelab-1788394921-a26a1848338b` failed `validate_baseline_runtime` inside the D2 flap window) and **cross-node traffic has never gone green on macOS** (`live_two_hop_validation` 0/40 all skip; `traffic_test_matrix` 0/10 fail — a stage known to be misassigned to the plain client, see §5 flag F6) | **NEVER-PROVEN** — never past bootstrap: `bootstrap_hosts` 0/2 fail (last 2026-07-19 `livelab-1784493982-db3ff1aaafe6`); every validation/distribute stage skip-only |
| **admin** | **LIVE-PROVEN** — `admin_issue` P=25/F=3, last pass 2026-08-29 `livelab-1787974284-9dd5f7c3c5dc` | **NEVER-PROVEN** — never elected on a macOS `--node`; the Refresh's 🟢 cites the roll-up column `macos_admin=pass` (2026-07-19 `livelab-1784501586-537e1901b9bb`), whose stage rows show the **macOS node in the anchor role** while `admin_issue` passed on the **Linux** admin node (§5 flag F1) | **NEVER-RUN** — zero stage rows |
| **anchor** | **LIVE-PROVEN** — `anchor_validation` P=23/F=4, last pass 2026-08-29 `livelab-1787974284-9dd5f7c3c5dc` | **LIVE-PROVEN** — `anchor_validation` P=4/F=0 (last pass 2026-08-29 `livelab-1787984750-81a71286960e`); the seven baseline validators (`dns_failclosed`/`key_custody`/`mesh_status`/`runtime_acls`/`service_hardening`/`security_audit`/`authenticode`) P=9 each (last pass 2026-08-29 `livelab-1788036187-cda00c47ee95`); `validate_baseline_runtime` last pass 2026-09-01 `livelab-1788261812-690fc35b66d1` (P=14/F=5); plus the Refresh's 2026-08-31 green `livelab-1788165016205-17194-3` @ `5db953ad` (16 pass/0 fail/3 skip). Caveats: `traffic_test_matrix` 0/9 is the known misassignment, one `preflight` transient 2026-08-29 `livelab-1788026991`; formal 3-of-3 stability not recomputed here | **NEVER-RUN** — zero stage rows |
| **exit** | **LIVE-PROVEN** — `validate_baseline_runtime` P=182/F=0, `active_exit` P=129, `exit_handoff` P=132, `live_two_hop_validation` P=27; last pass **2026-09-02** `livelab-1788331859-79ce523d8354` | **NEVER-PROVEN** — the cell gets past setup (bootstrap/distribute pass; `distribute_assignments` P=2/F=5) and then stops at the dispositioned exit-posture `DnsFailclosed` gap: `validate_baseline_runtime` 0/2 fail (2026-08-31 `livelab-1788184018-6f3c08c20fe4`); every exit-role stage (`exit_handoff`, `active_exit`, `exit_dns_failclosed_validation`, `exit_nat_lifecycle_validation`, `exit_demotion_residue_validation`, the `live_*` suite) skip-only | **NEVER-PROVEN** — `bootstrap_hosts` fail 2026-07-25 `livelab-1785006739-b7667cce46db`; role stages skip-only; hardware blocker (CP-3) on top |
| **blind_exit** | **NEVER-RUN** — zero `role=blind_exit` stage rows on Linux on the `--node` engine (bash-era proofs do not count, Refresh §0). This gap is not stated anywhere in the Refresh | **PARTIAL** (proven once; §5.4 stability unmet) — `blind_exit` stage P=1/F=2: pass 2026-08-31 `livelab-1788173698-7bdcfe601493` (Refresh §1: 17 pass / 0 fail / 2 skip, commit `7bdcfe60`), after two same-day fails pre-lab-bug-fixes (`livelab-1788171760-3aacdc2cf581`) | **out of scope by design** (`main.rs` hard-error; Refresh §6) |
| **relay** | **LIVE-PROVEN** (lifecycle) — `relay_validation` P=57/F=0, `deploy_relay_service` P=57, last pass 2026-08-29 `livelab-1787974284-9dd5f7c3c5dc`; frame-forwarding (HP-3) remains a separate opt-in cell, first live pass pending | **NEVER-PROVEN** — no relay stage exists in the macOS `--node` stage vocabulary (61 distinct stages, none relay-named); the Refresh's 🟢 relay-lifecycle cites roll-up `macos_stage_relay_service_lifecycle=pass` (2026-07-19 `livelab-1784497253-11620a6627a4`), whose stage rows show the relay stages passing on **Linux** relay nodes while the macOS node ran anchor stages (§5 flag F1) | **NEVER-RUN** — zero stage rows |
| **role-transition** | (not a role cell; tracked as a capability) never run on `--node` on any OS — Refresh §1 and this ledger agree: `validate_macos_role_transition` skip-only ×6 | same — skip-only | same — never run |
| **nas / llm** | **NEVER-RUN** — zero rows on every OS; no stage exists (per `LiveLabStageCoverageGapPlan_2026-08-10.md` G1) — parked behind the D13 service-hosting program | same | same |

Cross-OS join: `live_mixed_topology_validation` is **0-for-all-history** on the `--node` engine — on the macOS node alone it is 40/40 skip with zero pass and zero fail (latest 2026-09-03 `livelab-1788394921-a26a1848338b`), consistent with the Refresh's "never attempted" disposition.

Linux resilience caveat: `network_flap` / CP-2 (traversal self-sustenance) remains the one Linux-wide red per the Refresh §2; the client/exit cells above are proven on their own stages, not on an overall-green run.

## 2. Single biggest blocker per non-green cell

- **macOS client** — CP-1: cross-node WG traffic has no L3 path in the current lab topology (macOS UTM Shared-NAT 192.168.65.0/24 vs Debian bridge100 192.168.64.0/24; triaged environmental 2026-08-29, Refresh §2 CP-1 verdict), plus the D2 DNS posture-flap re-prove still open (2026-09-03 `livelab-1788394921-a26a1848338b`).
- **macOS admin** — never elected on a macOS `--node`; its only "green" is a misattributed Linux-stage roll-up (§5 flag F1).
- **macOS exit** — the dispositioned `DnsFailclosed` exit-posture enforcement gap (`MacosDnsFailclosedEnforcementGap_2026-08-28.md`, `1278af04`); the merged fix wave targets exactly this, re-prove pending.
- **macOS blind_exit** — stability only: one clean pass (`7bdcfe60`), §5.4 3-of-3 not met; no known defect.
- **macOS relay** — never elected; no relay stage wired into the macOS plan vocabulary at all.
- **macOS role-transition** — never attempted on `--node` (stage exists, always skip).
- **Linux blind_exit** — never elected on `--node` (bash-era proof expired with the bash engine).
- **nas/llm (all OS)** — parked behind the D13 program (no stage exists; fenced disposition per Refresh §3).
- **Windows client / exit / admin / anchor / relay (all cells)** — CP-4: `--node` Windows bootstrap has never succeeded; named failing step `winget configure` at `scripts/bootstrap/windows/Bootstrap-RustyNetWindows.ps1:1130` (Configuration feature never enabled or checked), code-primary per `WindowsNodeBootstrapTriageVerdict_2026-08-28.md`; the exit cell additionally needs CP-3 hardware (`MSFT_NetNat`/HNS cannot run in UTM on Apple Silicon).
- **role-transition (both OS)** — never attempted; unowned sub-cell (Refresh §5 S3 keeps it in scope).
- **Cross-OS join** — never attempted; first execution is an unknown-unknown (Refresh §3 JOIN).

## 3. Ranked next-3 highest-value cells (after the macOS-client DNS blocker closes)

1. **macOS client — full-cell re-prove including cross-node traffic (closes CP-1 + the client cell together).** Highest value: it is the only path to a genuinely overall-green macOS run, it converts the environmental CP-1 triage into a closed disposition, and the client cell is the macOS anchor of every later cross-OS proof. First action: operator-authorized network re-attach of `macos-utm-1` onto the Debian lab segment (`prepare_lab_network` with explicit `approve_reconfigure`, per the CP-1 verdict — never an autonomous mutation), then a focused `rebuild_nodes=[macos client]` re-run at the D2-fix commit, expecting `validate_baseline_runtime` green and the first macOS `live_two_hop` attempt.
2. **macOS exit — re-prove under the merged DNS fix wave.** The three fixes (`551efad7` resolver ordering M1–M4, `c53fb247` ScopedResolverOnly posture in-bootstrap, `9cb8d094` expected-DNS-posture threading into the D1 check) are in HEAD `b089a555`; the cell already clears setup and dies exactly at the dispositioned gap. First action: elect `exit_platform=macos` (with `skip_linux_live_suite`) and re-run; expect `DnsFailclosed` to flip green under the exit posture. Note the harvest caveat: the exit-lifecycle stages (`exit_nat_lifecycle_validation`, `exit_demotion_residue_validation`, `live_two_hop`) require the full-suite path — the fast path drops them (Refresh §5).
3. **Windows column — land W-FIX-1 and the first `windows_stage_bootstrap=pass`.** Every one of the five Windows role cells is gated behind CP-4, so this single fix has the widest parity yield. First action: make the WinGet Configuration precondition explicit and self-healing in `scripts/bootstrap/windows/Bootstrap-RustyNetWindows.ps1` (verdict doc §7.2), then restore `windows-utm-1`'s remote-management path from the UTM console and re-run the minimal `debian-headless-2:exit` + `windows-utm-1:client` topology.

Cheaper alternates if lab hardware is constrained that day: a macOS blind_exit stability re-run (needs only a re-election, no code), and a macOS admin election (never attempted; would either produce the cell's first real signal or expose the next blocker).

## 4. "Refresh doc says" vs "ledger shows" — reconciliation

- **F1 (material): macOS admin 🟢 and macOS relay-lifecycle 🟢 are misattributed.** Refresh §1 cites `macos_admin=pass` (`livelab-1784501586`, `537e1901`) and `macos_stage_relay_service_lifecycle=pass` (`livelab-1784497253`, `11620a6`). Both runs' stage rows show the **macOS node in the anchor role running anchor stages**; the `admin_issue` / `deploy_relay_service` / `relay_validation` passes are on **Linux** nodes. The `macos_admin` / `macos_stage_*` run-matrix columns are bash-dialect roll-ups (the Refresh itself warns about this class for `cross_os_*`, §6), and the macOS `--node` stage vocabulary (61 stages) contains no admin or relay stage at all. This snapshot downgrades both cells to NEVER-PROVEN on the macOS node. The Refresh §1 opening sentence "macOS is partially stage-green (admin, relay-lifecycle, core, security stages pass in isolation)" inherits the same error for its admin/relay half.
- **F2: ledger size stale.** Refresh §0 says 178 rows (re-counted 2026-08-28); the ledger now holds 255 run rows. "Zero overall `pass`" remains true (the last 22 rows: 11 fail / 11 partial / 0 pass).
- **F3: blind_exit stability understated nowhere, overstated nowhere — but Linux blind_exit is missing from the Refresh's matrix entirely** (it only has macOS/Windows columns). The ledger shows Linux blind_exit is NEVER-RUN on `--node` — a gap no active doc states.
- **F4: Windows bootstrap claims consistent.** Refresh's corrected count (3 `bootstrap_hosts` fails + 2 skips across 2026-07-19/25) matches the ledger (client-run 0/2 fail 2026-07-19 `db3ff1aaafe6`; exit-run fail 2026-07-25 `b7667cce46db`).
- **F5: cross-OS 0-for-N consistent.** `live_mixed_topology_validation` on the macOS node: 40/40 skip, zero pass/fail — "never attempted" holds.
- **F6: `traffic_test_matrix` reds on macOS client (0/10) and anchor (0/9) are the known misassignment class** (the stage historically aliased onto plain-client/topology cells; AGENTS.md §12.3 caution). They are recorded here for completeness and must not be read as a dataplane defect signal by themselves; the honest macOS traffic signal is `live_two_hop_validation` (0/40 skip = never attempted).
- **F7: anchor cell recency.** The Refresh's matrix update (2026-08-28/31) predates two further greens this snapshot records: `validate_baseline_runtime` last pass 2026-09-01 `livelab-1788261812-690fc35b66d1` and `anchor_validation` 2026-08-29 `livelab-1787984750-81a71286960e`. No contradiction — the cell is greener than the doc's last update.
- **F8: DNS fix wave vs D2 flap.** The Refresh's "DnsFailclosed stays red until the enforcement design lands" is half-superseded by the merged fix wave (`551efad7`/`c53fb247`/`9cb8d094`), but the re-prove has not yet produced a green: the latest macOS client run (2026-09-03 `livelab-1788394921-a26a1848338b`) still failed `validate_baseline_runtime` inside the D2 flap window (worktree HEAD `b089a555` records this attribution). Neither ledger nor Refresh yet shows a post-fix green.

## 5. References

- Ledgers: `documents/operations/live_lab_node_run_matrix.csv` (255 rows), `documents/operations/live_lab_node_stage_results.csv` (37,496 rows, window 2026-07-10 → 2026-09-03).
- Standing status source of truth: `CrossPlatformRoleParityRefresh_2026-07-23.md` (matrix updated 2026-08-28/31; §7 lists the stale pointers in the Plan/Roadmap, including their bash-archive DoD evidence pointers in ParityPlan §5/§8 and Roadmap §10 — those corrections are still outstanding in the two older docs).
- Mandate + Definition of Done: `CrossPlatformRoleParityPlan_2026-06-21.md`; per-cell design/effort: `CrossPlatformRoleParityRoadmap_2026-06-22.md`.
- G2 bar + stability rule: `NodeEngineAcceptanceSpec_2026-07-23.md` (§5.4 N-of-N at a single clean commit).
- CP-4 verdict: `WindowsNodeBootstrapTriageVerdict_2026-08-28.md`; DNS gap: `MacosDnsFailclosedEnforcementGap_2026-08-28.md`.
