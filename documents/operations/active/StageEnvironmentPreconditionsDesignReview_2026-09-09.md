# Review of `StageEnvironmentPreconditionsDesign_2026-09-09.md` — 2026-09-09

**UNTRUSTED adversarial review (GLM-5.3-flash, read-only grounded agent, 12
steps), docs-only.** Verdict: **BUILD-WITH-CHANGES** — five required changes,
listed at the end. The manager has NOT yet independently re-verified every
citation below; treat file:line claims as the reviewer's observations.

## Findings

**1. CONFIRMED — no producer of a relay candidate exists anywhere (QH-80 premise).** `relay_id: None` at `ops_e2e.rs:3773, 3792, 3946, 3953` (doc cited ranges match); `relay_id: Some` returns **zero matches repo-wide** (grep run). `load_relay_client` returns `Result<Option<RelayClient>, DaemonError>` — but it is at `rustynetd/src/daemon.rs:5059`, not `:4983` as the doc cites (line drift; doc's "verified at design date" caveat covers it, but cites should be re-pinned before build). The stage itself never checks session material: `relay_forwards_frame_validation.rs` delegates to `exercise_linux_relay_forwards_frame` with only relay-alias/inventory/adapter/platform pre-checks, so the doc's "missing facts live below the stage layer" is accurate.

**2. CONFIRMED — live_two_hop returns `Skipped` on missing entry/second client.** `live_two_hop_validation.rs` `execute()`: `missing_entry = alias_matching_label(ctx, "entry").is_none()`, `missing_second_client = ssh_params_for_second_client(ctx).is_err()` → `StageOutcome::Skipped(...)`, with regression test `skips_when_topology_lacks_entry_and_second_client`. Helpers at the file tail match the doc's `:233-238` / `:268-275` cites.

**3. REFUTED — the doc's central §1 "correction of the audit" miscounts.** Doc: "on the tree today the Setup count is two (`membership_init`, `distribute_assignments`)". Wrong: **four** Setup stages hard-require an Exit. `distribute_assignments.rs:283-285` is in `distribute_bundle_kind_inner`, whose own doc comment says "Shared bundle distribution logic used by Assignment, Traversal, and `DnsZone` stages" — it fails `"no Exit node in assignments"` before minting, and mints **on the exit adapter** (`ctx.adapters.get(exit_alias)` → `issue_bundles_to_dir`). So `distribute_traversal` and `distribute_dns_zone` hard-fail without an Exit too — both pin it with unit tests literally named `no_exit_node_fails` asserting `StageOutcome::Failed`. (`distribute_membership` does *not*: it only filters `role != NodeRole::Exit` at `distribute_membership.rs:48`.) This is also the answer to Q3's "name any stage it forgot": **§5's flag-day table declares `ExitNodePresent` only for membership_init + distribute_assignments and files the other two under "All other 71 stages … No change"** — inconsistent with the table's own stated criterion ("declaring only facts whose satisfaction is already enforced at runtime"). Exit is enforced at runtime for traversal/dns_zone exactly as for distribute_assignments; they belong in phase 1.

**4. CONFIRMED — mechanism matches the landed QH-83 precedent.** `define_stage_catalog!` row shape is exactly `Variant => "wire_name" @ Suite / Tier / Evidence` (`stage/mod.rs:277+`), `evidence()` is a compiler-total match, and the doc comment says "the 82nd stage forces its own conscious witness-or-opt-out decision" — i.e. 81 rows today, which I verified by counting catalog rows: **exactly 81**. `verify_declared_evidence` at `runner.rs:295`, invoked only under `matches!(outcome, StageOutcome::Passed)` at `runner.rs:194-196` — the doc's post-Passed extension point is real. `ReasonCode::RequiredCapabilityAbsent` exists at `error.rs:240-243` with verbatim the semantics the doc quotes. `is_blocking` (`error.rs:304-308`) = `Failed | NotRun | NotProven`; `Skipped` is non-blocking. Default plan: `build_returns_66_stages` at `plan.rs:614-618` pins 66; suite inclusion at `plan.rs:364-371` (Setup/Cleanup always, Disruptive opt-in only) supports §5's default-plan claims, and `relay_forwarding_validation_absent_from_default_plan` (`plan.rs:817`) pins the opt-in. **But §5's arithmetic is wrong: 11 phase-1 stages + "all other 71" = 82 ≠ 81 catalog rows** (should be 70).

**5. Q1 (facts real? producers provable?):** every fact I spot-checked corresponds to a real runtime assumption with the cited site: Exit×7 verbatim (`membership_init.rs:40`, `distribute_assignments.rs:285`, `exit_handoff.rs:29`, `active_exit.rs:61`, `exit_dns_failclosed_validation.rs:49`, `exit_nat_lifecycle_validation.rs:45`, `exit_demotion_residue_validation.rs:50`) **plus the two forgotten ones (finding 3)**; relay×3 (`deploy_relay.rs:93-97`, `relay_validation.rs:69-71`, relay_forwards skip); admin (`admin_issue.rs:33-38` verbatim); macOS-elected (`macos_anchor_port_mapping_authority_validation.rs:56-59` checks `ctx.macos_anchor_validators_elected` → Skipped). Producer-side content points are real for the bundle stages (verifier-key barrier + signed-bundle install in `distribute_bundle_kind_inner` is a genuine verified production point, and the three provisions can be recorded from one site, kind-parameterized). UNVERIFIED: the parenthetical "(per-node systemd unit active)" for `RelayServiceDeployed` — `deploy_relay.rs:128` only calls `adapter.deploy_relay_service()`; whether the adapter confirms unit-active state I did not read. Phase-2 facts (Anchor, BlindExit, SecondUnderlay, macOS role/reboot elections beyond the one site) unverified, as the doc itself flags.

**6. Q2 (fail-closed?):** four of five classes hold as specified: produced-absent → offline plan refusal; runtime-absent → `NotProven{RequiredCapabilityAbsent}` (blocking, cascades); wrong-party → `record_env_fact` refuses; duplicate provider → plan refusal. Two gaps: **(i) Malformed is NOT covered.** `record_env_fact(..., witness: &str)` as specified has no content validation; an empty or non-content witness string mints the fact, and the post-Passed check only tests ledger *presence*. §4's table implies coverage it doesn't have. Require non-empty, digest-shaped witness. **(ii) The `Reused` path breaks supply.** Explicitly-skipped producers from a prior run return `Reused{digest}` without executing (`runner.rs:240-247`), so they never call `record_env_fact`; a later run that reuses setup stages will hard-block every consumer at the pre-execute gate. Fail-closed, yes — but it silently kills the supported reuse workflow, and §4's "Reused{digest} mechanism, untouched" waves it away rather than deciding it. Needs an owner decision: either refuse reuse of declared producers at plan time, or carry provisions from the reused run's ledger.

**7. Q3 (flag-day):** the "one deliberate behavior change" claim (opt-in relay-forward refusal) is correct, and `planned_out` on 2-node is non-blocking as claimed — but the doc **contradicts itself on planned_out semantics**: §5 says the runtime skip "becomes unreachable" (stage not executed), while §4 row 1 says "if the stage is also dependency-supplied it still runs guarded" (stage executed, skip reachable). Both cannot be true. It must be resolved before build, because it determines: whether dependents of a planned-out stage cascade (`mark_blocked`), whether `RunStatus` goes `Partial`, and whether the "66 stages" plan invariant moves. Also finding 3's two stages, and the §5 71/70 arithmetic error.

**8. Q4 (tests mutation-real?):** tests 2–10 are mutation-real — each names a mechanism whose reversion flips the assertion (plan-gate refusals, pre-execute `NotProven` with "never calls execute", cascade via `mark_blocked`, wrong-party `Err`, witness demotion mirroring the existing `runner.rs:1340` QH-83 mutation test, schema pin). Two gaps: **test 1 is not a unit test** — deleting a macro column is a *build* failure, so it needs a trybuild-style compile-fail harness (or the honest statement that the compiler is the gate; I did not verify a QH-83 trybuild precedent exists). **No test covers the `Reused`-producer path** (finding 6ii) and none covers planned-out dependency-cascade semantics (finding 7) — both because the mechanisms are unspecified, not because tests were skipped.

**9. Q5 (CLAUDE.md §3/§4):** §3 requires "one hardened execution path … No runtime fallback" (CLAUDE.md:61). The belt-and-braces framing is fine, but §4's "still runs guarded" is a second outcome path (gate says absent, stage says Skipped-non-blocking) — resolve finding 7 by deleting the runtime skip for declared requirements, not by keeping both. Effort (§7) omits the two extra phase-1 declarations, the reuse-policy decision, and witness validation; §8's owner-decision list needs a 6th (reuse vs declared producers) and the witness-content rule folded into #5.

## Verdict

**BUILD-WITH-CHANGES.** The mechanism is sound and faithfully mirrors the landed QH-83 column; the vocabulary is real. Required changes: (1) add `ExitNodePresent` requires to `distribute_traversal` + `distribute_dns_zone` and fix §1/§5's count and arithmetic; (2) specify `planned_out` execution semantics (executed-or-not, cascade, RunStatus) and delete the contradiction between §3.5/§5 and §4; (3) add witness content validation to `record_env_fact`; (4) add an owner decision + test for the `Reused`-producer supply path; (5) re-pin drifted citations (`daemon.rs:5059`); correct test 1 to a compile-fail harness or drop the "mirrors QH-83 test" claim.

**Could not verify:** whether the relay adapter's `deploy_relay_service()` confirms per-node unit-active state; whether a trybuild compile-fail precedent exists from QH-83; `runner.rs:448+` ordering_after-vs-dependency edge semantics (cited by the doc, not independently read); phase-2 fact assumptions; and whether the QH-80 "90 s in" runtime narrative matches the run ledger (structurally corroborated only by the `live-lab-linux-relay-fwd10-20260907-180632` reference in `distribute_assignments.rs`).

## Tools used (31 call(s) over 12 step(s))
