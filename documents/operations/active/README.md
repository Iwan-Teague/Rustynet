# Active Operations Work

This folder contains the current implementation ledgers, active plans, and active lab-reference assets.

## Primary Execution Ledgers

Start with these when you need current status, remaining work, or the public implementation ledger:

- `LiveLabHandover_2026-08-14.md` — **START HERE for live-lab work.** The operating contract for a stage failure (plan → adversarial review → implement → adversarial review → mutate), how to launch runs (direct vs MCP, and how they differ), why the TUI says IDLE, and the traps that cost real time (piped exit codes, suppressed stderr, mistimed captures, cross-family counter comparisons).
- [OwnerDecisionDigest_2026-08-27.md](./OwnerDecisionDigest_2026-08-27.md) — **single index of every decision waiting on the repo owner after the 2026-08-27 wave, extended in place by the 2026-08-28 addendum.** 17 wave entries + 6 addendum entries (18–23) with status (pending / gated-on-measurement / approved / in-progress-elsewhere / physical): the macOS helper lifetime and residue-marker disposition and the measurement that gates them (D-7, re-scoped by the 2026-08-28 QH-40 measurement verdict), the four never-dispatched-Linux-stage decisions, the six SignedMembership signing sub-flow decisions plus one latent driver default (D-4a), the CN-4 `NatProfileId` CLI tightening, the blind-relay token/hello v2 selection, the cross-network Tier B topology blocker (CN-6, no qualifying 2-LAN fleet), the two macOS anchor/exit cell blockers (MAC-D1/D2), the Windows W-FIX bootstrap dispositions (1–3 landed; 4–5 operator-physical). A digest with pointers, not a re-argument — each entry names the recommending document and section. Line references to unmerged branches are cited as `branch:file`.
- [TwoLanFleetSetupRunbook_2026-08-28.md](./TwoLanFleetSetupRunbook_2026-08-28.md) — **turnkey operator runbook for the owner-approved option (a) from the CN-PROOF §0.6 decision: turn the lab into a genuinely two-LAN fleet so the vxlan cross-network validators stop skipping.** States the three code gates CN-3 needs (≥2 underlay /24 groups `substrate.rs:754-756`/`:1529-1532`; `entry`+`aux` roles `cross_network.rs:929-932`; client/exit distinct /24s on the management plane `cross_network.rs:514-520`/`:944-949`/`:1085-1105`), shows the fleet already spans two /24s (192.168.64.x UTM Shared + 192.168.0.x real LAN via the lenovo pair) so **no UTM reconfiguration is needed** — only the missing reverse path (lenovo→64.x, measured 100% loss) — and gives the recommended route-via-Mac path as Phase 0 (a reversible ~15-min probe of the one unknown: does macOS forward into vmnet) through Phase 3 (the exact CN-3 invocation with entry/aux roles assigned), every step with a verify command and a what-if-it-fails remedy, plus full rollback to the single-LAN lab. Options (b) second lenovo-bot segment and (c) VLAN are recorded as fallbacks; the (a-bridge) variant carries the management-SSH-plane hazard and is last-resort only.
- `LiveLabStageStatus_2026-08-14.md` — **per-OS live-lab status: what is proven, what is failing, and what has never been exercised.** Linux is zero-failure end to end (44/44 runnable, re-proven 2026-08-28); macOS was elected for the first time 2026-08-28 — anchor and exit cells both 🔴 blocked with located blockers; Windows has never been elected — bootstrap root-caused, W-FIX-1/2/3 landed, guest currently unreachable. Read before claiming any stage works.
- [BlindRelayRoleGap_2026-07-30.md](./BlindRelayRoleGap_2026-07-30.md) — **origin/history for the unbuilt `blind_relay` role; design now proposed, still not accepted or implemented.** Links the authoritative proposal below, preserves the original sharp token question, and corrects the stale claim that the relay checks only a signature: current v1 also checks freshness/replay/bindings/scope/capacity, but still receives the explicit node/peer identity pair and delegates membership/pair policy to the signed issuer.
- [BlindRelayRoleDesign_2026-08-27.md](./BlindRelayRoleDesign_2026-08-27.md) — **DESIGN ONLY — proposed identity-blind single-hop relay; no code or live proof.** Defines a falsifiable relay-host threat property: stable Rustynet endpoint identities are absent, while paired socket legs, IP/port, timing, size, volume, and duration remain visible. Selects an append-only `BlindRelay` modifier requiring `RelayHost`, a dedicated local primary/preset, exact capability set `{RelayHost, BlindRelay}`, reversible drain/stop/purge/verify transitions, and identity-free proof-of-possession token/hello v2 with no v1 fallback. Covers signed fleet negotiation, mixed-fleet rollout, replay/anti-rollback/anti-fork, fail-closed state tables, per-OS isolation/verifiers, enforcement + verification for every control, honest UX/performance budgets, and adversarial live-lab stages. Deliberately not added to the parity completeness matrix until architecture/security acceptance.
- [GossipCandidateScopingPlan_2026-07-30.md](./GossipCandidateScopingPlan_2026-07-30.md) — **operator decision: BOTH ACL-scoping modes must exist and the choice is the user's** (`repush` default, `index` explicit opt-in), plus the record of a co-location gate that was designed, reviewed and **WITHDRAWN before any code**. The gate would have keyed on a peer's self-advertised reflexive address — a field the sender chooses — so it rewarded the parent plan's own S3 finding rather than stopping it; it filtered an index nothing probes (`applied_endpoints` has no production reader); and its fail-closed clause would have denied every private candidate permanently, since the local reflexive set is empty on default configuration. Retained verbatim rather than deleted, because it is the design a later reader will re-derive. Records what a real gate needs: an input the sender cannot choose, i.e. the observed source of a return-routability probe, which places it **after I4**.
- [BlindRelayProtocolSelection_2026-08-28.md](./BlindRelayProtocolSelection_2026-08-28.md) — **PROPOSED, doc-only — resolves the encoding/framing, proof-of-possession, and replay-persistence open items (§16 items 1–3) of the blind-relay design by composition of in-tree primitives**, with file:line survey evidence for every selection: all v2 wire surfaces reuse the house canonical line-based `key=value` grammar (v1 token parser: allowlist keys, duplicate-key reject, signature-final-line, canonical re-encode equality — `lib.rs:2006-2125`), with a bounded-size table per field and a defined 16-vector test set (none exist today); the PoP suite is Ed25519 via ed25519-dalek v2 `verify_strict` over a domain-separated canonical-line transcript (`rustynet-control-blind-relay-pop-v1`) with HMAC-SHA256 rotating-key stateless address validation and explicit zero-at-boundary + zero-on-Drop erasure; replay persistence keeps the existing NonceStore line format but keyed by SHA-256 digests of `(v2, epoch, nonce, leg_handle)`, rotated only through the existing `LocalKeyRotationLedger`. Items 4/6 get baseline-vs-blind measurement plans over existing benches (`relay_forward.rs`) and live stages (no p95/p99 or CPU/RSS sampling exists yet — named as the gap); item 5 is a PROPOSED aggregate-only retention table awaiting owner approval. Ends with the §17 acceptance-checklist delta.
- [BlindRelayAdversarialReview_2026-08-29.md](./BlindRelayAdversarialReview_2026-08-29.md) — **first-pass adversarial security review of the blind-relay phases 1–4 on `main` (§17 mandate; doc-only, no code changed).** Seven attacks, all HELD with file:line evidence: canonical wire parsing (three minor findings: signature-value trim, CRLF normalization, bounded attacker-value echo in enum errors — all caught or inert at the hello boundary), PoP binding (`verify_strict`, domain-separated transcript, cross-circuit replay rejected), ten-step admission ordering (stage-order witness tests prove cheap-before-expensive), signed-state advertisement (AddNode/SetNodeCapabilities/enrollment all refuse `blind_relay`; exact-set allowlist `{relay_host, blind_relay}` rejects future capabilities by default), the triple go-live gate (compile-time `false` review bit + signed capability + operator flag; `&&` chain, private constructor, mutation tests), BR-P1 identity elimination (no identity fields, digest-only replay/waiting state, no per-hello logging), and a systematic fail-open hunt (clock/store/capacity all reject; no memory-only replay fallback). **Highest-priority follow-up F4:** a third leg for an already-paired `(privacy_epoch, circuit_handle)` currently allocates a fresh waiting circuit instead of close/quarantine (§7.6) — not exploitable until the forwarding phase lands, but a blocking obligation for it. **Verdict: production advertisement NO-GO** — §16 selections remain PROPOSED pending owner approval, and this review is a GLM single-model first pass, not the independent cryptographic/protocol review §15.1 requires; a second independent review is recommended before `BLIND_RELAY_V2_ADVERSARIAL_REVIEW_APPROVED` is considered.
- [BlindRelayAdversarialReview2_2026-08-29.md](./BlindRelayAdversarialReview2_2026-08-29.md) — **the genuinely-independent SECOND adversarial review of blind-relay phases 1–4 on `main` (Claude/Opus; doc-only, no code changed, no gate flipped).** Required because the first review was written by GLM, the same model family that wrote the code (§15.1 independence). Re-verified every claim against the code with file:line evidence: all seven attacks HELD, all five first-review findings (F1–F5) independently confirmed, and three observations added — **O1: the listener is DORMANT** (`try_open` has zero callers outside its own module; `rustynet-relay/src/lib.rs` only re-exports it), a stronger property than the triple go-live gate; **O2:** the address-validation first-exchange is unwired, so an opened listener would reject every hello at step 3; **O3:** first review's "19-line hello" is arithmetically 18 (code correct). **F5 framing elevated** from defense-in-depth to a direct §5.1 "reject-future-by-default" violation at validation site (3) (`daemon.rs:2119–2148` is a future-permissive forbidden-list, not an exact-set compare) — fix before go-live. **Verdict: production advertisement NO-GO** — the review precondition is satisfied for the reviewed surface, but §16 owner sign-off and §13.2 live-lab evidence are independent unmet gates, and F4 (before forwarding) / F5 (before go-live) are open obligations; the compile-time review bit and reducer/enrollment gates must stay as they are.
- [WindowsWfpCoexistenceAudit_2026-08-28.md](./WindowsWfpCoexistenceAudit_2026-08-28.md) — **the QH-46 defect class (a foreign firewall silently discarding traffic the daemon authorized) examined for Windows/WFP.** Maps the full WFP surface (persistent max-weight sublayer + ALE tunnel-permit filters keyed on the tunnel LUID, `rustynet-windows-native/src/lib.rs`), then verdicts seven coexistence hazards with file:line evidence. Headline: **verdict 1 is REAL AND UNDETECTED** — Windows `admit_host_firewall_forwarding` was a self-documented no-op (`phase10.rs:4870-4876`), which made the QH-54 periodic posture loop vacuous on Windows. **Verdict 1's §3 fix shape IMPLEMENTED 2026-08-28** (see the doc's §3 Disposition and the QH-46 addendum in `QualityHardeningTodo_2026-07-25.md`): the Windows admit arm now calls `assert_forwarded_traffic_admitted()` (read-only `FwpmFilterEnum0` at the forward layers + portable fail-closed arbiter); verdicts 2-5 are bounded-fail-closed availability risks (verdict 2's narrower shape still OPEN); the Windows-guest live proof remains pending. Closes the `LiveLabStageStatus_2026-08-14.md` "Windows analogue is unexamined" flag.
- [CryptoPolicyHardening_2026-08-25.md](./CryptoPolicyHardening_2026-08-25.md) — **ACTIVE hardening campaign record** for branch `sec-hardening` (20 commits): four fail-closed defects fixed in the release-manifest sign/verify pipeline and Ed25519 signing surface (zero-seed signing, zero pinned verifier key, silent empty-signature fallback, duplicate `(name, target)` artifact tuples), `try_from_seed` added with the infallible `from_seed` confined to test builds, and mutation-tested pins for empty-message/cross-key/oversized-signature rejection, attestation kind/key-id bindings, ICMP default-deny (plain + membership-aware), malformed seed files, and operator-facing zero-seed refusal. Includes the mutation-testing methodology record and a checked non-issues list.
- [I4EnforcementFlipPlan_2026-07-30.md](./I4EnforcementFlipPlan_2026-07-30.md) — **WITHDRAWN before implementation; now a findings record.** Planned the parent plan's I4 (flip traversal enforcement to gossip) and was adversarially reviewed as *actively harmful*: it would have shipped the S3 victim-IP reflector it was written to close. Root cause worth reading even if I4 is not imminent — **a probe status does NOT mean an endpoint was proven**: `phase10.rs:6109-6131`'s fail-closed arm programs the highest-priority candidate anyway and reports `decision: Direct`, and priority is attacker-chosen, so "attested" as the plan defined it would have programmed a victim's IP. Also records: making the peer-set check per-peer is a fail-open (`PeerConfig.endpoint` arrives pre-populated from the control-plane bundle, so "hold" silently keeps it); the claim that probe status is rebuilt each pass is wrong and its correction exposes a hard cold-start deadlock; the bootstrap provenance label needs a signed wire-format change; and the gossip→index plumbing the plan assumed does not exist. §4 gives the real I4 scope, §5 the lesson: a signal was trusted for its name rather than for what it proves.
- [I4TraversalAttestationPlan_2026-08-05.md](./I4TraversalAttestationPlan_2026-08-05.md) — **DESIGN ONLY, NOT APPROVED, NO CODE.** Scope is items 1–6 and 8 of [I4EnforcementFlipPlan_2026-07-30.md](./I4EnforcementFlipPlan_2026-07-30.md) §4. **Conclusion: I4 cannot ship, and the withdrawn plan's own corrective definition of "attested" is also unsound.** Accepting `reason ∈ {ExistingFreshHandshake, FreshHandshakeObserved}` does not work because in production the ICE race cannot attribute a handshake to an endpoint: `handshake_endpoint()` is a trait default returning `Ok(None)` (`traversal.rs:714-716`) whose only overrides in the tree are test-only, so the race credits `pairs[0]` by default (`traversal.rs:1725-1730`) while the trigger is a per-public-key counter with no endpoint awareness. Also records that the fail-closed arm calls **`commit_verified_traversal_path_for_peer`** on the branch where nothing answered — a function with `verified` in its name, one line deeper than the predecessor's excerpt goes. **Read §3.1 before re-deriving the mechanism**: this plan's first explanation was refuted by adversarial review and is kept rather than deleted. Sender-supplied `priority` is *discarded and recomputed* on the race path (`ice_priority.rs:284` reads only `source` and `endpoint`), so the intuitive "attacker sets a big u32" story is **wrong**; the harm survives through a coarser class ladder in which a public victim IP declared `Host` outranks a legitimate RFC1918 candidate, made worse by foundation dedupe probing only one remote address per (kind, family) lane — so the real endpoint may never be probed. Two framings are corrected in the defender's favour and one against: probing **already** programs every candidate it touches (`send_probe` → `reconfigure_managed_peer`, `phase10.rs:102-108`), so the incremental exposure is *persistence of proof*, not first-time programming; the attack needs an authority-signed bundle, so it is not off-path; and a single unsettled empirical question — whether `latest-handshakes` can advance from an endpoint other than the one just probed — **would collapse the entire finding** if answered negatively, and could not be settled because cargo is unusable on this host. Establishes three further blockers: **item 1 is a signed wire-format change**, since gossip host candidates carry no port and `signing_preimage` covers that absence with literal zero-padding (`peer_gossip.rs:414`, documented `:380-381`) while port 0 is hard-rejected in three places, and the would-be bridge `ice_priority::prioritize_candidate_set` — whose module header advertises "a remote peer's gossiped `CandidateSet`" — has **zero callers outside its own file**; **items 1–2 are transitively blocked on unimplemented work**, because gossip keys on `[u8;32]` while the traversal index keys on `NodeId` and no reverse map exists, which needs membership to publish the gossip verifying key, i.e. [GossipMembershipIdentityAlignment_2026-08-05.md](./GossipMembershipIdentityAlignment_2026-08-05.md), itself design-only; and **denying at cold start deadlocks into a *permanent* restriction**, because cold start works only via the static control-plane fallback at `daemon.rs:7016` and `promote_to_permanent_if_over_limit` escalates — resolved by gating the deny on the same controller state the early-return already uses, conceding one authority-verified-but-not-reachability-proven generation, stated explicitly rather than left to be misread later. Also: **no persistent attestation state exists** (`PeerTraversalPrior` records *which* class last won, never *when*, bumps its timestamp on failure paths, and is deliberately fail-open), the retained handshake stamp is **sticky** via `.or(...)` so it never returns to `None`, `saturating_sub` makes a future-dated timestamp permanently "fresh", and `apply_traversal_authority_to_peers` takes `&self` so it cannot record or expire attestation at all. Item 8: the operator-selected ACL mode changes what "missing from the index" means and **neither reading is "unreachable"** — in the default `repush` mode missing means *nobody forwarded it yet*, so fail-closed converts convergence delay into an outage. **Revision 3: a second review refuted four of revision 2's corrections, including its headline recommendation** — read §0 first. A3.3 is withdrawn (the proof surface `live_proven` is reason-blind at `daemon.rs:7461`, and A3.3 creates the mixed-reason case whose aggregate literal is hard-coded at `:7395`/`:7398`); the rejection of A3.1 was itself wrong (deleting the fallback does not cost connectivity, because `ExistingFreshHandshake` returns `Direct` without entering the race — but it does reroute unattributed races into the arm where the raw sender `priority` selects, so A3.1 is viable only paired with item 2); §7's cold-start resolution does not hold (statuses exist only for already-programmed peers, so the gated deny deadlocks peer *addition* on every membership change and needs a per-peer skip); and the empirical question is no longer load-bearing (misattribution follows from the send loop alone, no roaming needed). Also corrected: only the **host** lanes lack a port — srflx carries a real port inside the signed preimage (`peer_gossip.rs:423`), so the wire-format finding is narrower than stated; and one `[verified]` tag cited evidence that did not exist. **Nothing in it is landable as written.**
- [I4GossipImplementationHandoff_2026-08-05.md](./I4GossipImplementationHandoff_2026-08-05.md) — **EXECUTION HANDOFF for a fresh implementing agent**, covering the work designed in [I4TraversalAttestationPlan_2026-08-05.md](./I4TraversalAttestationPlan_2026-08-05.md) and [GossipMembershipIdentityAlignment_2026-08-05.md](./GossipMembershipIdentityAlignment_2026-08-05.md). Nineteen dependency-ordered items, each with file:line anchors, a stated proof obligation (the specific named test that must fail when the change is reverted), and a landable-or-blocked verdict; plus a **do-not-do-this** section, a blocked-on section separating *unimplemented design* from *off-limits crate* from *operator decisions an implementing agent must not decide silently*, and the operational rules (gates, commit mechanics, push policy). Produced by extraction agents each attacked by a verification agent — **all four areas had material problems found by verification**, and the corrections are marked `[VC]` inline, including two constants the source plan left unread (`DEFAULT_MAX_RECONCILE_FAILURES = 5`, `daemon.rs:338`), a false borrow-checker rationale that would have stopped someone attempting the right fix, an unwritable proof obligation, and a required third edit site without which A3.3 would leave a multi-peer fleet still reporting `path_reason=fresh_handshake_observed`. **Read the reconciliation section first**: the body was generated while the source plan reached revision 3, so it is internally inconsistent in three places, each resolved at the top by the plans' author with self-verified citations — items 4/5/6 build a design retracted **as sufficient** and must land as *plumbing only*; a **missing item** is added for the proof surface itself (`live_proven`, `daemon.rs:7461`, which nothing in the 91k-character body touched, so an unattributed endpoint would still report `path_live_proven=true`); and the body's escalation of the empirical handshake-attribution question to the operator is **withdrawn**, because misattribution follows from the send loop alone (`phase10.rs:102-108` reprograms the endpoint for every pair before any poll) and needs no roaming assumption. Also records the surviving chain — close the fail-closed arm, then A3.1, then the proof surface — and states plainly that **that chain has not itself been adversarially reviewed**, which on this document family's record (every previously unreviewed recommendation refuted, twice on claims tagged verified) is a live caveat rather than a formality.
- [GossipProvisioningPhase1_2026-08-04.md](./GossipProvisioningPhase1_2026-08-04.md) — **PARTIALLY IMPLEMENTED** (`37be86af` + follow-ups); read §10 before continuing, because blockers remain open. Provisions the gossip signing secret so the dormant subsystem can run. Worth reading as a case study in how the same mistake recurred: the plan named the **wrong template twice** by grepping a name and inheriting another consumer's mechanics, and the implementation then **ignored the plan's own lab-first instruction** and put the mint in `rustynet install` — which lab guests never use, since they bootstrap through `ops e2e-bootstrap-host`. That single misplacement meant gossip would have stayed dormant on every machine the project can prove it on, and lab-first was exactly what would have caught it. Also records that the HKDF input is **33 bytes, not the 32 minted** (the loader appends a newline), which is harmless for the daemon but a silent identity fork for any independent deriver — precisely what the follow-on work introduces. §10.2 lists what is still open, including that **no already-installed node has any provisioning path at all**.
- [GossipMembershipIdentityAlignment_2026-08-05.md](./GossipMembershipIdentityAlignment_2026-08-05.md) — **DESIGN ONLY, NOT APPROVED, NO CODE WRITTEN.** How signed membership comes to publish each node's real gossip Ed25519 verifying key, closing the [GossipProvisioningPhase1_2026-08-04.md](./GossipProvisioningPhase1_2026-08-04.md) §7 gap that makes gossip peer registration structurally impossible. Root cause is a **missing primitive, not a set of buggy producers**: `rustynetd key` has no verb that prints the gossip verifying key (`rustynetd/src/main.rs:460`), the value exists only in-process as `GossipNode::local_node_id` (`gossip_runtime.rs:201`/`:276`), so no producer is *able* to publish the right thing and fixing producers first is impossible. **Read §0.2 and §0.4 before the design.** Three revisions, two adversarial reviews, and each review refuted a headline claim of the revision before it — both times a claim the author had labelled `[verified]`. The retractions are kept verbatim rather than deleted, because the pattern is the transferable lesson: **the narrow supporting fact was right every time, the sweeping conclusion drawn from it was wrong.** Retracted: (1) that alignment creates a **fail-open revocation** path — it fails closed three times over (Active-only registration `gossip_runtime.rs:846`, unknown-source rejection *before* the revoked check `peer_gossip.rs:601-602`, and revoked ids unregistered not merely blocklisted `daemon.rs:5552-5556`); (2) that **"the product path never runs genesis"** — `ops init-membership` is an **ungated verb in the shipped binary** that shells `rustynetd membership init --force` (`rustynet-cli/src/main.rs:1412`, `:14009-14011`, `:20459`), while the lab bootstrap verbs beside it *are* `vm-lab`-gated, so genesis is product work and on macOS publishes CSPRNG bytes for a node whose installer never mints a gossip secret at all; (3) that **no test names `decrypt_private_key`** — one does, and it is worse than absence: `build_gossip_node_derives_gossip_only_subkey_from_encrypted_secret` (`daemon.rs:20158`) is almost exactly the pin this design proposes writing, but its fixture is 31 `'A'` + `\n` = **32 bytes ending `0x0a`**, so the conditional newline append is a no-op and the test silently exercises the vacuous case while its own comment claims to "pin the newline"; (4) that only one type embeds `MembershipNode` (`MembershipState.nodes` at `membership.rs:189` is a second, twenty-six lines after the cited line); (5) a "50.1% measured (4000 samples)" statistic replaced by the closed form — every curve point has exactly one compressed encoding, so the valid fraction is `8ℓ/2^256` = **exactly 50%**, and the sample never had three significant figures. What **survived** all three passes, confirmed independently twice plus by direct reading, is the feasibility claim: **no reader anywhere treats `node_pubkey_hex` as a WireGuard key**, so publishing the gossip key breaks no consumer — the apparent overloading is producer-side, one collected value handed to two sinks. Substantive findings: membership genesis publishes a key with **no private counterpart in existence** (`node_key_bytes` is CSPRNG-filled, hex-encoded and zeroized without ever being persisted, `main.rs:4119`/`:4121`/`:4129`/`:4232`), so no migration can recover it; there are **two independent gossip peer-registration authorities** (`enrollment_consume.rs:219-220` registers an enrollee-supplied key taken at face value, via `daemon.rs:8615`) and revocation reaches only one, a bypass that **does reach an accept** but ages out after two further epoch bumps (`peer_gossip.rs:637-641`) — and alignment is what **closes** it, provided both enrollment paths change together; the producer count is **~15, not 6**, across **five** flag spellings (`--node-pubkey`, `--node-pubkey-hex`, `--client-pubkey-hex`, `--new-pubkey`, `--pubkey`), two binaries and two scripting languages, and at the vm_lab orchestrator the source is *typed* `BTreeMap<String, WireguardPublicKey>` (`orchestrator/context.rs:40`) feeding the real wg peer config from the same variable, making those sites **a split of one value into two, not a swap**; **P10** — `RotateNodeKey` writes the field from a differently named source (`new_pubkey_hex`, `membership.rs:2018`) reached via `--new-pubkey`, invisible to every earlier enumeration including both reviews' — which matters because it is the verb the migration itself would use, so **the migration mechanism is an unaligned producer**; and a **platform blocker** — Windows cannot hold a gossip identity at all (`daemon.rs:11539-11547`, "the gossip transport is unix-only"), macOS never mints one (`init-gossip` absent from both non-Linux installers), yet **no capability, role or platform filter excludes them from peer registration** (`gossip_runtime.rs:838-870` filters only on Active/not-self/hex/`from_bytes`/overlay-address), so their entries do become candidate gossip peers on every Linux node. Migration needs **no new membership operation** (`RotateNodeKey` already exists and is CLI-reachable) because the field sits inside the **signed canonical payload** (`membership.rs:333`, parsed `:2070`) and cannot be edited in place — but carries an **epoch budget**: the accept window is ±2 (`peer_gossip.rs:147`, `:162`) and each rotation bumps the epoch by 1, so migrating more than two nodes unbatched stops gossip mesh-wide until snapshots land (transient outage, not a brick). §6 leaves **five decisions to the operator** rather than picking silently: migrate vs new-nodes-only; genesis mint ordering; whether `--force` re-mint stays unconditional (`install/live_linux.rs:180`, `ops_e2e.rs:476`); what macOS/Windows publish; and whether rotating the gossip trust anchor should be owner-gated (`requires_owner_signer` covers only `RotateApprover`/`SetQuorum`, `membership.rs:441-446`). Computed rather than asserted, since a review flagged these as undecidable without running code: `[9u8;32]` and `[10u8;32]` **are** canonical Ed25519 points (so a validation tripwire does **not** break the three shipped audit binaries whose fixtures use them), `[7u8;32]` and `[2u8;32]` are **not**, and `"00".repeat(32)` (`rustynet-cli/src/main.rs:22250`) decompresses **but is small order** (8·P = identity), so it passes a decompression tripwire and fails any `verify_strict`-grade one. §8 records what was deliberately not fixed, including that `register_peer` is additive with no prune-on-rotate. Also corrects drifted references in Phase 1 §7 (genesis at `main.rs:4119-4129` not `:4002-4012`; the e2e producer at `ops_e2e.rs:1953` not `:1926-1927`, and that site is itself a pass-through with the real WireGuard source at `:4053`) and notes §7's producer table omits every operator-facing CLI path and the whole orchestrator family, which biases a reader toward believing the problem is lab-only.
- [LiveLabStageCoverageGapPlan_2026-08-10.md](./LiveLabStageCoverageGapPlan_2026-08-10.md) — **PARTLY SUPERSEDED ON ARRIVAL; read its provenance header first.** A `--node`-engine stage-coverage audit, drafted by a sub-agent that had been told to stay read-only and wrote the file anyway — landed because its claims were **independently re-verified**, not because it was trusted. Corrects `FullTodoInventory_2026-07-28.md` §Wave-3 on three of four points: the cross-OS adversarial security stages ARE ported and DO run on the engine of record, the three formerly-inert chaos scaffolds ARE implemented, and `extended_soak`/negative-control/cross-network all exist in the `--node` vocabulary. Re-verified at `27e49d54`: **G1** no nas/llm stage exists on any engine (0 wire-names, 0 registry names, word-boundary matched); **G3** the twenty-four per-control security columns are `not_run` in all 106 ledger rows; **G4** no `--node` run has ever dispatched chaos (the single chaos-bearing run carries no `run_note`, so it is legacy/bash); **G2** relay frame-forwarding exists only as a bash-dialect name. **Its headline increment I1 was already implemented by concurrent work in `27e49d54`** — the per-control artifact filename matches this plan verbatim, so the plan was evidently that work's input; the producer side is done and the ledger reads `not_run` only because no run has happened since, making the remaining G3 step a RUN, not code. G3 is framed as the mirror image of QH-37: that defect made columns read greener than reality, this one makes them read redder, and both corrupt the same evidence base. I2/I3/I4 are pre-review and were not re-verified line by line. **REVISION 2 (2026-08-10) folds in a full adversarial review of the plan — 1 blocker, 4 majors, 4 minors, and eight claims that survived attack.** The blocker was I4: "nothing needs writing, just select the suite" is false, because all nine chaos stages depend on `LiveMixedTopologyValidation`, which has never passed, so enabling the suite yields nine cascade-skips — **enabled ≠ dispatched**, and the original acceptance criterion (a dry-run plan listing them) was satisfiable while the goal was not. The majors: I1's four-value column rule had **no branch for an audit that was never reached** (fixed with a distinct `blocked` verdict, since `not_run` asserts no node of that platform was in the run — false on a run where it was present); `StageSpec.special` is **one fixed column string**, not a per-platform template, and `macos_/windows_relay_forwards_frame` plus any `nas`/`llm` column are **absent from `DEFAULT_MATRIX_COLUMNS`**, so I2/I3 each carry an unplanned schema change; the "runs on all three platforms" claim is runtime *support*, not evidence — only Linux has ever executed it, and two doc comments asserted the opposite of their own code; and I1 closes **24 of ~49** affected columns, with the remainder (notably `linux_key_custody`, whose report artifact IS produced while the column reads `not_run`) now tracked as I5. The review also confirmed I1's precedence **matches** the QH-37 fix rather than contradicting it, and that the recorder already receives `report_dir` so I1 needed no new plumbing.
- [GossipProducerAlignmentIncrement1_2026-08-08.md](./GossipProducerAlignmentIncrement1_2026-08-08.md) — **IMPLEMENTED AND LIVE-PROVEN 2026-08-09** (S1-S11; S8b deliberately narrowed to `--node`-only).  Records that **two bugs got past a twice-reviewed plan, one of them caused BY the plan** — its instruction to avoid `systemctl is-active --quiet` was wrong, `active` landed on the same stdout as the key, every node failed `got 71`, and the unit test asserted the plan's wrong rule and passed; and `macos_membership.rs` branched on the producer's platform rather than the SUBJECT peer's, so a macOS-exit run silently republished Linux nodes' gossip identities as WireGuard keys under a flag named `unaligned-wireguard`.  Originally: The landable *producer* increment for [GossipMembershipIdentityAlignment_2026-08-05.md](./GossipMembershipIdentityAlignment_2026-08-05.md): make a Linux joiner publish its own derived gossip verifying key, and make the live lab fail if it does not. Motivated by the first **measured** instance of the defect — on 2026-08-07 the joiner published a value that decodes exactly from its WireGuard public key, and the exit node logged `gossip_reject_unknown_source` continuously at `peers=0`. Reviewed twice: as a design (four lenses + judge, five blockers) and as a written document (three lenses + judge, **five further blockers and six majors**) — **revision 1 was not implementable**, and three of its defects sat in the ordered steps an implementer follows literally. Records what each review broke: the verb has **six** callers not three, and the sixth is the *Linux* shell orchestrator, which one section deferred while another hard-rejected the flag it uses; the Windows `--node` producer escapes the containment entirely because it calls a **different verb on a different binary** (`membership add-peer --node-pubkey-hex`); the verdict predicate "the joiner's key is in the registered set" is **unobservable** because only a count is published and `register_peer` logs nothing; "absence is an `Err`" would have bricked every mixed-OS run because `CollectPubkeys` applies to all nodes; and the named remote helper cannot express the `-u` switch the custody check makes mandatory. Also corrects the parent design (its "macOS never mints" is stale for the **product** installer but true for the **lab** path, so the platform gap is still *both* macOS and Windows) and corrects itself twice: the claim that a lab macOS node is "inert" was retracted (the plist gates on **file existence**, not on who minted, and lab guests are long-lived), and the cold-boot `disconnect-cleanup` FIB error was retracted as a **symptom, not a cause** — it runs as `ExecStopPost=-`, whose status systemd ignores, so it cannot drive the restart loop. Establishes that canonical-Ed25519 validation is **not** a control (measured 0.5054 pass rate over 20 000 random strings; it cannot distinguish an X25519 WireGuard key from an Ed25519 gossip key), so **only the flag's spelling** separates aligned from unaligned. Both gating probes PASS: the systemd credential is readable by a host-namespace `sudo -n -u rustynetd` process, and the joiner appears in the exit node's assignment bundle with a `/32`, retiring the overlay-join hazard in that direction. Fail-closed posture and the pre-existing **peer-removal gap** it activates (additive-only sync; a W→G key change leaves W registered for the process lifetime) are stated, and that gap must be filed in the security ledger **before** this lands.
- [BlindExitSeizureHardening_2026-08-04.md](./BlindExitSeizureHardening_2026-08-04.md) — **INVESTIGATION + DESIGN, NOT APPROVED.** What a physically seized `blind_exit` appliance reveals about the mesh it serves, and what can be done about it. Three designs judged, winner attacked from three lenses, **all three returned NEEDS_CHANGES**. Read §1 first: **there is no requirement to anchor this work to** — `SecurityMinimumBar.md` has no physical-compromise clause at all and `Requirements.md:204-207` lists three threat considerations, none of them seizure, so any hardening shipped decays silently. §2 is the cheap independent win: `peer_traversal_priors.v1` is a transit-history ledger persisted with no pruning path, and because its path derives from the traversal watermark it does **not** follow a state-root relocation — a design that moves the state root removes everything except the one file that matters. Also records that process-memory hardening does not exist at all (zero hits for `mlockall|LimitCORE|MemorySwapMax|MADV_DONTDUMP`), so a core dump or swap page writes the whole roster to flash today. §4 states the two ceilings software cannot pass — RAM capture of a powered forwarder, and the unattended-boot oracle, which the PCB plan's deliberate no-secure-boot choice forecloses the only defence against — plus the fact that unlinkability is bounded by the weakest node, since every non-blind_exit node stores the full plaintext roster.
- [GossipSecretProvisioningPlan_2026-08-03.md](./GossipSecretProvisioningPlan_2026-08-03.md) — **INVESTIGATION + DESIGN, NOT APPROVED, NOT IMPLEMENTED.** Why the gossip data plane is dormant on every node (nothing sets `RUSTYNET_GOSSIP_SIGNING_SECRET` — note **no `_PATH` suffix**, the Rust const is named `GOSSIP_SIGNING_SECRET_PATH_ENV` but its value is not, and copying the identifier name sets a var the daemon never reads), and what wiring it would take. Three designs were judged and the winner was then attacked from three lenses; **all three returned NEEDS_CHANGES with five ship-blockers**, the first of which — an `Environment=` line plus an `ExecStartPre` file test in the *shipped* systemd unit — would have **failed startup on every existing node**. Also records that the configured secret path is never opened (the loader derives a custody dir + key id from it and reads the OS store or `<parent>/wg-private-<hex>.enc`), that the configured passphrase path is inert on unix, that a semantically wrong but validly-encrypted secret is never rejected, and that `--gossip-watermark` being passed by the installers is what makes the subsystem look provisioned when it is not. §5 lists the operator decisions that must be made before any code is written.
- [GossipStatusSurfacePlan_2026-07-30.md](./GossipStatusSurfacePlan_2026-07-30.md) — **IMPLEMENTED** in `001c23b1` + `ab8e47e4`; **live-lab acceptance still UNMET and declared as such** in the doc header (the run matrix ends 2026-07-29 and references neither commit). Implements item 7 of the I4 findings record: make the gossip data plane observable. Unlike the three designs withdrawn before it, **its premise was upheld by adversarial review** — `accepted_count`, `minted_count` and `rejected_counts` exist and have **no production reader**, so a rate-limited or wholly-rejecting node is indistinguishable from a healthy one, and the per-origin limiter landed in `021c1ef0` is currently invisible. The surface question took three revisions and is worth reading as a case study: revision 3 moved the fields from `status` to `netcheck` on four arguments, and review refuted all four — the "structural twins live on netcheck" claim was false (57 of netcheck's 72 keys are duplicated onto `status` too), and **`netcheck` is disqualifying**: `IpcCommand::Netcheck` passes `force_reprobe=true` (`daemon.rs:8146`, where `Status` passes `false`), forcing a full ICE re-race with ~240 ms of `thread::sleep` per peer on the single-threaded daemon loop, rewriting the traversal anti-replay watermark to disk, and reaching `force_fail_closed_or_restrict` (`:4982`) — so **polling gossip health could trip the node fail-closed**. Revision 4 returns to `status`, appends via one named `{gossip_suffix}` capture so the 86 positional args are untouched, and adds two fields review proved necessary: `gossip_push_failures_total`, because `minted_count` is bumped *before* the broadcast loop (`gossip_runtime.rs:453` vs `:456-472`) so a node whose every push fails reads healthy; and `gossip_identity_mismatch`, which a node whose mints every peer rejects would otherwise not distinguish itself by. That field is **computed fresh and is three-valued, deliberately NOT reading** the existing `gossip_identity_mismatch_warned` (`daemon.rs:3850`): that latch is set once, never cleared, and its check short-circuits on itself, so surfacing it would report a mismatch repaired by re-enrollment as broken for the life of the process — and it fails open when membership or the gossip node is absent, which now reports `unknown` rather than `false`. §6 logs **fourteen** defects found along the way, including that `status --json` has never rendered, that `is_mutating()` misclassifies both read commands as read-only, and that the gossip socket binds `0.0.0.0:51821` while its module doc claims it binds on the mesh interface and builds a security argument on that claim.
- [HostObservabilityStabilityPlan_2026-07-24.md](./HostObservabilityStabilityPlan_2026-07-24.md) — **ACTIVE. Two coupled halves, both adversarially reviewed.** (a) **Observability layers** to make a host self-diagnosable after the 2026-07-24 `ubuntu-kvm-1` reboots (a hardware reset/power event — `kernel.panic=0` ⇒ a panic hangs not reboots): **L0** = add the agent identity to `systemd-journal` (a fable review collapsed an over-engineered root-owned diag-wrapper after finding a `journalctl`-under-sudo pager→root-shell escape + sudo-brick risk + that dmi-SEL/mcelog/pstore yield nothing at L0); **L1** read-only MCP tools; **L2** active tools (lm-sensors/mcelog/ramoops, `kernel.panic=10`) + `host_crash_forensics`; **L3** physical/BIOS checklist. (b) **§7 fleet-onboarding architecture** (first-class): idempotent composed verbs (`onboard-host --new`/`add-guest`/`remove-guest`/`offboard-host`) + additive `fleet-converge`/`fleet-status` over an image catalog, so Ubuntu+macOS boxes add/subtract easily. Hardened by **four independent fable reviews** (architecture/security/teardown/macOS) — must-fixes baked in: per-stage-verify idempotency, backing-file-safe teardown (drop `--remove-all-storage`), no `curl\|sudo bash` (sha256-pinned/image-baked bootstrap), time-bounded privilege + per-host keys, secrets→sidecar proven-by-test, corrected UTM premise (AppleScript `make` exists) + stated one-Mac ceiling (`local_utm` = this-machine-only). Impl order: **macOS headless-AppleScript-under-TCC spike FIRST**. Two reversible decisions flagged for the owner (§7.7: standing-vs-time-bounded privilege; mac semi-manual-vs-remote_utm).
- [CrossPlatformRoleParityRefresh_2026-07-23.md](./CrossPlatformRoleParityRefresh_2026-07-23.md) — **DRAFT (grounded; adversarially reviewed 2026-07-23; matrix updated 2026-08-28)**: re-scopes the release-blocking mac/win role-parity mandate to the **engine of record** (the `--node` orchestrator, which the acceptance spec's G2 gate reads), superseding the *status* half of the ParityPlan/Roadmap for the G2 era. **Headline reframe:** every ✅ in the old §3 matrix is a **legacy-bash proof** (cited run-ids live in the frozen bash archive, not the `--node` ledger) — so under G2 (release reads the `--node` ledger, bash being deleted) mac/win parity is **~0% proven on the engine of record**: the `--node` ledger is at **178 rows** (re-counted 2026-08-28 at `34a9e6f8`) with zero overall pass, cross-OS (`live_mixed_topology_validation`) is 0-for-178, Windows `--node` bootstrap has never succeeded (CP-4 triaged 2026-08-28: BOTH, code primary), and macOS is only *stage*-green (admin/relay) with `two_hop` failing every run. **2026-08-28 matrix update:** macOS `anchor` + `exit` were elected for the first time on `--node` (runs 47/48, `elected-blocked` with located blockers MAC-D1/MAC-D2) and both blockers are now **fixed in code** (`fix-landed-rerun-pending`: MAC-D1 `8ec851a9`, MAC-D2 `03619d0f`; path-constant hardening `a0851175`) with no post-fix lab run yet; the macOS DNS fail-closed **enforcement** gap is dispositioned design-only/owner-gated (`owner-gated-leak`, MacosDnsFailclosedEnforcementGap_2026-08-28.md) so `DnsFailclosed` stays red until that lands. Gives the `--node`-native status matrix, the critical path (CP-1 `two_hop` · CP-2 `network_flap`/traversal · CP-3 Windows-exit WinNAT hardware · CP-4 Windows bootstrap), the parallel-track program, the env-vs-code blocker split, the intended-OS-divergences (drift rule — not parity holes), the stale-doc corrections (DoD pointers → the `--node` ledger), and the G2 mapping. Key insight: the mac/win role **stages already exist**; the gap is running them green behind the blockers, not authoring validators.
- [NodeEngineAcceptanceSpec_2026-07-23.md](./NodeEngineAcceptanceSpec_2026-07-23.md) — **DRAFT (draft 2, adversarially reviewed), awaiting owner sign-off**: the Track B functional gate for the Rust `--node` orchestrator, written as a **`--node`-native acceptance bar** rather than a bash↔Rust parity gate (native-spec foundation + a **one-time** bash differential sweep as a migration safety net, since bash is being deleted and a parity gate expires with its reference + enshrines its quirks). An adversarial pass found and closed a structural hole: the first draft measured only **outcomes** (stages go green) and never **adjudication** (can the engine correctly produce a RED?), with every evidence property self-attested by the engine under test — a rubber-stamp engine would have passed. Draft 2 adds the missing dimension and splits the fused gates into **three**: **G1 engine-adjudication trust** (T0+T1 green **and** a T5 negative-control suite that must go RED-for-the-right-reason **and** an independent verifier that recomputes evidence from raw artifacts, on today's cells → gates the W5.6 default flip, **independent of parity**), **G2 parity attainment** (all role×OS cells green → gates release), **G3 bash-retirement** (one-time diff sweep, enumeration as a flip precondition → gates W5.7 deletion). Also: machine-readable total `StageId→tier` map + gate (the prose list had already drifted); fenced dispositions (named ledger, per-item owner sign-off, expiry, security not dispositionable); flake-sized N-of-N stability at a clean flip-candidate commit (replacing the arithmetically-refuted "two consecutive"); GREEN = exactly `Pass`; identity asserted not recorded; digest-bound evidence; `network_flap` correctly-adjudicated-RED until the traversal fix lands then green. §10 sign-off + §11 changelog inside.
- [NodeEngineFlipDispositions_2026-07-24.md](./NodeEngineFlipDispositions_2026-07-24.md) — **owner-signed W5.6 flip dispositions (§6.1)**: the named ledger that lets the bash→`--node` default flip proceed at clean commit `a414ceb` (5-of-5 A2-verified stability) while three items are dispositioned as correctly-adjudicated gaps — D1 `two_hop` (a `run_cargo_ops` `--features vm-lab` tooling bug, not the dataplane), D2 the two live-deferred T5 controls (residue/daemon-kill; the other two T5 classes proven in-pipeline), D3 `network_flap` (the traversal self-sustenance product gap). Each has an owner sign-off, a stated reason, and a G2-follow-on expiry; none is an engine-trust failure (the engine reported every one loudly and correctly, which is what G1 tests). Mirrored into the acceptance spec §6.1.
- [G3EnumerationDiff_2026-07-23.md](./G3EnumerationDiff_2026-07-23.md) — **COMPLETE + adversarially reviewed**: the `NodeEngineAcceptanceSpec` §8 **enumeration-half flip precondition** (diff-only, no lab) — the sole detector of *silently-dropped* stage coverage in the bash→`--node` migration. Result: **zero silently-dropped stages** (as scoped) — every stage the frozen bash archive ever proved GREEN maps to a **wired** `--node` `StageId` (all of `StageId::ALL` is plan-registered with a no-dead-stage gate). Two diffs: bash-archive CSV columns (549 rows) + the authoritative bash-script `run_stage` names (catches CSV-invisible stages). Differences are all benign: dialect naming, deliberate **consolidation** (bash's 8 Linux security stages → one `security_audit_validation`, per `LINUX_SECURITY_AUDITS`), `cross_os_*` bash-dialect aggregates (spec §3-T3), or **G2 mac/win attainment** (a release gate, not the flip — G1≠G2). Dispositions two CSV-invisible bash meta-stages (`fresh_install_os_matrix_report` report-aggregation, `local_full_gate_suite` CI-gate) as owner-sign-off items for W5.7, and flags one honest DEPTH caveat (`role_switch_matrix` may be shallower on `--node` — a G2 item). The adversarial review confirmed the claim holds and corrected a false "records GREEN" statement (the 8 audits are coverage-present but attainment-pending on `--node`, 0 ledger passes). Satisfies the W5.6 flip precondition; the full outcome-level G3 sweep still gates W5.7 bash deletion separately.
- **Bash Orchestrator Retirement Program — COMPLETE 2026-08-27** (moved to `../done/BashOrchestratorRetirementProgram_2026-08-22.md` with `../done/BashRetirementPlan_2026-07-24.md`): the legacy bash live-lab orchestrator is DELETED; the Rust `--node` engine is the sole engine. G3 satisfied — A3 sweep + A4 enumeration archived, every gap cell owner-signed-dispositioned (`BashRetirementDispositions_2026-08-22.md`), Spec §8 marked satisfied, deletion landed at `e93a0e4f`, all §7 gates + lab_monitor gates green, post-deletion `--node` run verifier-recomputed.
- [G3FullSweepDiff_2026-08-22.md](./G3FullSweepDiff_2026-08-22.md) — **A3 artifact of the retirement program: the outcome-half G3 sweep is COMPLETE — MUST-KEEP** (raw output archived as `G3FullSweepDiff_2026-08-22_parity_diff.json`). Matched exit+client topology driven on BOTH engines back-to-back (bash 15/15 pass @ clean `25d49e20`; `--node` 37 pass / 0 fail / 22 topology-skips): **14 shared stages, zero `matches:false`** — everything both engines executed they adjudicated identically; the only bash-only rows are 3 harness/meta stages (dispositioned B7), and the 46 node-only rows are the surviving engine's superset vocabulary. Discharges Spec §8 for the matched-topology stages ONLY — the mac/win/cross-OS cells run through A4 + Phase B.
- [BashRetirementGapEnumeration_2026-08-22.md](./BashRetirementGapEnumeration_2026-08-22.md) — **A4 artifact of the retirement program above — MUST-KEEP (survives the harness deletion).** The ledger-wide, quote-aware per-cell comparison of the frozen bash archive (549 rows) vs the `--node` ledger (151 rows): **56 raw gap columns = 12 ledger-dialect false gaps + 44 real**, each mapped to exactly one Phase-B bucket (B1 false gaps / B2 Windows-behind-CP-4 / B3 macOS prove-on-node / B4 cross-OS / B5 relay HP-3 / B6 residual Linux). This, not the A3 matched-topology sweep, is the Phase-B disposition worklist.
- [BashRetirementDispositions_2026-08-22.md](./BashRetirementDispositions_2026-08-22.md) — **Phase-B adjudication ledger of the retirement program (B-INITIAL, all owner sign-offs PENDING) — MUST-KEEP.** Every A4 gap cell mapped to exactly one entry: B1 the 12 dialect false gaps, B2 the Windows column grouped behind CP-4 (+ 8 individually-signed [T4] security cells, the CP-3 hardware exit, the design-excluded blind_exit, the gossip/bundle code gaps), B3 the four macOS prove-on-node commitments (C1-C3) + the anchor gossip_seed/enrollment_endpoint deferral, B4 the five cross-OS entries, B5 the relay lifecycle-vs-HP-3 split, B6 the two residual Linux cells. Carries the A0 deletion-precondition evidence (zero strict-pass soak rows; the honest signal is 0-fail partials) awaiting the owner decision.
- [TraversalSelfSustenancePlan_2026-07-23.md](./TraversalSelfSustenancePlan_2026-07-23.md) — **APPROVED design, implementation not started**: the execution plan to close the sole remaining Linux `--node` red (`live_network_flap_validation`), which is the canary for a **real production gap** — on default config (`auto_tunnel_enforce: true` → `EnforcedV1`) a mesh fails closed ~120 s after setup because nothing re-issues the signed traversal-authority state; the hardened daemon is a pure local-custody consumer and the control-plane minter distributes once at bootstrap. Chooses **hardened Design A** (each node self-signs its own candidates via the already-built-but-dormant D2.5 gossip; membership authorizes *who*, self-signed candidate provides *how to reach*) over Design B (anchor re-mint — rejected because it would put the mesh root signing secret on a running daemon). Records the resolved grounding (membership `public_key` = the Ed25519 gossip identity; gossip per-source sequence = anti-replay parity; gossip drive already wired in prod, only `attach` + a **new daemon signing identity** + register-from-membership missing), the mandatory hardening (epoch-bind CandidateSets for expiry-bounded revocation, ACL-scoped gossip application, per-origin rate-limit/plausibility), and the file:line-level increments I1–I7 (daemon signing-key custody → gossip activation → epoch-bind wire bump → fail-closed enforcement rewire → ACL-scope → rate-limit → integration test → live-verify). A multi-session, security-critical build on the most sensitive crate; no TTL widening, fail-closed preserved.
- [QualityHardeningTodoReview_2026-07-25.md](./QualityHardeningTodoReview_2026-07-25.md) — **adversarial review OF the register below — read this first.** Applying the register's own norm (*"a finding is not a finding until someone independently reproduced it"*) to the register itself found **one false retraction, three refuted claims, four misattributions, and four unrecorded findings**. Every item still describes a real defect; the prose around several is wrong. Headline: **QH-07's retraction is FALSE** — the retracted "43 false-green `pass` rows" claim was **correct** (verified with a quote-aware parser; the register's 97/9/1 is reproducible only by a naive comma split of a quoted CSV), and the root cause is a **ledger-code defect**: the alias table collapses `traffic_test_matrix` and `live_two_hop_validation` into one column, so `linux_stage_two_hop` reads `pass` on 43 rows for a stage with a lifetime record of 116 fail / 263 skip / **0 pass** — while `CLAUDE.md:418` instructs every agent to use that column as their acceptance criterion. Also: **QH-06 verified** (clippy passes clean workspace-wide with no exclusions on the pinned toolchain; the "known red" was a Homebrew-1.97 artifact, and the stale `--exclude rustynet-mcp` guidance is struck from the charter); **QH-13 upgraded** INFERRED→VERIFIED and medium→**high** (vendor-documented OpenSSH argv joining; its own premise understated it); **QH-03 refuted** for the script it names (the fail-open ones are recover-vms and disk-status); **QH-12 refuted** (the helper it cites does not exist; the *correct* escaper already ships at ~20 sites); **QH-04's "unrecoverable" refuted** (the daemon self-heals) though it remains release-blocking via a production trigger the register misses. Corrected priority: **QH-07 → QH-13 → QH-01 → QH-04 → QH-06 → QH-11 → QH-09 → QH-05 → QH-08 → QH-10**.
- [QH01TemplateInjectionFixPlan_2026-07-25.md](./QH01TemplateInjectionFixPlan_2026-07-25.md) — **plan + build record for QH-01** (P1, privileged boundary): the escape-first fix for the host-script template-injection class. §3 records that the *first* design (a single-pass renderer alone) was **refuted** — `pool` was unvalidated and `image` path-shape-only, so two breakouts needed no ordering trick and rendered byte-identically under it; the renderer therefore had to **own the quoting** via typed bindings over the `shell_quote` escaper that already existed in the same file. §9 = the adversarial plan review (2 blockers, 4 serious); §10 = the build outcome (all 32 production + 18 test render sites converted incl. 3 root-privileged `sudo bash -s` scripts, a bypass that cannot compile, QH-12 closed, QH-02 partially closed with mutation-verified tests, 2172 tests green). **Scope limit: this does NOT close the injection class** — the SSH-argv sink stays open under QH-13.
- [QualityHardeningTodo_2026-07-25.md](./QualityHardeningTodo_2026-07-25.md) — **OPEN register of 41 cross-cutting defects and process weaknesses** (QH-01 through QH-41; 15 at the 2026-07-25 review, appended to since — QH-37 and QH-22 are now RESOLVED) from the 2026-07-25 multi-line session (two_hop dataplane, ubuntu-kvm-1/WinNAT, host-observability), kept out of the individual work ledgers because each spans several files or lines of work. **Read [QualityHardeningTodoReview_2026-07-25.md](./QualityHardeningTodoReview_2026-07-25.md) first** — applying the register's own norm to the register found one false retraction, three refuted claims, four misattributions and four unrecorded findings; every item still describes a real defect but the prose around several was wrong, and per-item verdicts are inline. **Corrected priority: QH-07 → QH-13 → QH-01 → QH-04 → QH-06 → QH-11 → QH-09 → QH-05 → QH-08 → QH-10.** Headline items: **QH-07 (HIGH) — FIXED 2026-07-27, and its figures corrected: the counts in this entry and in the register never reproduced.** Re-derived at commit `9cdd660f` with a quote-aware reader: the `linux_stage_two_hop` column reports `pass` on **35 of 94** rows for a stage that has never passed, `traffic_test_matrix` has **185** passes, and `live_two_hop_validation`'s record is **222 skip / 81 fail / 0 pass** (only the `0 pass` was ever right, and it is the part the finding rests on). The production alias was **not** at `live_lab_run_matrix.rs:3744/3747/3770` — those lines are inside a `#[cfg(test)]` oracle; it was the `logical` field on the `traffic_test_matrix` spec in `live_lab_stage_registry.rs`, now `None`. The removal is forward-only, so the 35 contaminated rows are permanent and the "never trust that column for a historical row" instruction still stands; **QH-13 (HIGH)** values validated for a single-quoted script context also reach an SSH-argv sink that the remote login shell re-parses; **QH-01** the host-script template-injection class (in progress on `claude/wsd-qh01`) — order-independence alone is insufficient, the renderer must own the quoting, and three sites in `recover_guest_network.rs` are piped to `sudo bash -s` as root; **QH-04** the assignment/traversal atomicity exposure, a release-blocking product defect still unowned. Deliberately not summarised item-by-item here — a 15-item precis in an index is itself a drift surface, which is what this register is about. Two standing conventions it establishes: **name the tree/commit each claim was verified against**, and **split the confidence label** when mechanism and example diverge.
- [DesignShiftEvaluation_2026-07-30.md](./DesignShiftEvaluation_2026-07-30.md) — **evaluation of five candidate design shifts proposed "now, before live labs"; two REJECTED, one downgraded.** Verdicts: **(1) replace the hand-rolled canonical `key=value` signed encoding — REJECTED**, because the obvious both-formats migration is forbidden by AGENTS/CLAUDE §3 (no runtime fallback/legacy branch), leaving a flag-day cutover strictly more expensive than the "M" claimed, while a re-encoding is itself a high-risk change (canonical-binary malleability: map ordering, duplicate keys, non-minimal ints) and the residual after `c36edc97`+`1801ca35` is now narrow; the cheaper substitute is a single validated `write_field()` helper so a *new* field cannot skip the guard (S, not implemented). **(2) generation/anti-rollback counter in signed artefacts (CTL-06) — REJECTED as framed**, the "rides along free with (1)" coupling was doing the persuading and dissolves with (1); a generation *field* without a persisted high-water mark + fail-closed regression check is worse than nothing because it presents as anti-rollback while providing none. **(3) tri-state validator results — DO, scoped**; premise verified by measurement (33 production validators in `role_validation/`: 28 `Result<(), String>`, 5 `Result<String,String>` — all binary; a first pass reporting 87 wrongly counted 54 test fns), size S–M, and the load-bearing half is the **recorder contract**, since an `Indeterminate` mapped to `pass`/`skip` repeats the §12.3 incident that masked 35 run-matrix rows. **(4) one role→capability table + CI gate — DO** (XS claimed, unverified; enumerate legitimate presentation mappings first or the gate false-positives and gets disabled-but-believed). **(5) derive the ledger summary from its rows — DO** (~20 lines; four row-vs-summary disagreements in one session, all understating what was done). **The most important content is §0, not the proposals:** adversarially reviewing proposal 1 found the same-day ENR-01/ENR-03 fix `c36edc97` was **incomplete** — it guarded `MembershipState::validate`, but there are **three** encoders of that framing, and `MembershipUpdateRecord::canonical_payload` (the text approver signatures cover) validated only `trim().is_empty()` while `reason_code`/`policy_context` had no validation at all, with `--update-id`/`--reason`/`--policy-context`/`--approver-id` all operator-supplied; fixed in `1801ca35`. Records the ordering rule that survived review (**harness** changes must precede live labs because they change what is *measured*; **product** changes merely invalidate a *run*) and an explicit §7 evidence-limits section — both review agents and all four Tier-1 mapping agents died to a stream-watchdog stall, so proposals 3/4 sizings are self-measured and one salvaged-but-unverified claim (that IPV-04's family-agnostic case may be a **regression introduced by** `f2e084d9`) is flagged as **do not act on without reproducing**.
- [LinuxMtuPrivilegedHelperAllowlistGap_2026-07-21.md](./LinuxMtuPrivilegedHelperAllowlistGap_2026-07-21.md) — **live regression, diagnosed, fix not yet implemented**: `enforce_baseline_runtime` fails on every Linux lab guest (Fedora/Ubuntu/Rocky confirmed) because FIS-0027's new `ip link set mtu 1420 dev <iface>` bring-up step (`fef40bb`, unrelated to and predating the session-wrap-up work below) was never added to the privileged-helper's `ip` argv allowlist, so it's rejected every time, driving the daemon into permanent lockdown and cascading into the managed-DNS systemd unit failing. Root cause confirmed live via `journalctl` on a real guest, not guessed. Lays out the exact fix (an allowlist entry matching the literal MTU constant, not any numeric value — with the security reasoning for why a type-range or a signature would be the wrong, weaker choice here) and the exact test needed to prove the scoping is tight.
- [SecurityAuditAndMainConsolidation_2026-07-21.md](./SecurityAuditAndMainConsolidation_2026-07-21.md) — **session wrap-up**: the A1–A5 security audit (A1/A3/A5 fixed, A2 doc-corrected, A4 substantial — see the two docs below), the macOS full-tunnel exit dataplane fix, and a full "get everything on main" branch consolidation (every branch/worktree in the repo enumerated and disposed of — merged, confirmed-already-on-main, or deleted with operator approval after confirming no recoverable history). §7 lists what was deliberately left open: Part B of the audit, the adjacent bearer-token gap, live-lab proof, and remaining branch housekeeping.
- [FullTodoInventory_2026-07-28.md](./FullTodoInventory_2026-07-28.md) — **current repository-wide TODO roll-up (raw capture, not curated).** Every open item found across a full sweep of all 291 markdown docs in the repo (117 in this folder, 43 non-active operations docs, 17 top-level + 4 repo-root prompts), captured as of 2026-07-28, plus a code-level TODO/FIXME grep (zero hits — all open work lives in docs). Includes a "stale or internally-contradictory docs" section (17 instances found where a doc's status line disagrees with a sibling doc or its own later sections) — check that before trusting any single status line at face value. A point-in-time snapshot, not a live tracker; supersedes the 2026-07-10 ledger below as the current rollup. Focused ledgers remain authoritative for their own scope.
- [RustynetUnifiedTodoLedger_2026-07-10.md](./RustynetUnifiedTodoLedger_2026-07-10.md) — **superseded 2026-07-28, see above.** Prior dated repository-wide TODO roll-up covering security/release blockers, the full live-lab verification ladder, completion of the Rust `--node` engine, canonical dual-plane VM networking and MCP behavior, desktop role parity, cross-network dataplane, Android/iOS client programs, NAS/LLM evidence, testing/fuzzing, serialization, CI/supply chain, performance, operations, platform expansion, external decisions, and one shared Definition of Done. Kept for section structure and history.
- [ParallelAgentWorkPlan_2026-07-01.md](./ParallelAgentWorkPlan_2026-07-01.md) — **partitions the current backlog into 3 independent big jobs for concurrent agents** (HP-3 real relay packet-forwarding proof; cross-OS role transitions + Windows anchor live bundle-serving; a Tier-1 security-hardening batch — replay-persistence, crash-midapply fail-closed, relay-flood DoS, UPnP SSRF, privileged-helper socket fuzz), plus an optional 4th (macOS/Windows security-validator ports). Documents the known in-flight work to not duplicate (`ENR-1`/`TOCTOU-1` enrollment-replay audit, uncommitted as of 2026-07-01), the shared-wiring-file collision risk + append-only discipline, and the git-worktree-per-job mechanics for landing back onto `claude/cross-platform-parity-hardening` sequentially. Ready-to-paste prompts in `ParallelAgentPrompts_2026-07-01/`
- [NonSecurityParallelHandoff_2026-07-13.md](./NonSecurityParallelHandoff_2026-07-13.md) — **stand-alone multi-model dispatch prompt for the guardrail-safe NON-security backlog** (durability, refactor, tests, tooling, diagnostics, docs), curated from the unified TODO ledger + RNQ audit. Explicitly excludes the crypto/trust-state/killswitch/exit-NAT/DNS-failclosed/privileged-helper/ACL surfaces (§4 → they stay on the Opus + §13.2 path). Tiers work by model: `fable` (RNQ-05 durability, RNQ-07 cancellable timeouts, RNQ-17 feature-gate the lab robot out of the shipped binary, advisor MCDA), `sonnet` (RNQ-09 SIGTERM test, sysinfo parser coverage, lab-monitor input-robustness, read-only diagnostics), `haiku` (doc/index/CODE_MAP drift), plus a GRAY dataplane-perf tier that a `fable` agent may build+bench but may NOT land without Opus §13.2 sign-off. Each package is a worktree-isolated stand-alone agent brief with a suggested dispatch order.
- [LabMonitorTUIDesign_2026-06-29.md](./LabMonitorTUIDesign_2026-06-29.md) — **design spec for `rustynet-lab-monitor`**: a pixelated terminal TUI that is the live GUI for the parity campaign — stage block grid, VM status, role×OS parity matrix, start/stop control, ETA from timing CSVs. New crate `crates/rustynet-lab-monitor/`; tech: ratatui 0.28 + crossterm + tokio; reads state files directly (no MCP dependency at runtime). Build prompt at `scripts/loop/lab_monitor_build_prompt.md`
- [LiveLabMonitorTUIAccuracyImprovements_2026-07-10.md](./LiveLabMonitorTUIAccuracyImprovements_2026-07-10.md) — **implemented monitor truth/freshness hardening**: active TSV precedence, invocation-correct resume manifests, fail-loud manifest/data errors, schema-v2 fetched test counts, source-age display, complete UTM discovery, explicit VM power/online/readiness/run-use columns, canonical cross-platform toolchain preflight, and run-backed roles only.
- [DeepSeekLiveLabOrchestrationPipeline_2026-06-27.md](./DeepSeekLiveLabOrchestrationPipeline_2026-06-27.md) — **DESIGN/pre-build: offload the live-lab run+triage loop to DeepSeek via a new `deepseek_live_lab` MCP function.** Three-tier hierarchy (main agent owns all code changes + security calls; `deepseek-v4-pro` orchestrates the lab read-only-on-repo but executing a *confined allowlisted* lab-action set — launch/progress-stage/restart-VM/clean-env/recover/link-nodes; `deepseek-v4-flash` disposable research+verify sub-agents). The main agent stops hand-driving the orchestrator: one call → v4-pro runs the target area via a structured runbook prompt (main-agent-provided or auto-fetched `brief`, optional per-call tweak) → on fail, a **rigid non-negotiable multi-review pipeline** (Flash research → Flash verify-claims → v4-pro repo-review, all grounded, no code changes) → one verified report back. Includes the confirmed model probe facts (canonical `deepseek-v4-pro`/`-flash` ids, both tool-call-capable, pro reasons by default with no working effort-dial), the confined tool surface mapped to existing orchestrator/recovery/lab-state entry points, the stage-by-stage long-run execution model, the StructuredReport schema, and the 7-step implementation plan. **No DeepSeek tier ever writes the repo, runs gates, or makes the security call — it proposes, the main agent verifies and disposes.**
- [LiveLabSecurityTestCoverage_2026-06-22.md](./LiveLabSecurityTestCoverage_2026-06-22.md) — **threat-coverage map for the adversarial live-lab security suite** + **Tier 0/1/2 build-order priority section (2026-07-01)** at the top. Start here for "is this attack tested, and where, and what do I build next?" Contains: the 18 vulnerability classes × `SecurityMinimumBar` controls × existing coverage, the two CRITICAL trust-path bugs (FINDING-A/RSA-0009 membership revoke, FINDING-B/DD-03 revocation-blind ACL admission) — **both now code-fixed (`a23df5f`, `92e5748`), verified 2026-07-01, but still zero live-lab proof** — the corrected relay-forwarding status (lifecycle ✅ live-proven, frame-forwarding ⚠️ unproven on every OS, HP-3), the CPA-1 audit-chain-integrity ⚠️UNVERIFIED flag, the 111-item full backlog by surface, and (Phase 3, §9-11) the MCP-server + lab-monitor-GUI mechanics for onboarding new stages. **Priority section is stale as of 2026-07-04** — see the status-check doc below for what's actually done.
- [SecurityStageBacklogStatusCheck_2026-07-04.md](./SecurityStageBacklogStatusCheck_2026-07-04.md) — **read-only status check**, written after a 141-commit gap landed on `main`: re-verifies the 6 items this ledger's Tier 0/Tier 1(1-5) priority section calls unbuilt are in fact done and intact (`RSA-0009`/`DD-03`/`RT-2`/`GM-1`/`ENR-1`/`TOCTOU-1`/`DOS-1`, evidence-cited file:line), confirms the 2 lab-monitor GUI backlog items (no `StageCategory`/SECURITY section; `disabled_stages` toggle still has zero effect beyond the synthetic `linux_live_suite` name — code self-documents the lie) are still genuinely open and untouched, confirms Tier 1 priorities 6-16 + Tier 2 (RR-01, FCF-1/2/3, RPT-01/HP-3, S3-10, RSA-0063 live validator, KC-04, PH-7, KL-2/3/4, KC-07, CNT-1, PH-2/3) still have zero code, and flags 2 gaps in the new stage-contract program that touch the security-stage family (coverage-as-code gate enforcement still open; security stages exempt from the new abnormal-termination barrier). Ends with a suggested priority order for picking the backlog back up.
- [CrossPlatformRoleParityPlan_2026-06-21.md](./CrossPlatformRoleParityPlan_2026-06-21.md) — **RELEASE-BLOCKING COMPLETENESS MANDATE: every node role + capability (client/admin/anchor/exit/blind_exit/relay, +nas/llm) must work and be LIVE-LAB-PROVEN on macOS AND Windows, not just Linux.** Single source of truth consolidating the previously-scattered cross-platform parity gap: the live-proven status matrix per role × OS, what remains (mostly LIVE PROVING + a few impls — the topology/validator framework already landed), the per-role × per-OS live-lab acceptance matrix, known blockers (e.g. a WinNAT-capable Windows guest for the exit role), and the parity Definition of Done. **Rustynet is not cross-platform complete until every cell in its §3 matrix is green — do not let this go unaddressed.**
- [LiveLabStageContractPlan_2026-07-03.md](./LiveLabStageContractPlan_2026-07-03.md) — **the live-lab stage-contract program**: one data registry owns the stage vocabulary (`live_lab_stage_registry.rs`), every run emits a run-scoped stage manifest (`orchestration/stage_manifest.json`), the run matrix upserts by run key with interim/final row roles, a closed terminal-state taxonomy + conclusion barrier makes planned stages unable to evaporate silently, and always-on drift gates pin both orchestrators' vocabularies to the registry. Status per finding + deliberate gaps inside; source analysis in [LiveLabFindings_2026-07-03.md](./LiveLabFindings_2026-07-03.md)
- [LiveLabFindings_2026-07-03.md](./LiveLabFindings_2026-07-03.md) — the 2026-07-03 end-to-end live-lab findings pass (both orchestrators, monitor, run matrix, job plumbing, coverage, timers): ten verified findings with evidence and fix shapes; implemented by the StageContractPlan above
- [LiveLabStageTriageLedgerPlan_2026-07-16.md](./LiveLabStageTriageLedgerPlan_2026-07-16.md) — **PROPOSED**: a committed, per-`(stage, OS)` record of *what was tried*, so an agent picking up a failed stage can read the attempt history instead of re-deriving (or repeating) a fix. Two fields only — the error (auto-stubbed by the `--node` engine from the `error_detail` it already writes) and the patch (agent-filled before the verification run, so the ledger row's own commit IS the patch commit). Outcome is deliberately NOT a field: it is derived by joining the stub chain against [live_lab_node_stage_results.csv](../live_lab_node_stage_results.csv), so it cannot drift. Surfaced to agents via the `stage_triage_history` MCP tool; a gate refuses unfilled stubs. `--node` only — the engines' stage vocabularies do not overlap, so a blended history would be meaningless (same rationale as the run-matrix split)
- [LiveLabFindings_2026-07-12.md](./LiveLabFindings_2026-07-12.md) — findings from driving `live_managed_dns_validation` to green on the focused Rust `--node` topology (fixed + committed in `a1e49c1`: SSH host-key pin `:22` port-suffix + exit `Client` capability). Records the three problems that fixing it unmasked/surfaced: (A) `live_reboot_recovery_validation` fails on the missing `/etc/rustynet/assignment-refresh.env` in the focused setup (skip-if-absent vs provision-the-timer), (B) Linux Exit daemon-role intent `admin` vs live `RUSTYNET_NODE_ROLE=client` (stale-env), (C) latent client-less exit spec at `ops_e2e.rs:3365`. Tracked in the §5.1 item of [RustynetUnifiedTodoLedger_2026-07-10.md](./RustynetUnifiedTodoLedger_2026-07-10.md)
- [AnchorEnrollmentEndpointEnforcementDesign_2026-08-27.md](./AnchorEnrollmentEndpointEnforcementDesign_2026-08-27.md) — **design + first enforcement point for defect D-3: `anchor.enrollment_endpoint` had zero runtime enforcement.** The capability was parsed, rendered and advertised by every anchor/admin node while no code anywhere read it — and the verb it names, `EnrollmentConsume`, was open on **every** installed daemon (client included: every install template provisions the enrollment secret + ledger), so a valid token could be burned and a gossip push peer registered on a node that never claimed the capability. Answers the four design questions explicitly — what the endpoint serves (`enrollment consume`, never `admit`, never the bundle), what a peer verifies (the capability in quorum-signed membership, bundle-pull-then-enrollment ordering for first contact), the fail-closed choice when the capability is claimed but no listener exists (per-request serve-side refusal; a start-time abort is documented as REJECTED-for-now with the reason — it would brick every anchor until the listener ships), and whether it needs its own signed attestation (**no**, with the reasoning traced to the bundle-pull review's own rejected alternatives — an anchor-minted attestation is the trust-authority shape `SecurityMinimumBar.md` §6.C forbids, and the token's HMAC already authenticates the only payload). Landed: a per-request capability gate in `handle_enrollment_consume` mirroring the bundle-pull serve gate, the generalised `snapshot_bytes_have_capability` read-side primitive (which also tightens the pre-existing bundle-pull gate to require an **Active** node), and 11 tests (9 of them negative). **Not** landed and deliberately left to the repo owner: the LAN-exposed enrollment listener (§7) — a network-exposed pre-auth parser, which per the bundle-pull precedent needs an owner decision plus an independent adversarial review, not a defect-ticket patch.
- [AnchorBundlePullRollbackWatermarkInvestigation_2026-07-20.md](./AnchorBundlePullRollbackWatermarkInvestigation_2026-07-20.md) — investigation → **IMPLEMENTED (`e0cc8e5`, merged to `main`)** fix for the stale-cache rollback vector left open by the review doc below. Confirms the exploit with a concrete step-by-step scenario using real function names, surveys prior art (TUF's version-watermark vs. timestamp-freshness split; Certificate-Transparency-style equivocation defenses as a separate, harder problem this doesn't try to solve), and makes the key finding: `rustynetd` **already shipped and unit-tested** a `MembershipWatermark` mechanism with exactly the needed monotonic-epoch/anti-fork/TOFU-on-absence property, built for daemon bootstrap and never wired into `anchor pull-bundle`. Implemented by extracting it into `rustynet-control` and reusing the same on-disk file rather than building anything new; 3 new tests prove the TOFU case, the exact vulnerability's closure, and the honest residual limit (deleting the watermark itself, not just `--output`, is a real, acknowledged TOFU-reset boundary — pinned by test, not hidden).
- [AnchorBundlePullAttestationSecurityReview_2026-07-20.md](./AnchorBundlePullAttestationSecurityReview_2026-07-20.md) — **design, implementation, and 3-independent-reviewer adversarial security review of the "membership head attestation" fix** for the anchor bundle-pull signature-verification gap (`SecurityMinimumBar.md` §3 control 2). Full trail: the design pass's rejected-alternatives reasoning, the implementation's deliberate deviations, all three reviewers' per-check verdicts (one real defect found+fixed — quorum counted by signer slot not distinct key, closed at `MembershipState::validate()` and found to reach further than intended, blocking state-root derivation for the vulnerable roster shape everywhere in the crate, not just at the final check), and one real defect that was left **open** — a stale-local-cache rollback vector, since closed (see the investigation doc above). Merged to `main` (`1a786dc`, and the rollback fix at `e0cc8e5`).
- [DocCodeDiscrepancyAudit_2026-07-18.md](./DocCodeDiscrepancyAudit_2026-07-18.md) — **documentation-vs-code accuracy audit, 55 findings, individually re-verified against this repo's actual current state** (an earlier pass had run against a stale 7-week-old snapshot; every finding was re-checked here from scratch, not carried forward on trust). 54 of 55 remain applicable; only 1 (production membership-directory wiring) turned out genuinely fixed since. **Two Critical, both got worse over the last 7 weeks despite unrelated feature volume:** `SecurityMinimumBar.md`'s "TLS 1.3 enforced for control-plane APIs" claim still has no real TLS anywhere, and a *new* self-asserted `tls13_valid=true` field is now hardcoded at issuance regardless of any real handshake; the WireGuard-boundary-leakage gate (`CLAUDE.md` §10.3, `scripts/ci/check_backend_boundary_leakage.sh`) fails today (ran it live: exit 78) with 16 violations, up from 11. A third, narrower-than-first-thought gap: `MembershipIncidentResponseRunbook.md`'s revoke-node procedure now correctly denies *new* selection of a revoked peer, but an *already-connected* revoked peer's WireGuard config isn't torn down until daemon restart (`apply_revocation()` exists, is called only from tests). Full severity breakdown and current file:line citations in the doc; recommended priority order in its §6.
- [RustNativeNodeOrchestratorQualityAudit_2026-07-10.md](./RustNativeNodeOrchestratorQualityAudit_2026-07-10.md) — **the 2026-07-10 quality audit and implementation ledger for the Rust `--node` engine**: tracks 17 source-verified findings. RNQ-01–14 now have core code fixes (with explicit remaining live/fault-injection proof where applicable); RNQ-15/16 are partially extracted/consolidated; the real lab/product crate split RNQ-17 remains open. Covers verifier-before-bundle ordering, residue assertions, sealed rerun/resume evidence, honest role columns, failure-dominant finalization, validated dependency graphs, signal/context provenance, exact node identity, pre-cleanup diagnostics, bounded cancellation-aware fanout, early role rejection, module extraction, typed validator dispatch, and product separation.
- [RustNodeOrchestratorCompletionBrief_2026-07-12.md](./RustNodeOrchestratorCompletionBrief_2026-07-12.md) — **completion brief for the Rust `--node` orchestrator**: Definition of Done (Track A structural, Track B parity gate, Track C evidence, Track D promotion/retirement), read order, authoritative sources for completion checklist, track-by-track status summaries, and recommended next-step focus areas
- [MacWinStageParityPlan_2026-07-02.md](./MacWinStageParityPlan_2026-07-02.md) — **cross-platform stage parity plan**: the problem (macOS/Windows have 14 fewer one-off validators vs Linux), the two dispatch paths (DaemonProbeOp enum for cross-platform checks + raw SSH for Linux-only audits), and the Tier 0–4 implementation roadmap (no-op + single-column additions + cross-platform daemon subcommand ports + killswitch-dependent checks + OS-specific validators)
- [LiveLabExecutionEfficiencyPlan_2026-06-20.md](./LiveLabExecutionEfficiencyPlan_2026-06-20.md) — **the operating method for the same-LAN "drive defects to zero" live-lab loop**: the confirmed efficiency primitives (setup/run split, per-node `rebuild_nodes`, single-stage wrapper re-run, `what_will_deploy`, setup-stage resume), the fast inner loop (re-run only the patched node/stage, not the whole pipeline), the mandatory periodic full-validation gate, security/reliability guardrails (no stale-bundle reuse, no control weakening), the never-idle parallel-work protocol, the Windows front-load track, and the same-LAN execution order. Follow this while iterating Linux→macOS→Windows→cross-OS
- [CrossPlatformRoleParityRoadmap_2026-06-22.md](./CrossPlatformRoleParityRoadmap_2026-06-22.md) — **the execution roadmap that operationalizes the parity mandate**: remaining work + effort per mac/win role cell, the ordered implementation program (admin → blind_exit → role-transitions → relay-lifecycle → anchor-live, with relay-forwarding/Windows-exit/Windows-blind_exit explicitly parked + reasons), file-by-file plans for the first three cells, the FAIL-LOUD live-stage spec (live result = stage status; no dry-run-as-pass), and — the throughput half — the optimized test pipeline: **concurrent Windows+macOS labs** on disjoint Debian backbones, second-to-minutes per-role wrapper loops against warm guests, keep-warm via `--rebuild-nodes`, and the **all-on-`main` workflow** (`orchestrate` never re-checks HEAD mid-run, so commit-during-run is safe; `--source-mode working-tree` ships uncommitted edits). Builds on the ParityPlan (status) + EfficiencyPlan (primitives)
- [AutonomousSecurityParityPassLog_2026-06-24.md](./AutonomousSecurityParityPassLog_2026-06-24.md) — progress log for the 2026-06-24 code-only security+parity pass: the commits, the HIGH killswitch-bypass found by the pfctl-boundary adversarial review + its fix, the applied RSA findings (0008/0010/0014/0016/0017/0026/0055/0058), the deferred/kept-as-is ones (0001/0003) with rationale, the workflow-verified cross-OS parity audit (corrects the stale §9), and the code-complete-vs-blocked map
- [LiveLabCoverageAndHonestyAudit_2026-06-25.md](./LiveLabCoverageAndHonestyAudit_2026-06-25.md) — **the cross-OS live-lab coverage + capture-honesty gap map + remediation plan** (5-track parallel audit + external best-practice research, 2026-06-25). Answers "are we *thoroughly* stress-testing all 3 OSes for every role and exploitable surface, and is the captured evidence honest?": the role × OS matrix (macOS=1 dedicated live Rust binary, Windows=0; real parity only on relay + mixed-topology; exit-handoff mac/win is deceptive false-parity), the security-surface × OS matrix (every adversarial surface — endpoint-hijack/enrollment/gossip/STUN-ICE/signed-state — is Linux-only), the ranked capture-honesty findings (incl. the **Linux** fail-open NAT teardown + vacuous DNS proof the 06-24 pass missed), the best-practice benchmark (Mullvad active-probe negative test, Tailscale natlab NAT-matrix + WFP/Router parity, TUF/ByzzFuzz/Jepsen chaos, `pfctl-rs`, Cure53/X41 audit-evidence bar), and the risk-ordered Wave 0–5 plan. **nas/llm have zero coverage on any OS.** Companion to `LiveLabSecurityTestCoverage_2026-06-22.md` (threat-class map) + the ParityPlan (status)
- [FocusedLiveLabRoleGapAnalysis_2026-07-02.md](./FocusedLiveLabRoleGapAnalysis_2026-07-02.md) — focused static analysis of the three near-term unconfirmed/hardening targets: Linux `blind_exit`, macOS `admin`, and Windows `anchor.bundle_pull`. Records what each live stage must demand, what currently exists in code/docs, the most likely failure point, and the concrete work needed to pass without weakening security criteria. No live labs were run for this analysis.
- [LiveLabWave0_LinuxHonestyFixes_2026-06-25.md](./LiveLabWave0_LinuxHonestyFixes_2026-06-25.md) — **Wave 0 plan + implementation spec** from the coverage audit: the Linux honesty fixes (fail-open exit-NAT teardown F0.1–F0.3, vacuous DNS-leak proof F0.4–F0.6, relay dry-run-as-pass honest demotion F0.7, two-hop config-text-only F0.8, lan-toggle weak-negative F0.9, demotion-residue internal_prefix F0.10), each with file:line, the exemplar pattern to copy, the exact fix, the test to add, the disjoint per-agent file-ownership map, shared JSON contracts, and the Wave-0 Definition of Done. All Linux-compilable + unit-testable, no live lab
- [LiveLabWave5Chaos_InertScaffolds_2026-06-25.md](./LiveLabWave5Chaos_InertScaffolds_2026-06-25.md) — **Wave 5 (chaos) plan + spec**: implement the 3 inert chaos scaffolds for real — `clock_attack` (libfaketime per-process clock skew → TUF freeze/rollback assertions), `crash_recovery` (kill -9 at trust-state persistence boundaries → atomic old-or-new, no watermark-downgrade), `resource_exhaustion` (decompression-bomb/endless/oversized ingest → bounded-cap reject, no panic). Untested on ALL OSes today; Linux-targeted live tests that compile + unit-test here, live-run-pending. Templates = the 5 real chaos tests; honesty rules + disjoint per-binary ownership; do-not-touch the shared harness
- [LiveLabWave2_CoreRoleCrossOsParity_2026-06-25.md](./LiveLabWave2_CoreRoleCrossOsParity_2026-06-25.md) — **Wave 2 plan + spec**: port the Linux core-role assertion binaries (exit-handoff *real failover*, two-hop, lan-toggle/blind_exit, managed-DNS, role-switch, network-flap, reboot-recovery, enrollment-restart) to drive macOS/Windows guests via the cross-OS `RemoteShellHost` seam (runtime `match platform` branches, not `cfg`), removing the `enforce_linux_only_until_validator_lands` gates. Deliverable = code-complete + unit-tested, **live-run-pending**. Includes the relay/mixed-topology templates, the no-fake-pass + flag-daemon-gaps honesty rules, the disjoint per-binary agent-ownership map, Batch A/B split, and the DoD
- [LiveLabWave1_IntegratedPipelineHonestSkips_2026-06-25.md](./LiveLabWave1_IntegratedPipelineHonestSkips_2026-06-25.md) — **Wave 1 plan + spec**: make the rust-native pipeline's cross-OS "reported-skip" cells (Windows relay deploy+validation, macOS/Windows anchor bundle-pull runtime) return `StageOutcome::Skipped` (→ `RunStatus::Partial`) instead of `Passed`-with-a-side-car-JSON, so a Windows-relay / macOS-anchor run no longer shows a green run/stage. Includes the safety analysis (Skipped deps don't block downstream; empty-role labs still pass), per-stage fixes, tests, and the per-node-matrix follow-up that's out of scope
- [LinuxBlindExitDataplane_2026-06-25.md](./LinuxBlindExitDataplane_2026-06-25.md) — **closes the fail-OPEN `blind_exit`-on-Linux gap** the role × OS matrix surfaced: membership permits `blind_exit` on Linux but the dataplane ignored the flag and ran the *regular* NATing exit (masquerade + unrestricted forward + own-egress allow — none of the hardening). New `linux_blind_exit` nftables module (mesh-scoped final-hop forward, no NAT, local-origin tunnel-only, terminal drop, bounded mesh-CIDR via the shared verifier, irreversible rollback) mirroring the reviewed `macos_blind_exit` PF anchor; `apply_nat_forwarding`/`rollback_nat_forwarding` wired; privileged-helper argv allowlist extended (least-privilege `flush chain … forward` + mesh-scoped saddr rule). 13 new unit tests (builder/evaluator/allowlist) pass; fmt+clippy clean. **Live-run-pending** (lab proves the live `nft` apply). Also records a separate deferred finding: the framed `PrivilegedCommandClient` made every line-based `spawn_privileged_capture_helper` socket test latently broken (masked by the Linux-CI bootstrap failure)
- [CrossPlatformCiHealth_2026-06-25.md](./CrossPlatformCiHealth_2026-06-25.md) — **the cross-platform-CI breakage cleanup + the windows-gnu cross-clippy runbook**: how the Windows CI lints are reproduced locally on Linux (`x86_64-pc-windows-gnu` cross-clippy over the 10 CI packages) with **zero CI round-trips**, the commits that cleared ~80+ pre-existing Windows clippy errors + the crypto/local-security/backend-wireguard lints, the **real Windows trust-state persist bug** fixed (`atomic_write_secure` parent-dir fsync → `cfg(unix)`, mirroring the rustynet-crypto fix), and the remaining CI TODOs (macOS flaky `vm_lab` subprocess tests — left; Debian/Linux-E2E `cargo: not found` bootstrap — deferred; two behavior-preserving Windows dead-stub removals to review; the Windows Security-gates step that runs `cargo audit`/`cargo deny` for the first time once the build is green). Companion to the coverage audit §8 TODO list
- [CrossOsRoleSwitchPlan_2026-06-24.md](./CrossOsRoleSwitchPlan_2026-06-24.md) — design for the **live cross-OS role-transition** parity cell (the ParityPlan §3 row the Roadmap §4 cell #4 depends on): reuse the single verified apply path `refresh_signed_state_with_reason` + the `StateRefresh` IPC (never a second/weaker branch), the per-OS role-flip seam (launchd plist rewrite + bootout/bootstrap on macOS; `windows_service` StateRefresh on Windows), the LocalOnly/SignedMembership/Irreversible ordering invariants (deploy-before-advertise, undeploy-before-revoke, blind_exit re-enroll through the single-use token path), and the FAIL-LOUD stage spec. Supersedes the gitignored `state/cross_os_role_switch_plan.md` the matrix previously cited
- [SignedMembershipTransitionSigningSubflowDesign_2026-08-27.md](./SignedMembershipTransitionSigningSubflowDesign_2026-08-27.md) — **D-4a: the capability-signing sub-flow the SignedMembership transition kind was missing.** The planner + concrete-action executor are real (contra the RoleSwitchPlan's stale "design-only" status line, corrected in-place); what was absent is everything after the local side effects — nothing emitted, signed, applied, or refreshed the membership capability update; the operator had to hand-chain `membership propose-set-capabilities`/`anchor advertise` → `sign-update` → `apply-update` (+ `assignment issue` when the exit set changes). Answers the four design questions: split-station signing (every signing op runs where its key already lives; approver key never moves; quorum>1 terminates at a partially-signed artifact), the generalized §10.7 ordering table per capability (deploy→emit→sign→publish / undeploy→revoke, with the documented exit advertise-leads inversion on ADD only), per-step-boundary partial-failure dispositions (every window is availability-only — the revoked-but-still-running state is impossible by construction), and idempotent re-run resumability (the sign→publish signed artifact is the one TTL-bounded checkpoint). Landed: the pure `rustynet-control::role_signing_subflow` step sequencer, 9 ordering unit tests, and a cross-crate contract test pinning the sequencer's local phase to `role_cli`'s executable action order. Driver placement, health-check semantics, and the remove-path availability window are owner decisions (§8); live proof is D-4b.
- [RustynetDataplaneExecutionPlan_2026-05-18.md](./RustynetDataplaneExecutionPlan_2026-05-18.md) — source-of-truth plan for the cross-network dataplane track (peer-distributed coord, home-server-as-zero-ingress-relay, uPnP/IPv6/ICE, enrollment-token onboarding, anchor-role formalisation, 8-role user-selectable surface incl. service-hosting nas/llm); supersedes nothing but is the active ledger for D2-D13 work
- [CrossNetworkTraversalDesignDecisions_2026-07-19.md](./CrossNetworkTraversalDesignDecisions_2026-07-19.md) — **DESIGN PROPOSAL / owner-decision framing (no code written)** resolving the open NAT-traversal design questions the unified ledger §8 leaves for D14.c–f. Verifies current state against code (STUN client is transport-only with zero classification; two parallel signed-message systems — the self-signed `GossipBundle` with no type-discriminator byte, and the lab-only authority-signed `SignedTraversalCoordinationRecord` that already carries `probe_start_unix`; port-delta + mailbox are 100% greenfield). Frames **D14.c** (honest NAT/CGNAT discovery) as a buildable advisory module whose only subtlety is that public STUN can't do RFC 5780 CHANGE-REQUEST → `Unknown`-is-conservative; poses the **load-bearing D14.d decision** (punch-timing trust model: **Option A self-signed proposal over gossip + unilateral accept — recommended** vs Option B authority-signed — rejected as reintroducing a coordination host) which needs a bounded gossip-wire-version + type-tag change once picked; and keeps **D14.e port-delta** + **D14.f mailbox** GATED with the risk analysis (both need owner sign-off; mailbox needs a written threat model first). Companion to the dataplane plan §D14 (which owns the slice IDs + bans)
- [CrossDistroPortabilityFindings_2026-07-29.md](./CrossDistroPortabilityFindings_2026-07-29.md) — **second cross-network run on deliberately different guests and distros** (Fedora 44 + Rocky 10.2 aarch64, Fedora 42 x86_64) to test whether the traversal fixes were Debian-specific. They are not: Fedora and Rocky both reached DataplaneApplied at the correct MTU. Records six portability issues found on the way — one **product defect** (a symlinked `/etc/resolv.conf`, the systemd-resolved default, fails DNS apply and cascades to the WireGuard backend never starting), plus Rocky/RHEL sudo `secure_path` omitting `/usr/local/bin` under the bootstrap's bare-name spawn, the CLI executable-path pin, the privileged helper being a separate service that must be restarted alongside the daemon, a 2000 ms helper IPC timeout that reads as a framing error on small guests, and non-uniform freshness windows in `/etc/default/rustynetd`.
- [CrossNetworkTraversalEvidence_2026-07-29.md](./CrossNetworkTraversalEvidence_2026-07-29.md) — **first live evidence that rustynet's own NAT traversal works across two genuinely separate physical networks** (Mac UTM fleet 192.168.64.0/24 behind 80.169.236.70; Ubuntu KVM fleet 192.168.121.0/24 behind 213.233.155.131). Unblocks the "no second network" constraint the substrate spec was parked on. Records three defects found and fixed (`cef5282c` boot killswitch never received the STUN allow-list; `13bb4a45` outbound WireGuard allowed only by destination port, which NAT traversal breaks; `caf80cbd` privileged-boundary argv schema), the packet-capture proof of a hole-punched WireGuard handshake across both NATs, measured D14.c NAT classification (Ubuntu cone, Mac symmetric, no uPnP/PCP or IPv6 either side), and five open gaps — MTU not adapted to the punched path (bulk TCP stalls), 30% loss, `path_live_*` under-reporting, reflexive endpoints never published back into the signed bundle, and kernel-WireGuard structurally unable to run STUN.
- [CrossNetworkDeploymentEvidence_2026-07-30.md](./CrossNetworkDeploymentEvidence_2026-07-30.md) — **live deploy of `efee52c1` to the `xnet3` pair, retiring the session's biggest open risk.** `93dbd421` made membership decoding refuse a snapshot with no `capabilities` field and fail closed; this proves, on a real daemon restart against real persisted state, that both nodes reload cleanly (`membership_epoch=2`, 0 error lines) — verified with **real mesh traffic, 0% loss**, not the `path_live_*` self-report §4.3 of the 2026-07-29 evidence warns is unreliable. Also records: the fleet holds **two separate meshes** (`xnet3` healthy same-subnet; `xnet2` `FailClosed` on stale bundles, which is what an unattended cross-network mesh decays into); **measured proof the dataplane does not depend on Tailscale** (peer endpoint is a local address, route goes straight out the guest NIC, and Tailscale is not installed on the guests at all); and two corrected beliefs — the Mac and `ubuntu-kvm-1` repo lineages have **converged** (`f22be5af` is now an ancestor of `efee52c1`, 97-commit fast-forward), and the fleet was **not** uniformly stale. Names D14.d (publish discovered reflexive candidates over gossip) as the remaining cross-network blocker, since it also closes the §4.4b same-site hairpin gap.
- [D14dGossipCandidateAndPunchSpec_2026-07-30.md](./D14dGossipCandidateAndPunchSpec_2026-07-30.md) — **`D14.d` label disambiguation, the punch-timing spec, and a record of three refuted designs.** Does NOT specify candidate self-sustenance: that is [TraversalSelfSustenancePlan_2026-07-23.md](./TraversalSelfSustenancePlan_2026-07-23.md) Design A, already APPROVED and adversarially reviewed, and this doc defers to it rather than duplicating it. Splits `D14.d` into **d1 (= that plan, not new work)** and **d2 (punch timing, genuinely unbuilt)**. §3 records what was refuted and why, so it is not re-derived: a new gossip message type whose safety claim was false by arithmetic (`0x02` *is* wire version 2); a "daemon already publishes, just bridge it" design (it builds candidate sets but `attach_gossip_runtime` has **no production call site** — gossip is dormant); and three issuance options (daemon self-issues / automated CLI / anchor-side), **all three rejected on security grounds** because the signed payload has no per-candidate node attribution and the endpoint-hint key also signs relay session tokens and relay fleet bundles. Also corrects a premise used throughout: receivers **do** validate candidate scope, but scope is not ownership — a victim's public IP passes as a `Host` candidate, which is why return-routability is mandatory.
- [CrossNetworkSubstrateIntegrationSpec_2026-06-21.md](./CrossNetworkSubstrateIntegrationSpec_2026-06-21.md) — **focused integration spec to make the cross-network live-lab stages actually run.** Resolves the one open design decision (substrate↔validator mapping: Tier A netns = deterministic NAT-matrix gate via classification tooling + a new daemon-in-netns validator; Tier B vxlan = the existing SSH e2e validator suite on separate-VM cross-NAT; Tier C slirp = cross-OS smoke), specifies the exact orchestrator wiring (`--cross-network-substrate` selector + `stage_run_cross_network_substrate_setup/_teardown` at the seam, orchestrator line 8291, before the per-profile loop), the phased plan (X1 Tier A → X2 Tier B → X3 the three §4.1 stages → X4 Tier C), files-to-change, gaps (netns lacks `double_nat_cgnat`; vxlan lacks uPnP/v6 modifiers), and the §D5.1 acceptance criteria. Companion to the dataplane plan §D5.1 (which owns the why + NAT vocabulary + pass criteria). ~80% of the substrate is already built; remaining work is integration glue, not invention
- [LiveLabVmConnectivityImplementation_2026-07-10.md](./LiveLabVmConnectivityImplementation_2026-07-10.md) — **execution ledger for the VM connectivity rulebook (§15.8 slices A–E)**: per-slice checklist + evidence for the dual-plane lab-network program. Slice A (read-only typed network-profile model `vm_lab/network_profile.rs` + audit/preflight `vm_lab/network_audit.rs`, reviewed manifests under `profiles/vm_lab/network/`, `ops vm-lab-network-audit`/`vm-lab-network-preflight`, redacted `state/vm_network_evidence.json`) status and the live-audit baseline findings live here. No VM network mutation may happen before Slice A acceptance is recorded in this ledger.
- [DataplanePerfBacklog_2026-06-12.md](./DataplanePerfBacklog_2026-06-12.md) — remaining hot-path performance items after the 2026-06-11/12 pass (engine outcome sink, relay await-recv, utun readv/writev, endpoint index), each with expected impact, approach, invariant pins, and the criterion bench to measure against; also records what landed (`6045ba7`, `611dc29`, `71255b5`) with measured numbers
- [EfficiencyAndAdvancedTechniqueOpportunityCatalog_2026-07-19.md](./EfficiencyAndAdvancedTechniqueOpportunityCatalog_2026-07-19.md) — **RESEARCH CATALOG, unscheduled, not a plan.** 31 code-grounded, independently-verified findings (11 subsystems: key custody, ACL/policy evaluation, membership/trust-state, enrollment, canonical serialization, NAT traversal, relay accounting, control-plane concurrency, build/test speed, CLI/daemon startup, Windows/service-hosting data paths) beyond what `DataplanePerfBacklog`/FIS-0001..0008 already cover — each with multiple candidate technique families (incl. Merkle/succinct-proof and blind-signature/ZK options where genuinely applicable, explicitly flagged where blocked by the audited-crate-only rule) and no chosen fix; the next agent designs and picks the approach
- [FableEfficiencyImplementationPlans_2026-07-19.md](./FableEfficiencyImplementationPlans_2026-07-19.md) — **PLAN-ONLY, unscheduled, no code written.** FIS-style implementation plans for 11 findings selected from the opportunity catalog above (BLD-1/2/3, CLI-2/3, RLY-1, CCY-1 narrow, CCY-3, WIN-3, NAT-2 narrow, NAT-3): each section re-verifies the catalog's current-behavior claims against the live tree (drift called out — e.g. CCY-3's shadow gossip state has zero readers; `rustynetd` deliberately compiles windows-native stubs cross-platform), picks the single best technique with runner-up reasoning, and gives a constraint check, phased S/M/L build path, and concrete pass/fail measurement per fix — strictly efficiency/dev-tooling/architecture-hygiene scope, nothing touching crypto, key custody, signature verification, ACL evaluation, privileged exec, route/killswitch mutation, or DNS fail-closed paths
- [NodeRoleTaxonomy_2026-05-21.md](./NodeRoleTaxonomy_2026-05-21.md) — canonical taxonomy for the six user-selectable node roles (D12): `relay`, `anchor`, `exit`, `blind_exit`, `client`, `admin`; preset compositions; transition matrix; per-platform eligibility
- [AnchorNodeRoleDesign_2026-05-21.md](./AnchorNodeRoleDesign_2026-05-21.md) — canonical design for the anchor node role (D11; one of the six presets in the taxonomy doc): role definition, what already exists in code, what needs building, per-platform host capability (Linux/macOS/Windows host; iOS/Android consume), refactor inventory, security controls
- [NodeRoleTaxonomyExtension_2026-06-11.md](./NodeRoleTaxonomyExtension_2026-06-11.md) — extends the six-role taxonomy to **eight** by adding the two service-hosting presets `nas` and `llm` (D13): the service-hosting role category, new `serves_nas`/`serves_llm` capabilities, eight-role transition matrix, tunnel-only + default-deny secure-exposure model, per-platform eligibility, and the §6.E security-control delta. Parent of the two role design docs below
- [NasNodeRoleDesign_2026-06-11.md](./NasNodeRoleDesign_2026-06-11.md) — deep dive for the `nas` node role: `rustynet-nas` sibling service, tunnel-only storage exposure with per-peer namespaces + at-rest encryption, the node-side **RustyBackup** client contract, per-platform, security controls, and build slices (D13.c)
- [LlmNodeRoleDesign_2026-06-11.md](./LlmNodeRoleDesign_2026-06-11.md) — deep dive for the `llm` node role: `rustynet-llm-gateway` in front of a loopback inference engine, **identity-from-tunnel (no API key)** + gRPC/HTTP-2 token streaming as plaintext inside the WireGuard tunnel, exit-node coexistence (internet via exit, LLM intra-mesh), admin access governance, the node-side **RustyAI** client contract, an industry survey (Tailscale / SPIFFE-SPIRE / LLM streaming transports), security controls, and build slices (D13.d)
- [ServiceHostingRolesDeltaPlan_2026-06-11.md](./ServiceHostingRolesDeltaPlan_2026-06-11.md) — delta ledger driving **D13**: current-state-vs-target gaps verified against the code, ordered slices D13.a–e, defect carry-overs from the security audits, gate plan, and live-lab readiness for `nas`/`llm`
- [ServiceHostingRolesRoadmap_2026-06-11.md](./ServiceHostingRolesRoadmap_2026-06-11.md) — top-level program roadmap for the NAS + LLM roles: the document set, milestones M0–M6, dependency graph, per-milestone gate checklist, risk register, and status tracker. **Start here** for the service-hosting-roles program
- [ServiceHostingPhase2Design_2026-08-29.md](./ServiceHostingPhase2Design_2026-08-29.md) — phase-2 design for the NAS + LLM roles (makes roadmap M2–M3 buildable): exists-vs-needed inventory of the service-access-policy primitives (`grants.v1`/`peers.v1`/`scopes.v1`, `evaluate_service_access`, `VerifiedPeerIdentity` tunnel→identity, `LlmAccessScope`/`LlmScopePolicy`), the three fail-closed enforcement layers, the `ServiceExposureController` daemon-runtime wiring gap, per-milestone (P2-M1/M2/M3 + M5) task breakdown with acceptance criteria + verification, and owner-gated decisions (grant-without-scope posture, nas scope type, any new signed wire-format field)
- [RustydnsExitIntegrationDecree_2026-08-25.md](./RustydnsExitIntegrationDecree_2026-08-25.md) — operator decree: RustyDNS × Rustynet tandem integration — user-toggleable, standalone-first, fail-closed on the toggle; managed-DNS handoff + exit port-53 redirect mechanisms; phased DoD (compose e2e → `dns` service-kind toggle per D13 → live-lab stage)
- [RustydnsTandemIntegrationDesign_2026-08-27.md](./RustydnsTandemIntegrationDesign_2026-08-27.md) — security-first cross-product contract for signed per-exit RustyDNS activation, managed handoff, transparent DNS containment, authenticated readiness, OS lifecycle/residue proof, and Rust `--node` evidence.
- [RustydnsTandemPhase1Notes_2026-08-28.md](./RustydnsTandemPhase1Notes_2026-08-28.md) — phase-1 tandem ledger: `rustynet-control::tandem_dns` toggle state machine (`reconcile`/`contain_now`, closed 24-code reason vocabulary), `ServesDns` capability + `ServiceKind::Dns` presets/CLI plumbing, fail-closed invariants with negative tests, and flagged owner-gated sub-decisions (incl. prepare-intent signed wire format).
- [RustydnsTandemPhase2Notes_2026-08-29.md](./RustydnsTandemPhase2Notes_2026-08-29.md) — phase-2 tandem ledger (D-6b): `rustynet-control::managed_dns_handoff` per-client managed-DNS resolver-assignment decision (handoff / contained / no-handoff, readiness+scope+exit-proof+mesh-range containment, no fallback), the **owner-gated RustyDNS mesh-IP signed carriage flag**, remaining sub-decisions, and verification.
- [MasterWorkPlan_2026-03-22.md](./MasterWorkPlan_2026-03-22.md)
- [PlugAndPlayTraversalRelayDeltaPlan_2026-03-29.md](./PlugAndPlayTraversalRelayDeltaPlan_2026-03-29.md)
- [OpenWorkIndex_2026-04-17.md](./OpenWorkIndex_2026-04-17.md)
- [PlatformImprovementBacklog_2026-05-14.md](./PlatformImprovementBacklog_2026-05-14.md) — most recently updated cross-platform improvement ledger; the X1-X7 numbered tracks are the cleanest current path for code-only quality work
- [HomelabConnectivityParityDeltaPlan_2026-05-21.md](./HomelabConnectivityParityDeltaPlan_2026-05-21.md) — active delta ledger for closing the macOS/Windows tunnel connectivity gap vs Linux baseline; covers macOS IPv6 (M3), macOS wireguard-go prereq (M2), macOS key-retention bug (M1), macOS killswitch (M4), Windows host candidate enumeration (W1), Windows gateway detection (W2), Windows E2E test coverage (W3), Windows IPv6 audit (W4), and cross-platform uPnP IGD orchestrator (C1)
- [AnchorLiveLabAndCrossPlatformRoleDeltaPlan_2026-05-23.md](./AnchorLiveLabAndCrossPlatformRoleDeltaPlan_2026-05-23.md) — delta ledger covering three parallel tracks. Track A adds live-lab coverage for the anchor role (five sub-stages exercising bundle-pull, gossip priority, enrollment endpoint, port-mapping authority, and downgrade revocation); non-destructive scaffold has landed; enrollment/downgrade destructive sub-stages are follow-up work. Track B closes the topology-selection + per-platform validator gaps that previously blocked Windows / macOS from being the active exit_server / relay / anchor in the orchestrator; **all seven steps now landed** (topology selector, macOS exit validators, platform-aware role-transition planner + per-OS exit/relay installers, Windows active-exit promotion stage, macOS relay lifecycle dry-run, and `ops e2e-bootstrap-{macos,windows}` non-Linux genesis verbs). Track C adds an adversarial / fault-injection / chaos suite (8 categories, 30+ stages) covering daemon crashes, clock attacks, signed-state forgery, crash recovery, resource exhaustion, network impairment, membership poisoning, and privileged-boundary stress — Track C remains planning-only

## Active Phase Checklists

These are the phase-local hardening checklists that are still open. The
phase checklists that finished (Phase 1, 2, 3, 5) have moved to
[`../done/`](../done/README.md); see that index for the archived slices.

- [Phase4LiveLabEvidenceRefreshChecklist_2026-04-12.md](./Phase4LiveLabEvidenceRefreshChecklist_2026-04-12.md) — still open on fresh-install + canonical cross-network evidence regeneration for a clean current `HEAD`
- [Phase5ReleaseReadinessSummary_2026-04-12.md](./Phase5ReleaseReadinessSummary_2026-04-12.md) — the Phase 5 checklist itself is archived; this summary is kept active because it is the operator-facing readiness picture and still records remaining full release-gate blockers
- [Phase6CrossNetworkAndSharedTransportChecklist_2026-04-13.md](./Phase6CrossNetworkAndSharedTransportChecklist_2026-04-13.md) — code-side cross-network/shared-transport hardening done; canonical cross-network + extended soak evidence still to regenerate

## Active Plans And Backlogs

- [BlindExitPcbHardwarePlan_2026-07-22.md](./BlindExitPcbHardwarePlan_2026-07-22.md) — **new workstream, DESIGN stage: a dedicated, minimum-traceability PCB appliance implementing the `blind_exit` node role** (a hardware counterpart to the existing software-only role, not a code change). Supersedes the undescribed docx report below as source of truth — that report's network diagram assumed NAT is present, which contradicts the actual no-NAT `blind_exit` dataplane (`linux_blind_exit.rs`/`macos_blind_exit.rs`), and its cited hardware source file doesn't exist in this repo. Locks: LAN-side/single-Ethernet topology behind the user's router (matches the no-NAT code); off-the-shelf Allwinner H616/H618-class SoC for v1 (open RISC-V softcore on an FPGA and a real ASIC shuttle were both evaluated and parked as a v2/stretch track, not rejected outright); Gigabit Ethernet via EMAC0 + external RGMII PHY; a 2.4GHz-only WiFi module as failover-only backup; kernel WireGuard backend, not userspace/boringtun; throughput targets derived from "10 devices streaming video comfortably" (100Mbps aggregate Ethernet, ~33Mbps aggregate WiFi failover, both worst-case peak not continuous); no vendor secure boot / no vendor Android BSP given Allwinner's historical BSP-backdoor precedent (confirmed not H616-specific); the eFuse chip-ID treated as a traceability liability, never exposed; 1GB RAM / 8GB soldered eMMC (not microSD) with signed A/B updates and no swap-on-flash; and the Allwinner FEL USB recovery mode as the hardware-level factory-reset path. §5 lists what still needs a real hardware benchmark before any BOM/schematic is finalized.
- [UbuntuHostLabControlFindings_2026-07-23.md](./UbuntuHostLabControlFindings_2026-07-23.md) — **live capability-parity verification of `ubuntu-kvm-1`**: confirms the KVM host has agent-driven VM status, per-guest IP, source sync, on-guest release build of `rustynetd`+`rustynet-cli`, and install — all working. **But a full live-lab run on box guests is still BLOCKED**, and the doc logs **5 pre-existing, non-box-specific bugs by severity**. Headline: BUG-BOX-5 (**HIGH** — the `--node` orchestrate pipeline's first node-readiness gate (`mod.rs:12222`) is UTM-only, so a run on libvirt guests aborts at stage 1 with *"local UTM discovery did not report the selected aliases"*; confirmed live, this is why the box's ledgers have zero `linux-x86` rows). Then BUG-BOX-1 (MEDIUM — `get_vm_diagnostics` calls `vm-lab-collect-artifacts --report-dir` but the CLI requires `--output-dir`, artifact collection always exits 64), BUG-BOX-4 (MEDIUM — `launch_live_lab_on_host` omits the required `--known-hosts-file`, a node run self-destructs at arg-validation), BUG-BOX-2 (LOW-MED — the manual `restart-runtime` bootstrap phase assumes a systemd unit no phase installs → status-5 on a fresh guest; the orchestrate deploy path is unaffected), BUG-BOX-3 (LOW — MCP tool calls time out client-side at ~60s on slow ops, CLI-direct is the documented workaround). Companion to `LinuxVmHostPlan_2026-07-14.md` (the owning program).
- [UbuntuHostLabControlRemediationPlan_2026-07-23.md](./UbuntuHostLabControlRemediationPlan_2026-07-23.md) — **DESIGN / PATCH PLAN (no code written)** for the 5 findings in the doc above: per-bug current-behaviour (file:line), fix options + tradeoffs, recommended fix, test, blast radius, and a sequencing/verification plan. Headline BUG-BOX-5 fix = controller-kind dispatch in the `--node` readiness gate (`orchestrator/readiness.rs:57`/`:165`, NOT the legacy-bash `mod.rs:12222` gate — location corrected after an adversarial review that caught the original draft citing a path `--node` never executes), reusing the already-libvirt-wired `observe_local_utm_target_ready` observer so the `--node` pipeline stops calling UTM discovery unconditionally. BUG-BOX-4 = inject `--known-hosts-file` into the launch template mirroring the existing `--ssh-identity-file` default. BUG-BOX-1 = one-line MCP flag fix (`--report-dir`→`--output-dir`) + rebuild. BUG-BOX-2 = install the systemd unit in the manual chain via the existing `install_rustynetd_service` bin + fail-loud. BUG-BOX-3 = doc/description containment. §8 has the owner-decision queue. None are security-sensitive; full §7 gates apply.
- [LinuxVmHostPlan_2026-07-14.md](./LinuxVmHostPlan_2026-07-14.md) — **stand up a dedicated x86-64 Linux box as a second, larger VM host (~10 guests) driven by libvirt/QEMU/KVM, integrated with the run-on-host `--node` orchestrator.** Owner-ratified **Tier 2** (full UTM-parity lifecycle: start/stop, ship code, compile, run the live-lab suite); §11 tracks the gated increment plan. Records that **no prior dedicated-Linux-host plan existed** and the whole program assumed the single macOS+UTM host. Key finding: the `--node` orchestrator is **SSH-throughout except a thin `utmctl` power+IP readiness gate**, so the libvirt work is confined there and Linux guests need no guest-agent channel. VM-lifecycle was hardcoded to `VmController::LocalUtm`/`utmctl` (no libvirt/virsh anywhere); **increment 1 landed** — the `VmController::Libvirt { domain, connect_uri }` variant + parser + fail-closed power/IP gates (virsh power = increment 2, IP discovery = increment 3). Strongest motive: an x86 KVM host does **nested virt**, unblocking the Windows **WinNAT/exit** parity cell the Apple-Silicon UTM lab structurally cannot prove (§1.1). Includes the OS recommendation (Debian minimal + libvirt/KVM primary; Proxmox VE alternative), a flash/host-setup runbook, the networking model, security baseline, and the owner-decision queue
- [AnchorNodeRoleDesign_2026-05-21.md](./AnchorNodeRoleDesign_2026-05-21.md)
- [NodeRoleTaxonomy_2026-05-21.md](./NodeRoleTaxonomy_2026-05-21.md)
- [AnchorEnrollmentEndpointEnforcementDesign_2026-08-27.md](./AnchorEnrollmentEndpointEnforcementDesign_2026-08-27.md)
- [SignedMembershipTransitionSigningSubflowDesign_2026-08-27.md](./SignedMembershipTransitionSigningSubflowDesign_2026-08-27.md)
- [NodeRoleTaxonomyExtension_2026-06-11.md](./NodeRoleTaxonomyExtension_2026-06-11.md)
- [NasNodeRoleDesign_2026-06-11.md](./NasNodeRoleDesign_2026-06-11.md)
- [LlmNodeRoleDesign_2026-06-11.md](./LlmNodeRoleDesign_2026-06-11.md)
- [ServiceHostingRolesDeltaPlan_2026-06-11.md](./ServiceHostingRolesDeltaPlan_2026-06-11.md)
- [ServiceHostingRolesRoadmap_2026-06-11.md](./ServiceHostingRolesRoadmap_2026-06-11.md)
- [CrossPlatformSecurityGapRemediationPlan_2026-03-05.md](./CrossPlatformSecurityGapRemediationPlan_2026-03-05.md)
- [DiagnosticFunctionsRoadmap.md](./DiagnosticFunctionsRoadmap.md)
- [HeterogeneousLiveLabEvidence_2026-04-28.md](./HeterogeneousLiveLabEvidence_2026-04-28.md)
- [LinuxUserspaceSharedLiveLabReadinessDelta_2026-04-02.md](./LinuxUserspaceSharedLiveLabReadinessDelta_2026-04-02.md)
- [MagicDnsSignedZoneSchema_2026-03-09.md](./MagicDnsSignedZoneSchema_2026-03-09.md)
- [OsAgnosticOrchestratorAndWindowsPeerDeltaPlan_2026-04-27.md](./OsAgnosticOrchestratorAndWindowsPeerDeltaPlan_2026-04-27.md)
- [VmLabCapabilityCookbook_2026-04-14.md](./VmLabCapabilityCookbook_2026-04-14.md)
- [VmLabCapabilitySources_2026-04-14.md](./VmLabCapabilitySources_2026-04-14.md)
- [WindowsExitAndRelayDeltaPlan_2026-05-10.md](./WindowsExitAndRelayDeltaPlan_2026-05-10.md)
- [WindowsExitNodeRunbook_2026-06-04.md](./WindowsExitNodeRunbook_2026-06-04.md) — how to run + prove a **successful active full-tunnel Windows exit** live lab. The `active_exit` orchestration stage (drives `route advertise 0.0.0.0/0` over the daemon control pipe → asserts forwarding + NAT + client-egress `Get-NetNatSession`) is built, unit-tested, and fail-path-validated (r54). The ONLY remaining gate is a Windows guest with the WinNAT/HNS stack — the current `windows-utm-1` lacks it (`MSFT_NetNat` class absent). Includes the WinNAT-readiness verify one-liner, the default-route prereq, the exact run command, and the deliberate `role.rs` support-flag promotion step for after a green run
- [WindowsLabVmStabilityAndSessionModel_2026-04-30.md](./WindowsLabVmStabilityAndSessionModel_2026-04-30.md)
- [WindowsNodeBootstrapTriageVerdict_2026-08-28.md](./WindowsNodeBootstrapTriageVerdict_2026-08-28.md) — **CP-4 triage verdict: why Windows `--node` bootstrap has never passed. BOTH code and guest, code primary.** Names the failing step — `Ensure-WingetConfigurationDependencies` → `& winget configure` at `scripts/bootstrap/windows/Bootstrap-RustyNetWindows.ps1:1130`, throwing at `:1132` — with the guest's own verbatim stdout as proof (*"Configuration is not enabled. Run `winget configure --enable` to enable it."*). The **code** defect: the bootstrap hard-depends on WinGet **Configuration**, an opt-in per-machine feature that installs Git/PowerShell 7/rustup/**WireGuard**, and never enables or precondition-checks it — `Require-Winget` (`:548-571`) gates only on `winget.exe` presence, `Get-WindowsBootstrapToolingState` collects ten-plus WinGet facts but not the feature's state, and **nothing in the repository has ever run `winget configure --enable`**, so every fresh guest reproduces it and every historical green ran on a hand-prepared machine. Three corrections to the CP-4 evidence base matter as much as the verdict: **(1)** the headline "5 fails" is **3** — `bootstrap_hosts` was `skip` on two runs that died upstream at `preflight` (lab topology; a 3602 s guest clock skew); **(2)** the five rows are **four distinct causes**, not one, and one 2026-07-25 `windows-x86-1` failure stays **unresolved** because its report is on the unreachable `ubuntu-kvm-1`; **(3)** the `*_stage_bootstrap` roll-up records `fail` for a `skip`ped stage on **every OS column at once** (`live_lab_run_matrix.rs` `merge_status`, `:2215-2231`), so Linux and macOS counts are inflated the same way — same bug class as the §12.3 `traffic_test_matrix` aliasing. Second code finding: raw PowerShell CLIXML reaches `error_detail` because `AdapterError::Command` (`orchestrator/error.rs:154-156`) renders stderr verbatim while the repo's own `strip_powershell_clixml_noise` (`vm_lab/mod.rs:35006`) is wired only to the `utmctl exec` path — which is why the cause sat unread in the report for five weeks. Fresh 2026-08-28 guest health: `windows-utm-1` boots and answers ICMP with RPC/NetBIOS/SMB up but TCP/22, 3389 and 5985 all closed and no QEMU guest agent, i.e. **no remote management path**, so no remediation was applied and no run was launched (it would have failed upstream and written a sixth spurious bootstrap `fail`). Ends with a five-item fix backlog, W-FIX-1 (the code fix) first. No code changed
- [WindowsLiveLabReadinessPlan_2026-05-31.md](./WindowsLiveLabReadinessPlan_2026-05-31.md) — focused readiness ledger for running live-lab **on a Windows node**: `restart-runtime` GREEN; **N1.3/N2/N3 done + merged** (first WireGuard-NT tunnel bring-up, then killswitch + DNS fail-closed in protected mode); **G8 IPv6 fail-closed mechanism validated live** (apply/block/rollback under the killswitch; full leak-proof deferred — the lab has no global IPv6); **N4 (two-node mesh) blocked on lab networking** (Windows is LAN-only and no peer currently shares its subnet). Ordered path N1-N6 (single-node tunnel smoke → killswitch/DNS fail-closed → two-node mesh → roles → full matrix) + enablers E1-E3 (Windows CI gate, native WFP killswitch, gate-timing CSV); cross-links the detailed Windows/connectivity/role ledgers
- [WindowsUtmTransportArchitecture_2026-04-30.md](./WindowsUtmTransportArchitecture_2026-04-30.md)
- [WindowsWorkingNodePlan_2026-04-17.md](./WindowsWorkingNodePlan_2026-04-17.md)
- [ProductionTransportOwningWireGuardBackendPlan_2026-03-31.md](./ProductionTransportOwningWireGuardBackendPlan_2026-03-31.md)
- [RustynetComparativeVpnExploitCoverage_2026-03-14.md](./RustynetComparativeVpnExploitCoverage_2026-03-14.md)
- [SecurityAuditLedger_2026-06-18.md](./SecurityAuditLedger_2026-06-18.md) — **review-only**, file-by-file security audit ledger (RSA-#### namespace) against `SecurityMinimumBar.md` and the audit charter checklist. **COVERAGE COMPLETE: 594/594 tracked code/config files have a dated row** (Tier 0–4 + vendored), audited highest-blast-radius-first across 8 multi-agent fan-out batches with first-hand auditor verification of every load-bearing finding, **then an adversarial re-verification pass + theme sweeps (truncation / `#[allow]` / verification-bypass).** **76 findings raised → 2 withdrawn as false-positive → 74 standing: 0 Critical / 2 High / 15 Medium / 34 Low / 19 Info / 4 Question.** The two Highs: RSA-0009 (membership reducer non-determinism → revoke/key-rotation can't apply; AUDIT-040) and RSA-0063 (macOS bootstrap leaves `NOPASSWD:ALL` sudoers on failure → local privesc; AUDIT-045/RN-32) — both survived adversarial re-verification. Re-verifies + extends RN-* / AUDIT-* / HB-* (incl. AUDIT-006 now remediated, RN-03/04/10/N1 confirmed fixed); supply chain clean (`cargo audit` 0/210, `cargo deny` OK); includes a SecurityMinimumBar Critical-control trace, an adversarial-re-verification log, and Appendix A sources. **All findings are proposals awaiting human approval — no production code/crypto/config changed.**
- [SecurityRemediationPlan_2026-06-19.md](./SecurityRemediationPlan_2026-06-19.md) — sequences the audit ledger's 73 findings into remediation **waves** (P0 = the 2 Highs RSA-0009/RSA-0063; P1 = Mediums grouped by 7 systemic themes A–G; P2 = Low/Info by category) with per-item proposed fix + verification test + effort estimate, plus a 6-item owner-decision queue for the Question findings. Planning only — no code change; flip ledger rows `open→applied` as fixes land.
- [SecurityHardeningBacklog_2026-06-01.md](./SecurityHardeningBacklog_2026-06-01.md) — actionable hardening TODO tracker (not a fresh review). Section A: net-new items on the single-node Windows smoke/harness code added 2026-05-31..06-01 (ephemeral cleartext key uses plain unlink not secure-scrub; `write_runtime_private_key` `0o600` is unix-only so smoke `%TEMP%` keys rely on inherited ACLs; bare-`netsh` recovery paths; Drop guard skips the IPv6-block rule; ipv6-smoke probes a hardcoded public IPv6; vm-lab PowerShell-string surface). Section B: the highest-priority OPEN findings from `SecurityReview_2026-05-24.md` **re-verified first-hand on `main` `e01dd64` (2026-06-01)** with current file:line (P0 RN-03 discarded force_fail_closed 10/44 sites, RN-04 pre-killswitch opt-in+Linux-only, RN-05 non-`node:` selectors bypass revocation, RN-06 Windows killswitch IPv4 LAN egress allow-all, RN-07 IPv6 leak [G8 partial], RN-11 permissive-on-empty) plus a best-practice assessment — the review's directions are sound, but the strongest Windows-killswitch fix moves the egress policy to **WFP** (not netsh rules, per E2), the pre-killswitch should be mandatory+cross-platform, and empty membership/context should deny. Records that G8 partially remediates RN-07 while RN-06 (IPv4 LAN-egress scoping) remains the larger open piece
- [SecurityReview_2026-05-24.md](./SecurityReview_2026-05-24.md) — firm-grade security review across six domains (privileged boundary, crypto/key custody, trust/anti-replay, untrusted-input/IPC, dataplane fail-closed/killswitch, secret-hygiene/supply-chain). 38 findings (7 High / 9 Medium / 17 Low / 5 Info) with CWE, file:line, exploit scenario, and a prioritized P0/P1/P2 roadmap; load-bearing findings verified first-hand. Now also includes scope/asset/trust-boundary mapping, a threat model (10 actor profiles), methodology limitations + residual-risk statement, indicative CVSS, compound exploit chains, a CLAUDE.md/SecurityMinimumBar compliance matrix, a master finding-status tracker, reproduction notes, and references/glossary. Remediation log RL-1..RL-8: **8 findings fixed + tested** (RN-01 membership-decoder DoS, RN-14 unsafe-lint enforcement, RN-22 ed25519 verify_strict, RN-24 zeroize, RN-23 keychain id, RN-19 symmetric argv gate, RN-17 helper peer-cred TOCTOU check, RN-15 CI --locked), RN-21 accepted (fail-closed). Headline open P0s: dataplane fail-open paths, Windows killswitch/IPv6 leaks, policy default-allow (revocation bypass)
- [TestCoverageImprovementPlan_2026-05-24.md](./TestCoverageImprovementPlan_2026-05-24.md) — owning ledger for the test-coverage track; prioritized P0/P1/P2 workstreams (roles.rs zero-coverage, signed-state/crypto/dns-zone negative tests, sysinfo parse/IO split, dataplane default-deny truth tables, gossip anti-replay persistence) plus tooling levers (llvm-cov ratchet, proptest, expanded fuzzing, broadened coverage-gate floors)
- [SerializationFormatHardeningPlan_2026-03-25.md](./SerializationFormatHardeningPlan_2026-03-25.md)
- [UdpHolePunchingAndRelayTraversalPlan_2026-03-07.md](./UdpHolePunchingAndRelayTraversalPlan_2026-03-07.md)
- [UdpHolePunchingHP2IngestionPlan_2026-03-07.md](./UdpHolePunchingHP2IngestionPlan_2026-03-07.md)
- [LiveLabTransitionOrderingStageDesign_2026-09-01.md](./LiveLabTransitionOrderingStageDesign_2026-09-01.md) — **DESIGN ONLY, NO CODE — implementation pending adversarial review + lab availability.** GAP-1 from the coverage gap hunt: a `--node` stage (`live_role_transition_ordering_validation`, rename+extend of the tunnel-liveness-only `role_switch_matrix`) asserting AGENTS §10.7 / SecurityMinimumBar §6.D ordering per transition kind — relay deploy-before-advertise, undeploy-before-revoke, exit-NAT teardown-before-revocation with residue==none, blind_exit blocked-before-side-effect irreversibility, and an append-only role-audit entry per transition (`rustynet-control` `role_audit.rs` chain re-verified). Specifies per-assertion observability, StageSuite placement, FAIL-LOUD rules, VM-free offline unit tests, and the adversarial-review checklist. GAP-2 from the same hunt was REFUTED and stays out of scope.
- [LiveLabTransitionOrderingStageAdversarialReview_2026-09-01.md](./LiveLabTransitionOrderingStageAdversarialReview_2026-09-01.md) — adversarial review of the GAP-1 design; verdict **READY-WITH-AMENDMENTS** (A1–A4, all grounded): A1 the rename of `role_switch_matrix` has a wider blast radius than the design listed (name-keyed in live_lab_stage_registry `logical`, failure-digest, evidence-verifier, lab-monitor coverage column, 3 MCP binaries, e2e wrappers, ledger) — must all move in one change; A2 verified `rustynetd` has ZERO `append_role_audit_entry` call sites (daemon-side bundle apply emits no audit entry; only CLI `execute_role_plan` does) so assertion (c) must be scoped to CLI-driven transitions; A3 the state-based ordering fallback admits stale-episode / wrong-instance false passes unless anchored to a transition-caused effect (fresh PID/boot-id or absent-in-pre-snapshot); A4 advertisement-gate sampling needs a trailing two-probe stability check + honest bounded-window semantics. Citations re-verified (trivial line drift only). Amendments fold into the GAP-1 design before impl.
- [LiveLabAclDenyAcrossFailoverStageDesign_2026-09-01.md](./LiveLabAclDenyAcrossFailoverStageDesign_2026-09-01.md) — **DESIGN ONLY, NO CODE — implementation pending adversarial review + lab availability.** GAP-3 from the coverage gap hunt (grounding verdict CONFIRMED, with two corrections to the hunt's phrasing): no stage re-probes ACL/policy **denied** pairs across a failover/failback path transition — `cross_network/scenario/failback_roaming.rs` asserts only reconnect-SLO + no-underlay-leak + signed-state-valid, and `traffic_test_matrix.rs`'s default-deny negative probe is a single non-routable TEST-NET address run once at steady state. Proposes extending `failback_roaming.rs` with denied-pair + allowed-control probes re-run after every transition (fail-closed attribution, VM-free offline decision-logic tests). Policy is evaluated at signed-bundle-apply time, so the seam is the scenario's own mid-run `reissue_for_roam` ACL re-derivation.
- [LiveLabAclDenyAcrossFailoverStageAdversarialReview_2026-09-01.md](./LiveLabAclDenyAcrossFailoverStageAdversarialReview_2026-09-01.md) — adversarial review of the GAP-3 design; verdict **READY-WITH-AMENDMENTS** (A1–A8): the 4th-node (`client-2` denied→exit) approach collides with `traffic_test_matrix`'s steady-state positive-probe loop (which fans over the same `ctx.assignments` and would fail the run expecting client-2→exit Reachable) — needs a per-pair expectation map; "routable-but-denied" mischaracterizes the model (an omitted allow-spec pair is *enforcement-absent*, not evaluator-denied — the policy gate is all-or-nothing and aborts+restricts) so the asserted property must be reframed as "re-derived generation excludes the absent pair"; the attribution rule self-defeats mid-reconvergence (both probes fail → INCONCLUSIVE→fail on the modal sample) — needs mid-transition vs settled-window semantics; and `ScenarioOutcome::passed` aggregates nothing so the new checks need explicit `if !checks.passed(...)` gates or they fake-pass.
- [LiveLabTraversalHintWireReplayStageDesign_2026-09-01.md](./LiveLabTraversalHintWireReplayStageDesign_2026-09-01.md) — **DESIGN ONLY, NO CODE — implementation pending adversarial review + lab availability.** GAP-4 from the coverage gap hunt (verdict CONFIRMED): no stage replays an old-but-validly-signed traversal hint onto the WIRE of an established running session — `cross_network/scenario/traversal_adversarial.rs` proves forged/stale/wrong-signer/nonce-replay only at the offline gate/bundle-load level plus an unsigned rogue-endpoint hijack, never a signed old-hint mid-session replay against the live apply path (`traversal.rs` `validate_signed_coordination_record` replay-window/staleness/nonce guards). Designs a sibling scenario driving the replay from the attacker vantage and asserting endpoint-immovable + attributable rejection + session-survives + generation-stable, distinguishing correct-rejection from silent-drop (fail-closed) with VM-free offline tests.
- [LiveLabTraversalHintWireReplayStageAdversarialReview_2026-09-01.md](./LiveLabTraversalHintWireReplayStageAdversarialReview_2026-09-01.md) — adversarial review of the GAP-4 design; verdict **READY-WITH-AMENDMENTS** (A1–A8): the decisive fact the design missed — traversal hints arrive as a signed bundle *file* loaded by `refresh_traversal_hint_state`, and an old-envelope replay is rejected at the bundle-load **watermark/anti-rollback** layer (`daemon.rs` `TraversalBootstrapError::ReplayDetected`), NOT at `validate_signed_coordination_record` (which only guards per-pair nonce use *within* an already-accepted envelope) — so the mechanism must be re-aimed and the "wire replay" framing corrected to bundle-path replay + forced refresh; attributable rejection has three sufficient failure modes (error evaporates on the next successful refresh, no refresh trigger fires post-injection, fetch-race overwrites) needing a forced refresh trigger + nonce/digest correlation id + a specified log channel; daemon-clock `now_unix` must be recorded for the staleness-vs-replay reason rule; teardown must restore the legitimate bundle + confirm recovery from the enforced-mode fail-closed state.
- [LiveLabSignedStateRollbackApplyLayerStageDesign_2026-09-01.md](./LiveLabSignedStateRollbackApplyLayerStageDesign_2026-09-01.md) — **DESIGN ONLY, NO CODE — implementation pending adversarial review + lab availability.** GAP-5 from the coverage gap hunt (verdict PARTIAL): the fault is named as an OFFLINE fixture (`live_chaos_signed_state_adversarial_test`'s `chaos_replay_old_membership`, `ChaosTargets::Offline`) but no stage drives a previous-epoch, previously-valid signed membership bundle at a RUNNING daemon through the normal distribution path; `security_audit` is an in-daemon self-audit. Enforcement is `rustynet-control` `membership.rs` `apply_signed_update` epoch-chain (`:1055-1059`) + `MembershipReplayCache` monotonic-epoch watermark (`:728-752`, `EpochRegression`). Designs `live_signed_state_rollback_replay` asserting rejection-at-the-enforcement-point + watermark-unchanged + membership-view-unchanged, with VM-free validator-level offline tests.
- [LiveLabSignedStateRollbackApplyLayerStageAdversarialReview_2026-09-01.md](./LiveLabSignedStateRollbackApplyLayerStageAdversarialReview_2026-09-01.md) — adversarial review of the GAP-5 design; verdict **READY-WITH-AMENDMENTS** (5): by check-order analysis of `apply_signed_update` (membership.rs:1032-1076) a genuine E-1 bundle fails at **`PrevStateRootMismatch` (:1052-1054) FIRST**, before the epoch-chain guard — and `EpochRegression` is constructed only in `verify_attested_snapshot` (:1513, the snapshot-pull surface), never on the apply path — so the design's expected-rejection set is wrong and would grade a CORRECT daemon as failed; fix the reason set to prev-state-root/epoch-chain/replay-detected on the `membership apply` IPC response. The "gossip/bundle channel, not an IPC poke" clause forbids the ONLY apply path that exists (local IPC `membership apply`, admin-role gated) — reframe to that entry (state-file drop is the thing to forbid). Capture at the mint point + SHA-256 pin + validity-window guard; decode/signature/expired/role rejections grade Blocked not Failed.
- [LiveLabCrossPlatformCustodySecretsAclStageDesign_2026-09-01.md](./LiveLabCrossPlatformCustodySecretsAclStageDesign_2026-09-01.md) — **DESIGN ONLY, NO CODE — implementation pending adversarial review + lab availability.** GAP-6 from the coverage gap hunt (verdict PARTIAL, with a grounding correction): per-platform *posture* validators for key-custody + runtime-ACLs already exist and run on macOS/Windows (`validate_macos_key_custody`/`validate_windows_key_custody` etc., real `rustynetd *-key-custody-check`/`*-runtime-acls-check` subcommands + fail-closed evaluators), so the hunt's "Linux-only validators" claim is only partly right. Genuinely uncovered: the *adversarial live* custody tests on mac/win (only `live_linux_key_custody_test` exists) and *all* secrets-in-logs validation off Linux (no `SecretsNotInLogs` validator kind at all). Designs `live_macos/windows_key_custody_test` adversarial binaries + a new `RoleValidatorKind::SecretsNotInLogs` posture layer + per-platform secrets live binaries; also surfaces a doc-drift finding (stale reported-skip comments) and the Windows `status`-subcommand §4.7 identity-challenge prerequisite. Aligns with the CrossPlatformRoleParity mandate.
- [LiveLabCrossPlatformCustodySecretsAclStageAdversarialReview_2026-09-01.md](./LiveLabCrossPlatformCustodySecretsAclStageAdversarialReview_2026-09-01.md) — adversarial review of the GAP-6 design; verdict **READY-WITH-AMENDMENTS** (A1 required): the design leans on a macOS/Windows "custody watcher drift flag" that **does not exist** — `macos_key_custody.rs`/`windows_key_custody.rs` are pure check-time collect+evaluate modules, and the real Linux adversarial semantics are **restart-based** (`live_linux_key_custody_test`: chmod → daemon restart → rejection = daemon-failed-to-start OR custody-check drift → restore → restart → recovery), so a macOS "checker sees drift" path would be a false pass (the daemon could keep running under a world-readable key). Reword to restart-based rejection per platform and fail the test if the daemon still runs under a downgraded key. Also A3: pin the new `secrets-not-in-logs-check` pattern set to the proven scanners (64-hex WG key, 32-hex ed25519, base64 EC-key headers) since no canonical forbidden-pattern table exists. Grounding (posture validators already run on mac/win; adversarial-live + secrets-in-logs are the gap) fully CONFIRMED.
- [GapStageDesignsAmendmentFoldChecklist_2026-09-01.md](./GapStageDesignsAmendmentFoldChecklist_2026-09-01.md) — **PLANNING ONLY, no code — the fold-first gate for the GAP stages.** Consolidates, per GAP (1, 3, 4, 5, 6), every adversarial-review amendment each design must fold BEFORE implementation: checklist item = amendment id + one-line action + the review's own file:line grounding. GAP-2 REFUTED (no design); GAP-7 folded into GAP-1's rename. Includes the cross-cutting failure themes (name-keyed blast radius, wrong-enforcement-layer assertions, `ScenarioOutcome::passed` gate wiring, Blocked-vs-Failed grading) and the shared hard precondition (green `live_lab_node_run_matrix.csv` row at a pinned commit, verdict from the stage's own report artifact).
- [MacosDnsFailclosedS1S4DesignAdversarialReview_2026-08-31.md](./MacosDnsFailclosedS1S4DesignAdversarialReview_2026-08-31.md) — adversarial review of the S1/S4 macOS DNS fail-closed fix design; verdicts S1 NEEDS-AMENDMENT (small; else-arm mutual-exclusion pin, RestrictionMode::None, phase10 `dns_protected()` accessor, post-Permanent tick-rate retry disclosure, cadence-on-attempt) and S4 NEEDS-AMENDMENT (restore-completeness hole + all-or-nothing observation). Amendments folded into the fix design; both S4 (7675deea) and S1 have since been implemented.
- [MacosExitMembershipRoleFixAdversarialReview_2026-08-31.md](./MacosExitMembershipRoleFixAdversarialReview_2026-08-31.md) — adversarial review + port-mapping-authority scope extension for the (owner-gated) macOS exit-cell membership fix; verdict READY-pending-owner-signoff with 8 implementation sign-off items; the anchor-cell `anchor.port_mapping_authoritative` grant folds into the same per-elected-role signed-capability-rewrite mechanism.
- [LiveLabCoverageGapHuntIndependent_2026-08-31.md](./LiveLabCoverageGapHuntIndependent_2026-08-31.md) — independent live-lab coverage-gap hunt (GAP-1..7) cross-referencing the stage catalog against SecurityMinimumBar/Requirements/§10.7; GAP-1 (transition-ordering) CONFIRMED and designed above, GAP-2 REFUTED, GAP-3 designed above; GAP-4/5/6 (live wire replay, apply-layer epoch rollback, mac/win-native custody/secrets/ACL) pending ground+design; recalibrates the 2026-08-31 audit whose new findings were over-elevated.
- [HP3RelayFrameForwardingOptInWiringSpec_2026-08-31.md](./HP3RelayFrameForwardingOptInWiringSpec_2026-08-31.md) — **WIRED 2026-09-01.** HP-3 relay frame-forwarding proof is now a first-class `--node` orchestrator stage (`relay_forwards_frame_validation`, `StageSuite::Disruptive`) reusing the existing `vm_lab` relay-forward helpers; OPT-IN ONLY via `--enable-relay-forwarding-validation` (injects nft blocks + restarts two peer daemons mid-run, QH-64, so it never appears in the default plan); run-matrix folds into the existing `linux_relay_forwards_frame` column
- [UdpHolePunchingImplementationBlueprint_2026-03-07.md](./UdpHolePunchingImplementationBlueprint_2026-03-07.md)
- [FableForkConsistentMembershipTransparency_2026-07-01.md](./FableForkConsistentMembershipTransparency_2026-07-01.md) — **SPECULATIVE R&D deep-dive — UNSCHEDULED, self-authored: not in-flight, not in the live-lab acceptance matrix.** Single-topic expert design study closing a currently-unaddressed trust-state attack class: **membership equivocation / fork attacks.** Today every check in `apply_signed_update` (`crates/rustynet-control/src/membership.rs:693`) is a *local linear* guard (`prev_state_root` link, epoch chain, `MembershipReplayCache` monotonic-epoch) — none detects a signer (stolen/coerced admin key, malicious anchor MITM on single-source bundle-pull, or concurrent-signer race) handing *two distinct-but-each-internally-valid* epoch N→N+1 histories to different nodes, silently forking the mesh. Proposes a three-layer defense on top of the existing Ed25519/SHA-256 primitives (no new crypto): (1) promote the already-hash-chained update log (`append_membership_log_entry:808`) to an **append-only Merkle history tree** (RFC 6962 / Crosby–Wallach) giving O(log n) inclusion + consistency proofs and a Signed Tree Head; (2) **gossip-based fork detection** (SUNDR fork-consistency, CT-gossip) — nodes exchange STHs on existing gossip, demand a consistency proof, and **fail closed** (freeze at last-consistent epoch + persist offline-verifiable fork evidence) on divergence; (3) opt-in **decentralized witness cosigning** (CoSi / Sigsum / Parakeet) to escalate detection→prevention, where witnesses hold **zero capability** (can only refuse, never grant — fits the advisory-never-grants rule). Includes grounded citations, explicit CLAUDE.md §3/§4/§8 constraint check, Pi-class cost analysis, honest scale-gating of Layer 3 (detection valuable at ≥2 nodes, prevention needs ≥5 independent witnesses), a 4-phase report-only-bake build path with an equivocation-injection live-lab acceptance stage, and self-assessed weakest points. Composes with (does not duplicate) FIS-0002's local-correctness formal spec by adding a `NoUndetectedFork` invariant
- [FableIntelligentSystemsProposals_2026-07-01.md](./FableIntelligentSystemsProposals_2026-07-01.md) — **SPECULATIVE R&D proposal set — UNSCHEDULED: not in-flight work, not in the live-lab acceptance matrix.** Eight adversarially-verified intelligent-systems design proposals (FIS-0001..0008, ranked): continuous path-quality EWMA scoring + make-before-break migration (boringtun already computes per-peer rtt/loss and rustynet discards both today), TLA+/stateright formal spec of the membership apply/anti-replay protocol (operationalizes the parked AnchorLiveLab §C6 line), Plumtree digest anti-entropy gossip, loss/RTT-variance congestion signal → relay preference + pacing, advisory role-placement scoring, SPRT/CUSUM live-lab flake-vs-regression classifier, relay DRR fairness + load-aware selection, nas/llm multi-instance service routing — each with grounded file:fn citations, an explicit CLAUDE.md §3/§4/§8 constraint check, a phased S/M/L build path, and a recorded independent citation-fact-check + adversarial-review verdict; plus 2 fully-designed-then-REJECTED entries (enrollment Bloom-cascade — RN-26 already prescribes the dominating exact fix; multi-anchor DNS quorum read — targets the SR-001-disabled fetch path) and an honest coverage note listing unverified side-findings routed to the audit pipeline

- [InventoryNetworkGroupPlan_2026-08-13.md](./InventoryNetworkGroupPlan_2026-08-13.md) — **C1 LANDED, C2/C3 OPEN.** `network_group` is parsed but never written: `--update-inventory-live-ips` maintains `ssh_target`/`last_known_ip`/`live_ips` only (`mod.rs:32672-32697`), so the label drifts every time a guest's address moves and three of twelve entries named a subnet that did not contain their own address. **Not cosmetic** — `ensure_inventory_entries_share_network` (`mod.rs:33747`) compares the label and returns `Err` before any stage runs, and the wrong labels inverted `--require-same-network` in **both** directions (two guests sharing an L2 rejected; a genuinely split mac+Linux pair accepted). C1 corrected the three labels, moving `last_known_network` with them, and gave `macos-utm-1` a **new** group — it is on the Apple-Virtualization vmnet behind host `bridge101` while the Linux guests are on the QEMU vmnet behind `bridge100`, and re-measured 2026-08-13 `debian-headless-2` cannot reach even the host's own `192.168.65.1` (traceroute dies at hop 1). Giving the mac the Linux label would have turned the audit green by asserting a same-L2 membership measurement disproves. Audit errors fell 8 → 1, the modal-plane heuristic flipped exactly as **QH-42** predicted (five false `off_fleet_subnet` cleared), and the one survivor is **true**, so a profile-enforced launch (`native.rs:1043-1071`) is still correctly blocked — C1 replaced eight false errors with one real one and must not be reported as unblocking it. **Revision 2 records four of the plan's own claims refuted by review**, three of them in the plan's own disfavour (the field has three consumers not one; the report *was* consumed, by the profile gate; the modal outcome *does* flip). C2 is re-scoped: derive from the **observed L2 segment** gated on `ProbeState::Ok`, never from the address — an address-only derivation would relabel a node onto whatever plane it wrongly landed on and report itself clean, and `macos-utm-1` carries four `live_ips` of which only one is live. Prior art it failed to cite on first draft: [QH41CrossBackendL2SplitPlan_2026-08-11.md](./QH41CrossBackendL2SplitPlan_2026-08-11.md) §C4 and QH-42.

- [VmnetSplitTrafficMatrixPlan_2026-08-13.md](./VmnetSplitTrafficMatrixPlan_2026-08-13.md) — **REVISION 2; the proposed remedy was WRONG and is withdrawn.** `traffic_test_matrix` is the sole failure in four consecutive runs: 100% loss both directions between `macos-utm-1` and the Debian nodes. Diagnosis (measured, survived review): host-level isolation between two vmnet networks — debian cannot reach even the host's OWN address on the other bridge (`192.168.65.1`) and traceroute dies at hop 1, while `net.inet.ip.forwarding` is already `1`. Permanent, because a macOS guest cannot run under QEMU on Apple Silicon. **Option C (relay between the planes) is DEAD in this lab** and the reason is worth knowing before anyone re-proposes it: a relay candidate on an RFC1918 address is rejected at traversal-bundle *parse* time (`rustynetd/daemon.rs:14699-14705`), called with `?` inside the per-candidate decode loop (`:14402`), so it aborts the ENTIRE bundle — minting one would break the Debian↔Debian paths that currently work. The lab cannot express a relay candidate anyway (`RELAY_SPEC` zero hits; issuer emits `relay_id: None`, pinned by a test). Settles the §5 Q1 question favourably though: `traffic_test_matrix` asserts **reachability only** (`traffic_test_matrix.rs:136`/`:148`), reading no path mode or endpoint — so C failed on feasibility, not semantics. Now proposes **bridging ONE guest** (`macos-utm-1` to 192.168.8.x), on the measurement that the Debians already dial OUT to that LAN successfully and WireGuard needs only one side to initiate (`traversal.rs:925 on_endpoint_roamed`); residual risk that `reconfigure_managed_peer` re-programs over the roamed endpoint is stated rather than hidden. Also corrects two of its own Revision-1 claims: "not a Rustynet defect" absolved three real product defects (no relay candidate expressible; daemon relay-session config unreachable in every deployment; `linux_relay_forwards_frame` unreachable under `--node` while its column reads `not_run` on 111/111 rows), and "the control plane is healthy" overstated — four macOS checks are genuine local posture, the fifth (`mesh_status`) is dispatched with no expected-peer/max-age and passes whenever the state file merely loads.

- [MacosDataplaneParityRoutesPlan_2026-08-13.md](./MacosDataplaneParityRoutesPlan_2026-08-13.md) — **CONCLUSION: macOS dataplane parity cannot be reached in software in this lab.** Three routes were proposed across two review cycles and each is independently blocked, two of them by deliberate fail-closed rules that must NOT be relaxed. **Relay** — a relay candidate on an RFC1918 address is rejected at traversal-bundle *parse* time (`daemon.rs:14699-14705`), called with `?` inside the decode loop (`:14402`) so it aborts the ENTIRE bundle; every address here is private, so minting one would break the Debian-to-Debian paths that currently work. The lab cannot express a relay candidate anyway (`RELAY_SPEC` zero hits; `relay_id: None`, pinned by a test). **Dual-plane scenario NIC** (`isolated_multivm_v1`) — blocked by TWO independent facts, and the first is fatal: Host Only is `NotSupported` on the Apple backend (`network_profile.rs:202-210`) while `derive_target_nics` derives the scenario adapter as Host Only (`network_prepare.rs:730`), so **live-proving Apple multi-NIC would NOT unblock it** — a correction to the predecessor plan's reading. Apple `Shared` is no workaround: it IS the isolated 192.168.65.0/24 vmnet causing the problem. **Bridging to en0** — forbidden by ADR-004 §5 ("never bridge automatically to `en0`"), enforced in code not just prose, and it re-creates the address churn the Bridged→Shared migration was written to kill. What remains is hardware (`ScenarioSubstrate::PhysicalInterface` is implemented and is the ADR's sanctioned bridge, but all six of this host's `en1`-`en6` ports are `inactive` with nothing plugged in), an explicit ADR-004 amendment, or re-scoping the macOS claim to blocked-on-hardware. Read it before re-proposing any of the three dead ends.

- [SharedUdpRelayTransportDeepDive_2026-08-17.md](./SharedUdpRelayTransportDeepDive_2026-08-17.md) — **design/review only; no product change.** Detailed authoritative-UDP and relay-lifecycle investigation: worker-loss quarantine, baseline-vs-overlay endpoint state, initial/apply/recovery route-policy transaction, strict control mux, V2 compatibility gates, session-evidence claim boundaries, token admission lifecycle, and acceptance tests. All recommendations are proposals with explicit Explore/Gate labels; five adversarial reviews incorporated.
- [SignedTraversalAuthorityLifecycleDeepDive_2026-08-18.md](./SignedTraversalAuthorityLifecycleDeepDive_2026-08-18.md) — **design/review only; no product change.** Adjacent signed traversal-authority analysis: V1 expiry/cutover, membership removal and key-rotation withdrawal, duplicate-key identity, typed candidate provenance, restart/cache bootstrap, gossip liveness, clock/key activation, and deterministic implementation gates. Every recommendation is explicitly Proposal/Explore/Gate, not implementation authority.
- [LiveLabCoverageGapDiscovery_2026-08-19.md](./LiveLabCoverageGapDiscovery_2026-08-19.md) — **discovery only; no lab activity or product change.** Active `--node` live-lab coverage analysis: measured unexecuted suites, narrow-oracle risks, cross-platform/cross-network gaps, shared-UDP recovery proof boundary, and a resource-safe, adversarial-review-first test roadmap.
- [LiveLabTestCoverageImplementationDesign_2026-08-19.md](./LiveLabTestCoverageImplementationDesign_2026-08-19.md) — **design only; no test or product change.** Strict coverage-gap implementation plan: proof-certificate pass semantics, current-run generation binding, lossless non-pass outcomes, resolved-plan verification, candidate-before-promotion evidence verdicts, strict monitor non-green behavior, focused/adjudication plans, typed reversible faults, and independent scenario contracts. Includes source-grounded adversarial L0 review. Every code slice requires adversarial review before implementation.
- [RepoStateAssessmentAndNextSteps_2026-08-19.md](./RepoStateAssessmentAndNextSteps_2026-08-19.md) — **assessment/planning artifact only; not an owning ledger, changes nothing.** Verified repo-state snapshot at commit `09daefdf`: git/CI/lab status, a docs-say-vs-reality divergence list, a grouped open-work inventory (evidence integrity, cross-platform parity, the QH ledger, the in-flight design programs, dataplane, service-hosting, security), a phased recommended order of operations, and the blocking operator decisions. Independently source-verified in two adversarial passes (§8/§8a) — findings folded inline.

## Additional Active Plans, Audits And Test Ledgers

Previously-unindexed active files, now listed for discoverability:

- [HotPathCloneAudit_2026-09-01.md](./HotPathCloneAudit_2026-09-01.md) — docs-only hot-path `.clone()` audit (per-packet/per-frame/per-tick paths only; the un-sampled ground `RepoCodeWorkHunt_2026-09-01.md` named): relay forward path and daemon tick cleared as tight; two new findings — P2 inbound receiver-index demux doing an O(n) scan + `NodeId(String)` heap clone per datagram (`engine.rs` `find_node_id_by_receiver_index`, distinct from backlog P4's endpoint index) and a vendored report-only P2 (boringtun `queue_packet` `to_vec()` per outbound packet during the pre-session window) — plus one P3 (fair-drain `FlowKey` clone under backpressure); every other inspected site is cleared-with-evidence or an already-tracked backlog item
- [LiveLabStagePassLikelihood_Linux_2026-09-01.md](./LiveLabStagePassLikelihood_Linux_2026-09-01.md) — docs-only pass-likelihood analysis of the Linux `--node` live-lab stages with no genuine pass evidence: `linux_stage_two_hop` is actually green post-QH-07-alias-removal, written by the only two-hop `StageId` `live_two_hop_validation` (26 post-removal run passes, 130 Linux node-row passes; the "0 pass" figure is a stale 2026-07-27 count; residue is the same stage id never dispatched on macOS/Windows), `mixed_topology` is hard-gated on all-3-OS by design, chaos suite is an opt-in selector never once selected, `blind_exit_dataplane_check` has exactly one unresolved fail; the 8 "dead legacy bash columns" suspicion is refuted — 7 are duplicate-by-supersession columns (live proof runs green one column over in the `linux_stage_*_check` family), 1 (`linux_relay_forwards_frame`) belongs to a real opt-in Disruptive stage; ranked quickest-to-green list inside
- [WindowsNativeCodeWorkHunt_2026-09-01.md](./WindowsNativeCodeWorkHunt_2026-09-01.md) — docs-only sweep of `crates/rustynet-windows-native/` (the ground `RepoCodeWorkHunt_2026-09-01.md` left un-swept; six categories, full-file read, every grep hit opened in context): clean at P1/P2; three P3s (lib doc asserting a Windows thumbprint implementation that exists on no platform, dead `inspect_handle_sddl` behind a blanket `allow(dead_code)`, `owned_pwstr_to_string` leaking the LocalFree'd buffer on the UTF-16 error path) plus a cleared-with-evidence list covering the WFP shape validator, forward-traffic arbitration, named-pipe UAC filtering, and DPAPI error handling
- [RepoCodeWorkHunt_2026-09-01.md](./RepoCodeWorkHunt_2026-09-01.md) — docs-only production-code hunt (six categories, per-hit open-and-read verification): essentially clean; two P3 findings (llm-gateway scope=None-unrestricted design confirmation, whole-file `allow(dead_code)` on two macOS backend files) plus a cleared-with-evidence list so future hunts don't re-read the same ground
- [NetworkFlapHandshakeCapture_2026-08-27.md](./NetworkFlapHandshakeCapture_2026-08-27.md) — QH-51 packet/state capture of the network-flap "handshake never returns" failure: NON-REPRODUCTION on current main (4/4 cycles recover in 8s, both Linux backends), mechanism packet-proven; closes QH-51
- [AdversarialSecurityReview_2026-07-29.md](./AdversarialSecurityReview_2026-07-29.md) — adversarial security review
- [AdversarialSecurityRemediation_2026-07-29.md](./AdversarialSecurityRemediation_2026-07-29.md) — remediation for the review above
- BashRetirementPlan_2026-07-24.md — moved to `../done/` (executed as Phase E of the retirement program).
- [LiveT5NegativeControlProofPlan_2026-07-24.md](./LiveT5NegativeControlProofPlan_2026-07-24.md) — T5 negative-control live proof
- [FleetEvidenceCollectionPlan_2026-07-28.md](./FleetEvidenceCollectionPlan_2026-07-28.md) — fleet evidence collection plan
- [RepoReview_2026-07-20.md](./RepoReview_2026-07-20.md) — repository review
- [SecurityAuditCatalogStalePathsTodo_2026-07-28.md](./SecurityAuditCatalogStalePathsTodo_2026-07-28.md) — stale-path cleanup TODO for the audit catalog
- [TestInventory_2026-07-30.md](./TestInventory_2026-07-30.md) — test inventory
- [TestQualityReview_2026-07-30.md](./TestQualityReview_2026-07-30.md) — test-quality review
- [QH22LedgerChronologyPlan_2026-08-10.md](./QH22LedgerChronologyPlan_2026-08-10.md) — QH-22 ledger-chronology plan
- [QH40ShutdownRollbackPlan_2026-08-11.md](./QH40ShutdownRollbackPlan_2026-08-11.md) — QH-40 shutdown/rollback plan (REFUTED; superseded by the design below)
- [MacOsHelperShutdownOrderingDesign_2026-08-27.md](./MacOsHelperShutdownOrderingDesign_2026-08-27.md) — QH-40/D-7 macOS privileged-helper shutdown ordering + fail-closed rollback reporting design
- [MacosPathConstantAudit_2026-08-28.md](./MacosPathConstantAudit_2026-08-28.md) — **audit-only sibling sweep of the QH-40 `DEFAULT_STATE_PATH` fix**: every remaining `#[cfg(not(windows))]` path constant in `daemon.rs` (21 constants), checked per-constant against what the macOS installer/plist actually configures and who resolves the default on macOS. **Verdict: no QH-40-style fail-open** — every wrong-on-macOS default is either deploy-time-overridden, fails closed on the missing file, cosmetic/test, or a doctor check that silently omits itself. Closest latent risk: the relay-fleet pair (no macOS plist flag; `Ok(None)` silent dormancy) — re-audit when the macOS relay cell goes live. Follow-up fix shapes recorded; nothing applied. Also flags the `rustynet-cli`-local socket constants diverging from daemon.rs's macOS arms, and `check_macos_doctor`'s WG custody check being dead code on macOS.
- [MacCellsHarvest_2026-08-28.md](./MacCellsHarvest_2026-08-28.md) — **MAC-CELLS: the macOS `anchor` and `exit` roles elected on the `--node` engine for the first time** (Parity Refresh §3 Track M sub-stream (b), harvest tasks 7+8). Neither cell earns a green, and both move off ⬛ *never elected* to 🔴 with a located blocker. **anchor** (`livelab-1787911937-77ff1933885f`, clean): capability advertisement passes live on macOS, but the bundle-pull runtime substages are gated on `NodeRole::Anchor.is_supported_for_platform` (`role.rs:68-70`, Linux-only) — a gate whose own comment promises promotion "once a green run is archived" while `anchor_validation.rs:181,236-247` makes that green unobtainable, so the promotion route must come from the `--anchor-platform macos` stage set instead. **exit** (`livelab-1787913512-a5e93c8dd781`, **dirty** — diagnostic only): fails at `membership_init` because the macOS adapter reads the membership owner pubkey from `/usr/local/var/rustynet/membership/` with a bare `cat` (`macos_membership.rs:29-34`, `macos_install.rs:27-28`) where the Linux twin reads `/etc/rustynet/membership.owner.key.pub` with `sudo -n` — on the guest the key exists only at the latter path, and the former directory is `0700 rustynetd`. Also records that `--skip-linux-live-suite` drops the whole post-baseline suite (61 → 19 stages, `plan.rs:545-549`), excluding `live_anchor` and all three `exit_*_validation` stages, so **four of the five brief-named harvest targets cannot be reached on the fast-path form every mac/win target key uses**. First macOS exercise of three tonight-merged changes: the QH-39 `scutil` resolver observation reds honestly and exposes a real macOS DNS fail-closed enforcement gap (`/etc/resolv.conf` claims loopback while `scutil --dns` reports 1.1.1.1/8.8.8.8); the QH-40 shutdown-residue marker fires correctly and durably records the full teardown-failure signature, but `shutdown-residue-check` defaults to the Linux state path (`daemon.rs:169-170`) and so reports **clean on macOS while a marker exists** — a fail-closed check failing open. The helper/client timeout pair was not exercised. No code changed.
- [MacosDnsFailclosedEnforcementGap_2026-08-28.md](./MacosDnsFailclosedEnforcementGap_2026-08-28.md) — **the macOS DNS fail-closed ENFORCEMENT gap that the QH-39 `scutil --dns` observation exposed live (MAC-CELLS run 3) — design-only, owner-gated.** Evidence: `/etc/resolv.conf` claims loopback-only (RustyNet's own apply-path write) while `scutil --dns` primary resolver reports 1.1.1.1/8.8.8.8, so macOS resolution was never locked to loopback. Root cause: `MacosCommandSystem::apply_dns_protection` (`crates/rustynetd/src/phase10.rs:4081-4130`) writes the cosmetic `resolv.conf` shim (mDNSResponder ignores it for the default lookup path) plus the `*.rustynet`-scoped `/etc/resolver/rustynet` (per-domain only, cannot set the default resolver) and asserts pf egress `:53` only — no SystemConfiguration arm exists, unlike Linux where resolv.conf IS the resolver source. Specified: M1 `networksetup -setdnsservers` per service (documented, persistent, strand-on-crash risk), M2 fixed-path `scutil -f` State:/ override (volatile, undocumented, needs reconcile re-assertion), M3 bind-`:53` rejected (unprivileged daemon, no redirect). Implementation gated on five owner decisions (M1-vs-M2, service scope, new privileged argv surface + name validation, loopback-posture/RustyDNS-tandem interaction, teardown ordering per §10.7). Verification plan: the shipped QH-39 check is the verifier — enforced state must exit 0, revert must red. Until it lands, DnsFailclosed is expected to red on every protected macOS node. No code changed.
- [QH41NetworkEvidenceBlindPlan_2026-08-11.md](./QH41NetworkEvidenceBlindPlan_2026-08-11.md) — QH-41 network-evidence blind-spot plan
- [CrossOsColumnSemanticsPlan_2026-08-12.md](./CrossOsColumnSemanticsPlan_2026-08-12.md) — cross-OS column-semantics plan
- [ManagedDnsMultiClientPlan_2026-08-13.md](./ManagedDnsMultiClientPlan_2026-08-13.md) — managed-DNS multi-client plan
- [MeshStatusVacuousPassPlan_2026-08-13.md](./MeshStatusVacuousPassPlan_2026-08-13.md) — mesh-status vacuous-pass plan
- [PreflightClockSkewPlan_2026-08-13.md](./PreflightClockSkewPlan_2026-08-13.md) — preflight clock-skew plan
- [SkipReasonPlan_2026-08-13.md](./SkipReasonPlan_2026-08-13.md) — skip-reason evidence plan
- [NeverDispatchedLinuxStagesTriage_2026-08-27.md](./NeverDispatchedLinuxStagesTriage_2026-08-27.md) — **triage of the three Linux stage-vocabulary entries that have never dispatched on the `--node` engine** (`validate_linux_relay_forwards_frame`, `linux_stage_chaos`, `linux_membership_genesis`), verified against 178 run rows / 28,893 per-stage rows. Two share one root cause — they are **bash-dialect registry rows** whose `special:` column mapping hangs off stage names the `--node` plan (built from `StageId::ALL`, not the registry) never emits; they run only via the standalone `ops vm-lab-validate-linux-security` subcommand, which appends no ledger row. Chaos is different and worse: opt-in `--enable-chaos-suite` was never passed in any run, **and** all nine chaos stages depend on `live_mixed_topology_validation` (tri-OS-only, 0-for-178), so enabling the flag today yields nine cascade-skips — enabled ≠ dispatched. Records what each stage uniquely proves that nothing else does (HP-3 forwarded-frame + ciphertext-only; the `0600 rustynetd:rustynetd` custody of the three canonical membership files), an explicit do-not-over-claim on `chaos_daemon_fault` vs QH-54, four owner decisions, and one stale-ledger correction: `BashRetirementDispositions_2026-08-22.md` **B1.6 is a false equivalence** — `linux_stage_membership` proves minting/distribution, not file custody, so an owner-approved "not a real gap" rests on a mis-statement. No code changed.
- [LiveValidation_2026-08-28.md](./LiveValidation_2026-08-28.md) — **attempted first live proof of `main` @ `e13132cb` (tonight's ten merges: CN-1/CN-2/CN-4 substrate + topology endpoint seam, QH-18 per-guest flock, QH-04 reconcile atomicity, QH-40 residue marker, D-3 enrollment gate) — NO RUN EXECUTED, so all ten remain unproven on hardware.** The suite was refused before any stage dispatched by the fail-closed stage-triage remedy gate (`policy_reject (78)`) on the undispositioned stub `livelab-1787849060-86f633c907cf::bootstrap_hosts`; the gate was not overridden and no ledger row was appended (correctly — there is no run to attribute). Establishes by commit ordering that the blocking failure (`egress interface does not exist: rustynet-vx0`, `ops_install_systemd.rs:2051` vs `substrate.rs:34`) is **pre-existing, not a regression from tonight's merges** — the CN-4 vxlan work was authored ~10 h after the run that failed. Also records two orchestrator defects found in passing: the MCP `start_live_lab_run` wrapper injects `--windows-vm`/`--macos-vm` into a Linux-only run (7 guest locks for 5 nodes), dragging in the Apple-Virt macOS guest whose ARP fallback then blocks forever in unbounded `fs::read_to_string`/`opendir` (`vm_lab/mod.rs:30169-30170`) — and because the QH-18 flock is taken **before** readiness, that wedge holds the whole UTM fleet locked with no timeout. Plus full recovery of all five UTM Linux guests (3 powered off, 2 stale-killswitch). No code changed. **§8 records the follow-up that closed both orchestrator defects on `work/launch-path-fixes`. §9 records ATTEMPT 2, where the suite actually RAN on code byte-identical to `main` @ `22e707b2`:** run `livelab-1787903378-068b29ebc54b`, ledger row 184, `passed=6 failed=1 skipped=54`, `overall_result=fail` at `bootstrap_hosts`. The launch gate was dispositioned against the QH-51 root cause and cleared — but the failure recurred on `debian-headless-2`, the one guest that remedy never touched: its `/etc/default/rustynetd` (mtime 2026-08-27T14:56:54Z, the same 14:57Z cross-network run that seeded the lenovo residue) still pins `RUSTYNET_EGRESS_INTERFACE=rustynet-vx0` while the link is absent. The other four guests were rewritten by this run with the correct `enp0s1`, proving the code derives the right interface absent a stale pin — it is obeying a file, and `cleanup_hosts` does not remove that file. **Attribution is closed: none of the seven merges since `e13132cb` touches `ops_install_systemd.rs` or `substrate.rs`, and the precedence chain that prefers the stale env last changed in `8578a25b` (2026-05-18).** Also: the §8 D1 fix is NOT live — `bin/rustynet-mcp-lab-state` is 15 days stale and still injected `--windows-vm`/`--macos-vm`, so the run went via the shell; and the §12.3.1 MCP LAN false-negative has returned (all five guests read UNREACHABLE via MCP, OPEN via `nc`). The new stub `livelab-1787903378-068b29ebc54b::bootstrap_hosts` is deliberately left `patch: null` and will refuse the next launch until the guest is cleared — clearing it was refused by command-permission policy in that session. **§10 records ATTEMPT 3, after the manager cleared that guest: `bootstrap_hosts` went `fail` → `pass` with no code change** (run `livelab-1787904828-2307d6731123`, ledger row 185, `passed=7 failed=0 skipped=54`, overall `partial`, no failed stage) — identical topology, profile and code, one deleted file the only delta, which closes the attribution conclusively. The run was then **stopped deliberately at 510 MiB free host disk** via `SIGTERM`, so teardown and cleanup still ran and the locks and ledger row were preserved. The consumer was not the run (520 KiB footprint) but **25 sibling worktrees carrying 272 GB of separate `target/` build caches** (`mgr-cn3-scenario-port` alone 25 G), growing 14 GB during the run — **this is now the binding constraint on live validation, ahead of any code defect**, and `prune_jobs` cannot touch it. Bootstrap through `install-systemd` on all five guests is proven on hardware. **§11 records ATTEMPT 4, the first end-to-end completion: run `livelab-1787906534-877a0226693c` on `main` @ `877a0226`, ledger row 186, `passed=44 failed=0 skipped=17`, overall `partial` with no failed stage.** A per-stage diff against the best comparable baseline (`livelab-1787825655`, 42/0/17) shows **zero flips and a purely additive delta** — the two new stages are `cross_network_substrate_setup` and `cross_network_substrate_teardown`, both green — so there is no regression anywhere against prior evidence; 44 ties the all-time ledger high rather than setting one. Six stages carrying repeated undispositioned failure stubs are now green, including `traffic_test_matrix` (6 stubs), `live_managed_dns_validation` (4), `live_network_flap_validation` (6, QH-51's subject), `gossip_convergence_validation` and `bootstrap_hosts`; no new stub was created. **Live evidence now exists for the QH-52 firewalld unbind path and the helper IPC timeout pair, but NOT for the CN-3 scenario port** — all eight ported validators skip for want of a vxlan substrate, so CN-3's completion remains unproven. Establishes why the ledger has never recorded an overall `pass` in 186 rows: all 17 skips are declared role/platform gaps (no blind_exit, no second client, no `entry` hop, Linux-only, no vxlan substrate), so `partial` is the ceiling this topology can reach — judged on what it can run, the suite is 44 for 44.

## Active Lab Assets

- [UTMVirtualMachineInventory_2026-03-31.md](./UTMVirtualMachineInventory_2026-03-31.md) — VM list, SSH key fingerprints, **probe-and-recover runbook** (`scripts/vm_lab/probe_and_recover_local_utm.sh`) for unsticking stale-killswitch lab guests
- [../live_lab_node_run_matrix.csv](../live_lab_node_run_matrix.csv) — **the Rust `--node` engine's live evidence ledger** (current runs append here); [../LiveLabRunMatrix.md](../LiveLabRunMatrix.md) and [../live_lab_node_stage_results.csv](../live_lab_node_stage_results.csv) provide the run-matrix README and normalized exact distro/version node-stage proof. `../live_lab_run_matrix.csv` is the **frozen legacy bash-orchestrator archive** (history only) — update after every run used as evidence
- [rustynet_blind_exit_pcb_report_2026-05-10.docx](./rustynet_blind_exit_pcb_report_2026-05-10.docx) — external/AI-generated analysis, not native to this repo; cites a hardware source file (`rustyjack-h616-pcb-architecture-v0.md`) that doesn't exist anywhere in this repository, and its network design assumes NAT is present, which contradicts the actual no-NAT `blind_exit` dataplane. **Superseded by [BlindExitPcbHardwarePlan_2026-07-22.md](./BlindExitPcbHardwarePlan_2026-07-22.md)** — treat this docx as historical background only, not a spec.
- `vm_lab_inventory.json` — VM topology + role assignments
- `lab_image_catalog.json` — the tracked base-image catalog (schema v1): name → url + sha256 + `digest_provenance` + os/arch + `kind` (qcow2 vs ISO), read by `ops vm-lab-image-catalog` and the arch gate. NOT a dated evidence artifact — this one is edited in place as the fleet's image set changes, and every rule in it is enforced by `crates/rustynet-cli/src/vm_lab/image_catalog.rs` (a unit test parses the tracked file, so an invalid edit fails the suite rather than a live command)
- `vm_lab_readiness_check_2026-04-28.json` — dated VM lab readiness summary (schema_version=1)
- `netns_transit_migration_evidence_2026-07-10.json` — Tier 1 live evidence for the netns transit migration to `198.18.0.0/15` (build/reachability/isolation + NAT mapping 3/3 + filtering 9/9 + clean teardown on debian-headless-2)
- `vm_network_evidence_postmigration_2026-07-10.json` — post-migration read-only network audit after the Slice B live application moved debian-headless-4/Windows/Fedora/ubuntu from Bridged to Shared (`mgmt_shared_smoke_v1`, txn `txn-1783721025-63223`); all bridged findings cleared; residual findings are the host Tailscale/mesh route collision + stale network_group labels
- `windows_utm_1_runtime_acls_2026-04-28.json` — windows-utm-1 runtime-ACL collector evidence
- `windows_utm_1_service_hardening_2026-04-28.json` — windows-utm-1 service-hardening collector evidence
- `windows_utm_1_validate_2026-04-28.json` — windows-utm-1 validate-bundle output

Rule: these JSON evidence files are dated artifacts from specific lab runs.
Add a new dated file when a new run produces fresh evidence; do not edit
existing ones in place.

## Rules

- Keep status and evidence in the owning ledger or plan.
- Do not add standalone prompt documents here. (Existing prompt documents are listed in the section above for discoverability only.)
- If an active file becomes historical, move it or reclassify it honestly and update the indexes in the same change.
