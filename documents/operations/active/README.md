# Active Operations Work

This folder contains the current implementation ledgers, active plans, and active lab-reference assets.

## Primary Execution Ledgers

Start with these when you need current status, remaining work, or the public implementation ledger:

- [CrossPlatformRoleParityRefresh_2026-07-23.md](./CrossPlatformRoleParityRefresh_2026-07-23.md) — **DRAFT (grounded; adversarial review pending)**: re-scopes the release-blocking mac/win role-parity mandate to the **engine of record** (the `--node` orchestrator, which the acceptance spec's G2 gate reads), superseding the *status* half of the ParityPlan/Roadmap for the G2 era. **Headline reframe:** every ✅ in the old §3 matrix is a **legacy-bash proof** (10/10 cited run-ids are in the frozen bash archive, zero in the `--node` ledger) — so under G2 (release reads the `--node` ledger, bash being deleted) mac/win parity is **~0% proven on the engine of record**: the `--node` ledger has 88 rows and zero overall pass, cross-OS (`live_mixed_topology_validation`) is 0-for-88, Windows `--node` bootstrap has never succeeded, and macOS is only *stage*-green (admin/relay) with `two_hop` failing every run. Gives the `--node`-native status matrix, the critical path (CP-1 `two_hop` client↔client WG-handshake bug · CP-2 `network_flap`/traversal · CP-3 Windows-exit WinNAT hardware · CP-4 Windows `--node` bootstrap triage), the dependency-ordered program, the env-vs-code blocker split, the intended-OS-divergences (drift rule — not parity holes), the stale-doc corrections (DoD pointers → the `--node` ledger), and the G2 mapping. Key insight: the mac/win role **stages already exist**; the gap is running them green behind the blockers, not authoring validators.
- [NodeEngineAcceptanceSpec_2026-07-23.md](./NodeEngineAcceptanceSpec_2026-07-23.md) — **DRAFT (draft 2, adversarially reviewed), awaiting owner sign-off**: the Track B functional gate for the Rust `--node` orchestrator, written as a **`--node`-native acceptance bar** rather than a bash↔Rust parity gate (native-spec foundation + a **one-time** bash differential sweep as a migration safety net, since bash is being deleted and a parity gate expires with its reference + enshrines its quirks). An adversarial pass found and closed a structural hole: the first draft measured only **outcomes** (stages go green) and never **adjudication** (can the engine correctly produce a RED?), with every evidence property self-attested by the engine under test — a rubber-stamp engine would have passed. Draft 2 adds the missing dimension and splits the fused gates into **three**: **G1 engine-adjudication trust** (T0+T1 green **and** a T5 negative-control suite that must go RED-for-the-right-reason **and** an independent verifier that recomputes evidence from raw artifacts, on today's cells → gates the W5.6 default flip, **independent of parity**), **G2 parity attainment** (all role×OS cells green → gates release), **G3 bash-retirement** (one-time diff sweep, enumeration as a flip precondition → gates W5.7 deletion). Also: machine-readable total `StageId→tier` map + gate (the prose list had already drifted); fenced dispositions (named ledger, per-item owner sign-off, expiry, security not dispositionable); flake-sized N-of-N stability at a clean flip-candidate commit (replacing the arithmetically-refuted "two consecutive"); GREEN = exactly `Pass`; identity asserted not recorded; digest-bound evidence; `network_flap` correctly-adjudicated-RED until the traversal fix lands then green. §10 sign-off + §11 changelog inside.
- [G3EnumerationDiff_2026-07-23.md](./G3EnumerationDiff_2026-07-23.md) — **COMPLETE + adversarially reviewed**: the `NodeEngineAcceptanceSpec` §8 **enumeration-half flip precondition** (diff-only, no lab) — the sole detector of *silently-dropped* stage coverage in the bash→`--node` migration. Result: **zero silently-dropped stages** (as scoped) — every stage the frozen bash archive ever proved GREEN maps to a **wired** `--node` `StageId` (all of `StageId::ALL` is plan-registered with a no-dead-stage gate). Two diffs: bash-archive CSV columns (549 rows) + the authoritative bash-script `run_stage` names (catches CSV-invisible stages). Differences are all benign: dialect naming, deliberate **consolidation** (bash's 8 Linux security stages → one `security_audit_validation`, per `LINUX_SECURITY_AUDITS`), `cross_os_*` bash-dialect aggregates (spec §3-T3), or **G2 mac/win attainment** (a release gate, not the flip — G1≠G2). Dispositions two CSV-invisible bash meta-stages (`fresh_install_os_matrix_report` report-aggregation, `local_full_gate_suite` CI-gate) as owner-sign-off items for W5.7, and flags one honest DEPTH caveat (`role_switch_matrix` may be shallower on `--node` — a G2 item). The adversarial review confirmed the claim holds and corrected a false "records GREEN" statement (the 8 audits are coverage-present but attainment-pending on `--node`, 0 ledger passes). Satisfies the W5.6 flip precondition; the full outcome-level G3 sweep still gates W5.7 bash deletion separately.
- [TraversalSelfSustenancePlan_2026-07-23.md](./TraversalSelfSustenancePlan_2026-07-23.md) — **APPROVED design, implementation not started**: the execution plan to close the sole remaining Linux `--node` red (`live_network_flap_validation`), which is the canary for a **real production gap** — on default config (`auto_tunnel_enforce: true` → `EnforcedV1`) a mesh fails closed ~120 s after setup because nothing re-issues the signed traversal-authority state; the hardened daemon is a pure local-custody consumer and the control-plane minter distributes once at bootstrap. Chooses **hardened Design A** (each node self-signs its own candidates via the already-built-but-dormant D2.5 gossip; membership authorizes *who*, self-signed candidate provides *how to reach*) over Design B (anchor re-mint — rejected because it would put the mesh root signing secret on a running daemon). Records the resolved grounding (membership `public_key` = the Ed25519 gossip identity; gossip per-source sequence = anti-replay parity; gossip drive already wired in prod, only `attach` + a **new daemon signing identity** + register-from-membership missing), the mandatory hardening (epoch-bind CandidateSets for expiry-bounded revocation, ACL-scoped gossip application, per-origin rate-limit/plausibility), and the file:line-level increments I1–I7 (daemon signing-key custody → gossip activation → epoch-bind wire bump → fail-closed enforcement rewire → ACL-scope → rate-limit → integration test → live-verify). A multi-session, security-critical build on the most sensitive crate; no TTL widening, fail-closed preserved.
- [LinuxMtuPrivilegedHelperAllowlistGap_2026-07-21.md](./LinuxMtuPrivilegedHelperAllowlistGap_2026-07-21.md) — **live regression, diagnosed, fix not yet implemented**: `enforce_baseline_runtime` fails on every Linux lab guest (Fedora/Ubuntu/Rocky confirmed) because FIS-0027's new `ip link set mtu 1420 dev <iface>` bring-up step (`fef40bb`, unrelated to and predating the session-wrap-up work below) was never added to the privileged-helper's `ip` argv allowlist, so it's rejected every time, driving the daemon into permanent lockdown and cascading into the managed-DNS systemd unit failing. Root cause confirmed live via `journalctl` on a real guest, not guessed. Lays out the exact fix (an allowlist entry matching the literal MTU constant, not any numeric value — with the security reasoning for why a type-range or a signature would be the wrong, weaker choice here) and the exact test needed to prove the scoping is tight.
- [SecurityAuditAndMainConsolidation_2026-07-21.md](./SecurityAuditAndMainConsolidation_2026-07-21.md) — **session wrap-up**: the A1–A5 security audit (A1/A3/A5 fixed, A2 doc-corrected, A4 substantial — see the two docs below), the macOS full-tunnel exit dataplane fix, and a full "get everything on main" branch consolidation (every branch/worktree in the repo enumerated and disposed of — merged, confirmed-already-on-main, or deleted with operator approval after confirming no recoverable history). §7 lists what was deliberately left open: Part B of the audit, the adjacent bearer-token gap, live-lab proof, and remaining branch housekeeping.
- [RustynetUnifiedTodoLedger_2026-07-10.md](./RustynetUnifiedTodoLedger_2026-07-10.md) — **dated repository-wide TODO roll-up** covering security/release blockers, the full live-lab verification ladder, completion of the Rust `--node` engine, canonical dual-plane VM networking and MCP behavior, desktop role parity, cross-network dataplane, Android/iOS client programs, NAS/LLM evidence, testing/fuzzing, serialization, CI/supply chain, performance, operations, platform expansion, external decisions, and one shared Definition of Done. Focused ledgers remain authoritative; update both the owner and this roll-up as work lands.
- [ParallelAgentWorkPlan_2026-07-01.md](./ParallelAgentWorkPlan_2026-07-01.md) — **partitions the current backlog into 3 independent big jobs for concurrent agents** (HP-3 real relay packet-forwarding proof; cross-OS role transitions + Windows anchor live bundle-serving; a Tier-1 security-hardening batch — replay-persistence, crash-midapply fail-closed, relay-flood DoS, UPnP SSRF, privileged-helper socket fuzz), plus an optional 4th (macOS/Windows security-validator ports). Documents the known in-flight work to not duplicate (`ENR-1`/`TOCTOU-1` enrollment-replay audit, uncommitted as of 2026-07-01), the shared-wiring-file collision risk + append-only discipline, and the git-worktree-per-job mechanics for landing back onto `claude/cross-platform-parity-hardening` sequentially. Ready-to-paste prompts in `ParallelAgentPrompts_2026-07-01/`
- [NonSecurityParallelHandoff_2026-07-13.md](./NonSecurityParallelHandoff_2026-07-13.md) — **stand-alone multi-model dispatch prompt for the guardrail-safe NON-security backlog** (durability, refactor, tests, tooling, diagnostics, docs), curated from the unified TODO ledger + RNQ audit. Explicitly excludes the crypto/trust-state/killswitch/exit-NAT/DNS-failclosed/privileged-helper/ACL surfaces (§4 → they stay on the Opus + §13.2 path). Tiers work by model: `fable` (RNQ-05 durability, RNQ-07 cancellable timeouts, RNQ-17 feature-gate the lab robot out of the shipped binary, advisor MCDA), `sonnet` (RNQ-09 SIGTERM test, sysinfo parser coverage, lab-monitor input-robustness, read-only diagnostics), `haiku` (doc/index/CODE_MAP drift), plus a GRAY dataplane-perf tier that a `fable` agent may build+bench but may NOT land without Opus §13.2 sign-off. Each package is a worktree-isolated stand-alone agent brief with a suggested dispatch order.
- [LabMonitorTUIDesign_2026-06-29.md](./LabMonitorTUIDesign_2026-06-29.md) — **design spec for `rustynet-lab-monitor`**: a pixelated terminal TUI that is the live GUI for the parity campaign — stage block grid, VM status, role×OS parity matrix, start/stop control, ETA from timing CSVs. New crate `crates/rustynet-lab-monitor/`; tech: ratatui 0.28 + crossterm + tokio; reads state files directly (no MCP dependency at runtime). Build prompt at `scripts/loop/lab_monitor_build_prompt.md`
- [LiveLabMonitorTUIAccuracyImprovements_2026-07-10.md](./LiveLabMonitorTUIAccuracyImprovements_2026-07-10.md) — **implemented monitor truth/freshness hardening**: active TSV precedence, invocation-correct resume manifests, fail-loud manifest/data errors, schema-v2 fetched test counts, source-age display, complete UTM discovery, explicit VM power/online/readiness/run-use columns, canonical cross-platform toolchain preflight, and run-backed roles only.
- [DeepSeekLiveLabOrchestrationPipeline_2026-06-27.md](./DeepSeekLiveLabOrchestrationPipeline_2026-06-27.md) — **DESIGN/pre-build: offload the live-lab run+triage loop to DeepSeek via a new `deepseek_live_lab` MCP function.** Three-tier hierarchy (main agent owns all code changes + security calls; `deepseek-v4-pro` orchestrates the lab read-only-on-repo but executing a *confined allowlisted* lab-action set — launch/progress-stage/restart-VM/clean-env/recover/link-nodes; `deepseek-v4-flash` disposable research+verify sub-agents). The main agent stops hand-driving the orchestrator: one call → v4-pro runs the target area via a structured runbook prompt (main-agent-provided or auto-fetched `brief`, optional per-call tweak) → on fail, a **rigid non-negotiable multi-review pipeline** (Flash research → Flash verify-claims → v4-pro repo-review, all grounded, no code changes) → one verified report back. Includes the confirmed model probe facts (canonical `deepseek-v4-pro`/`-flash` ids, both tool-call-capable, pro reasons by default with no working effort-dial), the confined tool surface mapped to existing orchestrator/recovery/lab-state entry points, the stage-by-stage long-run execution model, the StructuredReport schema, and the 7-step implementation plan. **No DeepSeek tier ever writes the repo, runs gates, or makes the security call — it proposes, the main agent verifies and disposes.**
- [LiveLabSecurityTestCoverage_2026-06-22.md](./LiveLabSecurityTestCoverage_2026-06-22.md) — **threat-coverage map for the adversarial live-lab security suite** + **Tier 0/1/2 build-order priority section (2026-07-01)** at the top. Start here for "is this attack tested, and where, and what do I build next?" Contains: the 18 vulnerability classes × `SecurityMinimumBar` controls × existing coverage, the two CRITICAL trust-path bugs (FINDING-A/RSA-0009 membership revoke, FINDING-B/DD-03 revocation-blind ACL admission) — **both now code-fixed (`a23df5f`, `92e5748`), verified 2026-07-01, but still zero live-lab proof** — the corrected relay-forwarding status (lifecycle ✅ live-proven, frame-forwarding ⚠️ unproven on every OS, HP-3), the CPA-1 audit-chain-integrity ⚠️UNVERIFIED flag, the 111-item full backlog by surface, and (Phase 3, §9-11) the MCP-server + lab-monitor-GUI mechanics for onboarding new stages. **Priority section is stale as of 2026-07-04** — see the status-check doc below for what's actually done.
- [SecurityStageBacklogStatusCheck_2026-07-04.md](./SecurityStageBacklogStatusCheck_2026-07-04.md) — **read-only status check**, written after a 141-commit gap landed on `main`: re-verifies the 6 items this ledger's Tier 0/Tier 1(1-5) priority section calls unbuilt are in fact done and intact (`RSA-0009`/`DD-03`/`RT-2`/`GM-1`/`ENR-1`/`TOCTOU-1`/`DOS-1`, evidence-cited file:line), confirms the 2 lab-monitor GUI backlog items (no `StageCategory`/SECURITY section; `disabled_stages` toggle still has zero effect beyond the synthetic `linux_live_suite` name — code self-documents the lie) are still genuinely open and untouched, confirms Tier 1 priorities 6-16 + Tier 2 (RR-01, FCF-1/2/3, RPT-01/HP-3, S3-10, RSA-0063 live validator, KC-04, PH-7, KL-2/3/4, KC-07, CNT-1, PH-2/3) still have zero code, and flags 2 gaps in the new stage-contract program that touch the security-stage family (coverage-as-code gate enforcement still open; security stages exempt from the new abnormal-termination barrier). Ends with a suggested priority order for picking the backlog back up.
- [CrossPlatformRoleParityPlan_2026-06-21.md](./CrossPlatformRoleParityPlan_2026-06-21.md) — **RELEASE-BLOCKING COMPLETENESS MANDATE: every node role + capability (client/admin/anchor/exit/blind_exit/relay, +nas/llm) must work and be LIVE-LAB-PROVEN on macOS AND Windows, not just Linux.** Single source of truth consolidating the previously-scattered cross-platform parity gap: the live-proven status matrix per role × OS, what remains (mostly LIVE PROVING + a few impls — the topology/validator framework already landed), the per-role × per-OS live-lab acceptance matrix, known blockers (e.g. a WinNAT-capable Windows guest for the exit role), and the parity Definition of Done. **Rustynet is not cross-platform complete until every cell in its §3 matrix is green — do not let this go unaddressed.**
- [LiveLabStageContractPlan_2026-07-03.md](./LiveLabStageContractPlan_2026-07-03.md) — **the live-lab stage-contract program**: one data registry owns the stage vocabulary (`live_lab_stage_registry.rs`), every run emits a run-scoped stage manifest (`orchestration/stage_manifest.json`), the run matrix upserts by run key with interim/final row roles, a closed terminal-state taxonomy + conclusion barrier makes planned stages unable to evaporate silently, and always-on drift gates pin both orchestrators' vocabularies to the registry. Status per finding + deliberate gaps inside; source analysis in [LiveLabFindings_2026-07-03.md](./LiveLabFindings_2026-07-03.md)
- [LiveLabFindings_2026-07-03.md](./LiveLabFindings_2026-07-03.md) — the 2026-07-03 end-to-end live-lab findings pass (both orchestrators, monitor, run matrix, job plumbing, coverage, timers): ten verified findings with evidence and fix shapes; implemented by the StageContractPlan above
- [LiveLabStageTriageLedgerPlan_2026-07-16.md](./LiveLabStageTriageLedgerPlan_2026-07-16.md) — **PROPOSED**: a committed, per-`(stage, OS)` record of *what was tried*, so an agent picking up a failed stage can read the attempt history instead of re-deriving (or repeating) a fix. Two fields only — the error (auto-stubbed by the `--node` engine from the `error_detail` it already writes) and the patch (agent-filled before the verification run, so the ledger row's own commit IS the patch commit). Outcome is deliberately NOT a field: it is derived by joining the stub chain against [live_lab_node_stage_results.csv](../live_lab_node_stage_results.csv), so it cannot drift. Surfaced to agents via the `stage_triage_history` MCP tool; a gate refuses unfilled stubs. `--node` only — the engines' stage vocabularies do not overlap, so a blended history would be meaningless (same rationale as the run-matrix split)
- [LiveLabFindings_2026-07-12.md](./LiveLabFindings_2026-07-12.md) — findings from driving `live_managed_dns_validation` to green on the focused Rust `--node` topology (fixed + committed in `a1e49c1`: SSH host-key pin `:22` port-suffix + exit `Client` capability). Records the three problems that fixing it unmasked/surfaced: (A) `live_reboot_recovery_validation` fails on the missing `/etc/rustynet/assignment-refresh.env` in the focused setup (skip-if-absent vs provision-the-timer), (B) Linux Exit daemon-role intent `admin` vs live `RUSTYNET_NODE_ROLE=client` (stale-env), (C) latent client-less exit spec at `ops_e2e.rs:3365`. Tracked in the §5.1 item of [RustynetUnifiedTodoLedger_2026-07-10.md](./RustynetUnifiedTodoLedger_2026-07-10.md)
- [TrackC_BashOrchestratorDefects_2026-07-13.md](./TrackC_BashOrchestratorDefects_2026-07-13.md) — Track-C log of legacy bash-orchestrator defects surfaced while pairing it against the Rust `--node` engine for the §0.a functional-parity gate. Per owner decision, the retiring bash path is DOCUMENTED, not fixed: BASH-DEF-1 (role collocation — one guest bootstrapped under both `client` and `aux` from its `lab_role`) and BASH-DEF-2 (dns-zone pubkey `/tmp` staging race on the collocated roles, fail-closing `validate_baseline_runtime`). Rust is correct on both. Includes the "pair where clean" config remediation (corrected `debian-headless-4` `lab_role` aux→client).
- [AnchorBundlePullRollbackWatermarkInvestigation_2026-07-20.md](./AnchorBundlePullRollbackWatermarkInvestigation_2026-07-20.md) — investigation → **IMPLEMENTED (`e0cc8e5`, merged to `main`)** fix for the stale-cache rollback vector left open by the review doc below. Confirms the exploit with a concrete step-by-step scenario using real function names, surveys prior art (TUF's version-watermark vs. timestamp-freshness split; Certificate-Transparency-style equivocation defenses as a separate, harder problem this doesn't try to solve), and makes the key finding: `rustynetd` **already shipped and unit-tested** a `MembershipWatermark` mechanism with exactly the needed monotonic-epoch/anti-fork/TOFU-on-absence property, built for daemon bootstrap and never wired into `anchor pull-bundle`. Implemented by extracting it into `rustynet-control` and reusing the same on-disk file rather than building anything new; 3 new tests prove the TOFU case, the exact vulnerability's closure, and the honest residual limit (deleting the watermark itself, not just `--output`, is a real, acknowledged TOFU-reset boundary — pinned by test, not hidden).
- [AnchorBundlePullAttestationSecurityReview_2026-07-20.md](./AnchorBundlePullAttestationSecurityReview_2026-07-20.md) — **design, implementation, and 3-independent-reviewer adversarial security review of the "membership head attestation" fix** for the anchor bundle-pull signature-verification gap (`SecurityMinimumBar.md` §3 control 2). Full trail: the design pass's rejected-alternatives reasoning, the implementation's deliberate deviations, all three reviewers' per-check verdicts (one real defect found+fixed — quorum counted by signer slot not distinct key, closed at `MembershipState::validate()` and found to reach further than intended, blocking state-root derivation for the vulnerable roster shape everywhere in the crate, not just at the final check), and one real defect that was left **open** — a stale-local-cache rollback vector, since closed (see the investigation doc above). Merged to `main` (`1a786dc`, and the rollback fix at `e0cc8e5`).
- [DocCodeDiscrepancyAudit_2026-07-18.md](./DocCodeDiscrepancyAudit_2026-07-18.md) — **documentation-vs-code accuracy audit, 55 findings, individually re-verified against this repo's actual current state** (an earlier pass had run against a stale 7-week-old snapshot; every finding was re-checked here from scratch, not carried forward on trust). 54 of 55 remain applicable; only 1 (production membership-directory wiring) turned out genuinely fixed since. **Two Critical, both got worse over the last 7 weeks despite unrelated feature volume:** `SecurityMinimumBar.md`'s "TLS 1.3 enforced for control-plane APIs" claim still has no real TLS anywhere, and a *new* self-asserted `tls13_valid=true` field is now hardcoded at issuance regardless of any real handshake; the WireGuard-boundary-leakage gate (`CLAUDE.md` §10.3, `scripts/ci/check_backend_boundary_leakage.sh`) fails today (ran it live: exit 78) with 16 violations, up from 11. A third, narrower-than-first-thought gap: `MembershipIncidentResponseRunbook.md`'s revoke-node procedure now correctly denies *new* selection of a revoked peer, but an *already-connected* revoked peer's WireGuard config isn't torn down until daemon restart (`apply_revocation()` exists, is called only from tests). Full severity breakdown and current file:line citations in the doc; recommended priority order in its §6.
- [RustNativeNodeOrchestratorQualityAudit_2026-07-10.md](./RustNativeNodeOrchestratorQualityAudit_2026-07-10.md) — **the 2026-07-10 quality audit and implementation ledger for the Rust `--node` engine**: tracks 17 source-verified findings. RNQ-01–14 now have core code fixes (with explicit remaining live/fault-injection proof where applicable); RNQ-15/16 are partially extracted/consolidated; the real lab/product crate split RNQ-17 remains open. Covers verifier-before-bundle ordering, residue assertions, sealed rerun/resume evidence, honest role columns, failure-dominant finalization, validated dependency graphs, signal/context provenance, exact node identity, pre-cleanup diagnostics, bounded cancellation-aware fanout, early role rejection, module extraction, typed validator dispatch, and product separation.
- [RustNodeOrchestratorCompletionBrief_2026-07-12.md](./RustNodeOrchestratorCompletionBrief_2026-07-12.md) — **completion brief for the Rust `--node` orchestrator**: Definition of Done (Track A structural, Track B parity gate, Track C evidence, Track D promotion/retirement), read order, authoritative sources for completion checklist, track-by-track status summaries, and recommended next-step focus areas
- [MacWinStageParityPlan_2026-07-02.md](./MacWinStageParityPlan_2026-07-02.md) — **cross-platform stage parity plan**: the problem (macOS/Windows have 14 fewer one-off validators vs Linux), the two dispatch paths (DaemonProbeOp enum for cross-platform checks + raw SSH for Linux-only audits), and the Tier 0–4 implementation roadmap (no-op + single-column additions + cross-platform daemon subcommand ports + killswitch-dependent checks + OS-specific validators)
- [TrackC_Pair1_Linux_2026-07-13.md](./TrackC_Pair1_Linux_2026-07-13.md) — **first paired bash↔Rust functional-parity run** (Pair-1, Commit 982a6e2, 2-node Linux exit+client topology): G1–G8 scorecard results, verdict (NOT yet full PASS), Finding P1-1 (cross_network_preflight divergence), Finding P1-2 (residue fail-open + no-op cleanup root cause, HIGH, resolved in 20bca19)
- [LiveLabExecutionEfficiencyPlan_2026-06-20.md](./LiveLabExecutionEfficiencyPlan_2026-06-20.md) — **the operating method for the same-LAN "drive defects to zero" live-lab loop**: the confirmed efficiency primitives (setup/run split, per-node `rebuild_nodes`, single-stage wrapper re-run, `what_will_deploy`, setup-stage resume), the fast inner loop (re-run only the patched node/stage, not the whole pipeline), the mandatory periodic full-validation gate, security/reliability guardrails (no stale-bundle reuse, no control weakening), the never-idle parallel-work protocol, the Windows front-load track, and the same-LAN execution order. Follow this while iterating Linux→macOS→Windows→cross-OS
- [CrossPlatformRoleParityRoadmap_2026-06-22.md](./CrossPlatformRoleParityRoadmap_2026-06-22.md) — **the execution roadmap that operationalizes the parity mandate**: remaining work + effort per mac/win role cell, the ordered implementation program (admin → blind_exit → role-transitions → relay-lifecycle → anchor-live, with relay-forwarding/Windows-exit/Windows-blind_exit explicitly parked + reasons), file-by-file plans for the first three cells, the FAIL-LOUD live-stage spec (live result = stage status; no dry-run-as-pass), and — the throughput half — the optimized test pipeline: **concurrent Windows+macOS labs** on disjoint Debian backbones, second-to-minutes per-role wrapper loops against warm guests, keep-warm via `--rebuild-nodes`, and the **all-on-`main` workflow** (`orchestrate` never re-checks HEAD mid-run, so commit-during-run is safe; `--source-mode working-tree` ships uncommitted edits). Builds on the ParityPlan (status) + EfficiencyPlan (primitives)
- [AutonomousSecurityParityPassLog_2026-06-24.md](./AutonomousSecurityParityPassLog_2026-06-24.md) — progress log for the 2026-06-24 code-only security+parity pass: the commits, the HIGH killswitch-bypass found by the pfctl-boundary adversarial review + its fix, the applied RSA findings (0008/0010/0014/0016/0017/0026/0055/0058), the deferred/kept-as-is ones (0001/0003) with rationale, the workflow-verified cross-OS parity audit (corrects the stale §9), and the code-complete-vs-blocked map
- [LiveLabCoverageAndHonestyAudit_2026-06-25.md](./LiveLabCoverageAndHonestyAudit_2026-06-25.md) — **the cross-OS live-lab coverage + capture-honesty gap map + remediation plan** (5-track parallel audit + external best-practice research, 2026-06-25). Answers "are we *thoroughly* stress-testing all 3 OSes for every role and exploitable surface, and is the captured evidence honest?": the role × OS matrix (macOS=1 dedicated live Rust binary, Windows=0; real parity only on relay + mixed-topology; exit-handoff mac/win is deceptive false-parity), the security-surface × OS matrix (every adversarial surface — endpoint-hijack/enrollment/gossip/STUN-ICE/signed-state — is Linux-only), the ranked capture-honesty findings (incl. the **Linux** fail-open NAT teardown + vacuous DNS proof the 06-24 pass missed), the best-practice benchmark (Mullvad active-probe negative test, Tailscale natlab NAT-matrix + WFP/Router parity, TUF/ByzzFuzz/Jepsen chaos, `pfctl-rs`, Cure53/X41 audit-evidence bar), and the risk-ordered Wave 0–5 plan. **nas/llm have zero coverage on any OS.** Companion to `LiveLabSecurityTestCoverage_2026-06-22.md` (threat-class map) + the ParityPlan (status)
- [FocusedLiveLabRoleGapAnalysis_2026-07-02.md](./FocusedLiveLabRoleGapAnalysis_2026-07-02.md) — focused static analysis of the three near-term unconfirmed/hardening targets: Linux `blind_exit`, macOS `admin`, and Windows `anchor.bundle_pull`. Records what each live stage must demand, what currently exists in code/docs, the most likely failure point, and the concrete work needed to pass without weakening security criteria. No live labs were run for this analysis.
- [LiveLabWave0_LinuxHonestyFixes_2026-06-25.md](./LiveLabWave0_LinuxHonestyFixes_2026-06-25.md) — **Wave 0 plan + implementation spec** from the coverage audit: the Linux honesty fixes (fail-open exit-NAT teardown F0.1–F0.3, vacuous DNS-leak proof F0.4–F0.6, relay dry-run-as-pass honest demotion F0.7, two-hop config-text-only F0.8, lan-toggle weak-negative F0.9, demotion-residue internal_prefix F0.10), each with file:line, the exemplar pattern to copy, the exact fix, the test to add, the disjoint per-agent file-ownership map, shared JSON contracts, and the Wave-0 Definition of Done. All Linux-compilable + unit-testable, no live lab
- [LiveLabWave5Chaos_InertScaffolds_2026-06-25.md](./LiveLabWave5Chaos_InertScaffolds_2026-06-25.md) — **Wave 5 (chaos) plan + spec**: implement the 3 inert chaos scaffolds for real — `clock_attack` (libfaketime per-process clock skew → TUF freeze/rollback assertions), `crash_recovery` (kill -9 at trust-state persistence boundaries → atomic old-or-new, no watermark-downgrade), `resource_exhaustion` (decompression-bomb/endless/oversized ingest → bounded-cap reject, no panic). Untested on ALL OSes today; Linux-targeted live tests that compile + unit-test here, live-run-pending. Templates = the 5 real chaos tests; honesty rules + disjoint per-binary ownership; do-not-touch the shared harness
- [LiveLabWave2_CoreRoleCrossOsParity_2026-06-25.md](./LiveLabWave2_CoreRoleCrossOsParity_2026-06-25.md) — **Wave 2 plan + spec**: port the Linux core-role assertion binaries (exit-handoff *real failover*, two-hop, lan-toggle/blind_exit, managed-DNS, role-switch, network-flap, reboot-recovery, enrollment-restart) to drive macOS/Windows guests via the cross-OS `RemoteShellHost` seam (runtime `match platform` branches, not `cfg`), removing the `enforce_linux_only_until_validator_lands` gates. Deliverable = code-complete + unit-tested, **live-run-pending**. Includes the relay/mixed-topology templates, the no-fake-pass + flag-daemon-gaps honesty rules, the disjoint per-binary agent-ownership map, Batch A/B split, and the DoD
- [LiveLabWave1_IntegratedPipelineHonestSkips_2026-06-25.md](./LiveLabWave1_IntegratedPipelineHonestSkips_2026-06-25.md) — **Wave 1 plan + spec**: make the rust-native pipeline's cross-OS "reported-skip" cells (Windows relay deploy+validation, macOS/Windows anchor bundle-pull runtime) return `StageOutcome::Skipped` (→ `RunStatus::Partial`) instead of `Passed`-with-a-side-car-JSON, so a Windows-relay / macOS-anchor run no longer shows a green run/stage. Includes the safety analysis (Skipped deps don't block downstream; empty-role labs still pass), per-stage fixes, tests, and the per-node-matrix follow-up that's out of scope
- [LinuxBlindExitDataplane_2026-06-25.md](./LinuxBlindExitDataplane_2026-06-25.md) — **closes the fail-OPEN `blind_exit`-on-Linux gap** the role × OS matrix surfaced: membership permits `blind_exit` on Linux but the dataplane ignored the flag and ran the *regular* NATing exit (masquerade + unrestricted forward + own-egress allow — none of the hardening). New `linux_blind_exit` nftables module (mesh-scoped final-hop forward, no NAT, local-origin tunnel-only, terminal drop, bounded mesh-CIDR via the shared verifier, irreversible rollback) mirroring the reviewed `macos_blind_exit` PF anchor; `apply_nat_forwarding`/`rollback_nat_forwarding` wired; privileged-helper argv allowlist extended (least-privilege `flush chain … forward` + mesh-scoped saddr rule). 13 new unit tests (builder/evaluator/allowlist) pass; fmt+clippy clean. **Live-run-pending** (lab proves the live `nft` apply). Also records a separate deferred finding: the framed `PrivilegedCommandClient` made every line-based `spawn_privileged_capture_helper` socket test latently broken (masked by the Linux-CI bootstrap failure)
- [CrossPlatformCiHealth_2026-06-25.md](./CrossPlatformCiHealth_2026-06-25.md) — **the cross-platform-CI breakage cleanup + the windows-gnu cross-clippy runbook**: how the Windows CI lints are reproduced locally on Linux (`x86_64-pc-windows-gnu` cross-clippy over the 10 CI packages) with **zero CI round-trips**, the commits that cleared ~80+ pre-existing Windows clippy errors + the crypto/local-security/backend-wireguard lints, the **real Windows trust-state persist bug** fixed (`atomic_write_secure` parent-dir fsync → `cfg(unix)`, mirroring the rustynet-crypto fix), and the remaining CI TODOs (macOS flaky `vm_lab` subprocess tests — left; Debian/Linux-E2E `cargo: not found` bootstrap — deferred; two behavior-preserving Windows dead-stub removals to review; the Windows Security-gates step that runs `cargo audit`/`cargo deny` for the first time once the build is green). Companion to the coverage audit §8 TODO list
- [CrossOsRoleSwitchPlan_2026-06-24.md](./CrossOsRoleSwitchPlan_2026-06-24.md) — design for the **live cross-OS role-transition** parity cell (the ParityPlan §3 row the Roadmap §4 cell #4 depends on): reuse the single verified apply path `refresh_signed_state_with_reason` + the `StateRefresh` IPC (never a second/weaker branch), the per-OS role-flip seam (launchd plist rewrite + bootout/bootstrap on macOS; `windows_service` StateRefresh on Windows), the LocalOnly/SignedMembership/Irreversible ordering invariants (deploy-before-advertise, undeploy-before-revoke, blind_exit re-enroll through the single-use token path), and the FAIL-LOUD stage spec. Supersedes the gitignored `state/cross_os_role_switch_plan.md` the matrix previously cited
- [RustynetDataplaneExecutionPlan_2026-05-18.md](./RustynetDataplaneExecutionPlan_2026-05-18.md) — source-of-truth plan for the cross-network dataplane track (peer-distributed coord, home-server-as-zero-ingress-relay, uPnP/IPv6/ICE, enrollment-token onboarding, anchor-role formalisation, 8-role user-selectable surface incl. service-hosting nas/llm); supersedes nothing but is the active ledger for D2-D13 work
- [CrossNetworkTraversalDesignDecisions_2026-07-19.md](./CrossNetworkTraversalDesignDecisions_2026-07-19.md) — **DESIGN PROPOSAL / owner-decision framing (no code written)** resolving the open NAT-traversal design questions the unified ledger §8 leaves for D14.c–f. Verifies current state against code (STUN client is transport-only with zero classification; two parallel signed-message systems — the self-signed `GossipBundle` with no type-discriminator byte, and the lab-only authority-signed `SignedTraversalCoordinationRecord` that already carries `probe_start_unix`; port-delta + mailbox are 100% greenfield). Frames **D14.c** (honest NAT/CGNAT discovery) as a buildable advisory module whose only subtlety is that public STUN can't do RFC 5780 CHANGE-REQUEST → `Unknown`-is-conservative; poses the **load-bearing D14.d decision** (punch-timing trust model: **Option A self-signed proposal over gossip + unilateral accept — recommended** vs Option B authority-signed — rejected as reintroducing a coordination host) which needs a bounded gossip-wire-version + type-tag change once picked; and keeps **D14.e port-delta** + **D14.f mailbox** GATED with the risk analysis (both need owner sign-off; mailbox needs a written threat model first). Companion to the dataplane plan §D14 (which owns the slice IDs + bans)
- [CrossNetworkSubstrateIntegrationSpec_2026-06-21.md](./CrossNetworkSubstrateIntegrationSpec_2026-06-21.md) — **focused integration spec to make the cross-network live-lab stages actually run.** Resolves the one open design decision (substrate↔validator mapping: Tier A netns = deterministic NAT-matrix gate via classification tooling + a new daemon-in-netns validator; Tier B vxlan = the existing SSH e2e validator suite on separate-VM cross-NAT; Tier C slirp = cross-OS smoke), specifies the exact orchestrator wiring (`--cross-network-substrate` selector + `stage_run_cross_network_substrate_setup/_teardown` at the seam, orchestrator line 8291, before the per-profile loop), the phased plan (X1 Tier A → X2 Tier B → X3 the three §4.1 stages → X4 Tier C), files-to-change, gaps (netns lacks `double_nat_cgnat`; vxlan lacks uPnP/v6 modifiers), and the §D5.1 acceptance criteria. Companion to the dataplane plan §D5.1 (which owns the why + NAT vocabulary + pass criteria). ~80% of the substrate is already built; remaining work is integration glue, not invention
- [LiveLabVmConnectivityImplementation_2026-07-10.md](./LiveLabVmConnectivityImplementation_2026-07-10.md) — **execution ledger for the VM connectivity rulebook (§15.8 slices A–E)**: per-slice checklist + evidence for the dual-plane lab-network program. Slice A (read-only typed network-profile model `vm_lab/network_profile.rs` + audit/preflight `vm_lab/network_audit.rs`, reviewed manifests under `profiles/vm_lab/network/`, `ops vm-lab-network-audit`/`vm-lab-network-preflight`, redacted `state/vm_network_evidence.json`) status and the live-audit baseline findings live here. No VM network mutation may happen before Slice A acceptance is recorded in this ledger.
- [DataplanePerfBacklog_2026-06-12.md](./DataplanePerfBacklog_2026-06-12.md) — remaining hot-path performance items after the 2026-06-11/12 pass (engine outcome sink, relay await-recv, utun readv/writev, endpoint index), each with expected impact, approach, invariant pins, and the criterion bench to measure against; also records what landed (`6045ba7`, `611dc29`, `71255b5`) with measured numbers
- [EfficiencyAndAdvancedTechniqueOpportunityCatalog_2026-07-19.md](./EfficiencyAndAdvancedTechniqueOpportunityCatalog_2026-07-19.md) — **RESEARCH CATALOG, unscheduled, not a plan.** 31 code-grounded, independently-verified findings (11 subsystems: key custody, ACL/policy evaluation, membership/trust-state, enrollment, canonical serialization, NAT traversal, relay accounting, control-plane concurrency, build/test speed, CLI/daemon startup, Windows/service-hosting data paths) beyond what `DataplanePerfBacklog`/FIS-0001..0008 already cover — each with multiple candidate technique families (incl. Merkle/succinct-proof and blind-signature/ZK options where genuinely applicable, explicitly flagged where blocked by the audited-crate-only rule) and no chosen fix; the next agent designs and picks the approach
- [FableEfficiencyImplementationPlans_2026-07-19.md](./FableEfficiencyImplementationPlans_2026-07-19.md) — **PLAN-ONLY, unscheduled, no code written.** FIS-style implementation plans for 11 findings selected from the opportunity catalog above (BLD-1/2/3, CLI-2/3, RLY-1, CCY-1 narrow, CCY-3, WIN-3, NAT-2 narrow, NAT-3): each section re-verifies the catalog's current-behavior claims against the live tree (drift called out — e.g. CCY-3's shadow gossip state has zero readers; `rustynetd` deliberately compiles windows-native stubs cross-platform), picks the single best technique with runner-up reasoning, and gives a constraint check, phased S/M/L build path, and concrete pass/fail measurement per fix — strictly efficiency/dev-tooling/architecture-hygiene scope, nothing touching crypto, key custody, signature verification, ACL evaluation, privileged exec, route/killswitch mutation, or DNS fail-closed paths
- [NodeRoleTaxonomy_2026-05-21.md](./NodeRoleTaxonomy_2026-05-21.md) — canonical taxonomy for the six user-selectable node roles (D12): `relay`, `anchor`, `exit`, `blind_exit`, `client`, `admin`; preset compositions; transition matrix; per-platform eligibility
- [AnchorNodeRoleDesign_2026-05-21.md](./AnchorNodeRoleDesign_2026-05-21.md) — canonical design for the anchor node role (D11; one of the six presets in the taxonomy doc): role definition, what already exists in code, what needs building, per-platform host capability (Linux/macOS/Windows host; iOS/Android consume), refactor inventory, security controls
- [NodeRoleTaxonomyExtension_2026-06-11.md](./NodeRoleTaxonomyExtension_2026-06-11.md) — extends the six-role taxonomy to **eight** by adding the two service-hosting presets `nas` and `llm` (D13): the service-hosting role category, new `serves_nas`/`serves_llm` capabilities, eight-role transition matrix, tunnel-only + default-deny secure-exposure model, per-platform eligibility, and the §6.E security-control delta. Parent of the two role design docs below
- [NasNodeRoleDesign_2026-06-11.md](./NasNodeRoleDesign_2026-06-11.md) — deep dive for the `nas` node role: `rustynet-nas` sibling service, tunnel-only storage exposure with per-peer namespaces + at-rest encryption, the node-side **RustyBackup** client contract, per-platform, security controls, and build slices (D13.c)
- [LlmNodeRoleDesign_2026-06-11.md](./LlmNodeRoleDesign_2026-06-11.md) — deep dive for the `llm` node role: `rustynet-llm-gateway` in front of a loopback inference engine, **identity-from-tunnel (no API key)** + gRPC/HTTP-2 token streaming as plaintext inside the WireGuard tunnel, exit-node coexistence (internet via exit, LLM intra-mesh), admin access governance, the node-side **RustyAI** client contract, an industry survey (Tailscale / SPIFFE-SPIRE / LLM streaming transports), security controls, and build slices (D13.d)
- [ServiceHostingRolesDeltaPlan_2026-06-11.md](./ServiceHostingRolesDeltaPlan_2026-06-11.md) — delta ledger driving **D13**: current-state-vs-target gaps verified against the code, ordered slices D13.a–e, defect carry-overs from the security audits, gate plan, and live-lab readiness for `nas`/`llm`
- [ServiceHostingRolesRoadmap_2026-06-11.md](./ServiceHostingRolesRoadmap_2026-06-11.md) — top-level program roadmap for the NAS + LLM roles: the document set, milestones M0–M6, dependency graph, per-milestone gate checklist, risk register, and status tracker. **Start here** for the service-hosting-roles program
- [MasterWorkPlan_2026-03-22.md](./MasterWorkPlan_2026-03-22.md)
- [PlugAndPlayTraversalRelayDeltaPlan_2026-03-29.md](./PlugAndPlayTraversalRelayDeltaPlan_2026-03-29.md)
- [OpenWorkIndex_2026-04-17.md](./OpenWorkIndex_2026-04-17.md)
- [PlatformImprovementBacklog_2026-05-14.md](./PlatformImprovementBacklog_2026-05-14.md) — most recently updated cross-platform improvement ledger; the X1-X7 numbered tracks are the cleanest current path for code-only quality work
- [HomelabConnectivityParityDeltaPlan_2026-05-21.md](./HomelabConnectivityParityDeltaPlan_2026-05-21.md) — active delta ledger for closing the macOS/Windows tunnel connectivity gap vs Linux baseline; covers macOS IPv6 (M3), macOS wireguard-go prereq (M2), macOS key-retention bug (M1), macOS killswitch (M4), Windows host candidate enumeration (W1), Windows gateway detection (W2), Windows E2E test coverage (W3), Windows IPv6 audit (W4), and cross-platform uPnP IGD orchestrator (C1)
- [AnchorLiveLabAndCrossPlatformRoleDeltaPlan_2026-05-23.md](./AnchorLiveLabAndCrossPlatformRoleDeltaPlan_2026-05-23.md) — delta ledger covering three parallel tracks. Track A adds live-lab coverage for the anchor role (five sub-stages exercising bundle-pull, gossip priority, enrollment endpoint, port-mapping authority, and downgrade revocation); non-destructive scaffold has landed; enrollment/downgrade destructive sub-stages are follow-up work. Track B closes the topology-selection + per-platform validator gaps that previously blocked Windows / macOS from being the active exit_server / relay / anchor in the orchestrator; **all seven steps now landed** (topology selector, macOS exit validators, platform-aware role-transition planner + per-OS exit/relay installers, Windows active-exit promotion stage, macOS relay lifecycle dry-run, and `ops e2e-bootstrap-{macos,windows}` non-Linux genesis verbs). Track C adds an adversarial / fault-injection / chaos suite (8 categories, 30+ stages) covering daemon crashes, clock attacks, signed-state forgery, crash recovery, resource exhaustion, network impairment, membership poisoning, and privileged-boundary stress — Track C remains planning-only

## Active Phase Checklists

These are the phase-local hardening checklists that are still open. The
phase checklists that finished (Phase 1, 2, 3, 5) have moved to
[`../done/`](../done/README.md); see that index for the archived slices.

- [Phase4LiveLabEvidenceRefreshChecklist_2026-04-12.md](./Phase4LiveLabEvidenceRefreshChecklist_2026-04-12.md) — still open on fresh-install + canonical cross-network evidence regeneration for a clean current `HEAD`
- [Phase5ReleaseReadinessSummary_2026-04-12.md](./Phase5ReleaseReadinessSummary_2026-04-12.md) — the Phase 5 checklist itself is archived; this summary is kept active because it is the operator-facing readiness picture and still records remaining full release-gate blockers
- [Phase6CrossNetworkAndSharedTransportChecklist_2026-04-13.md](./Phase6CrossNetworkAndSharedTransportChecklist_2026-04-13.md) — code-side cross-network/shared-transport hardening done; canonical cross-network + extended soak evidence still to regenerate

## Active Plans And Backlogs

- [BlindExitPcbHardwarePlan_2026-07-22.md](./BlindExitPcbHardwarePlan_2026-07-22.md) — **new workstream, DESIGN stage: a dedicated, minimum-traceability PCB appliance implementing the `blind_exit` node role** (a hardware counterpart to the existing software-only role, not a code change). Supersedes the undescribed docx report below as source of truth — that report's network diagram assumed NAT is present, which contradicts the actual no-NAT `blind_exit` dataplane (`linux_blind_exit.rs`/`macos_blind_exit.rs`), and its cited hardware source file doesn't exist in this repo. Locks: LAN-side/single-Ethernet topology behind the user's router (matches the no-NAT code); off-the-shelf Allwinner H616/H618-class SoC for v1 (open RISC-V softcore on an FPGA and a real ASIC shuttle were both evaluated and parked as a v2/stretch track, not rejected outright); Gigabit Ethernet via EMAC0 + external RGMII PHY; a 2.4GHz-only WiFi module as failover-only backup; kernel WireGuard backend, not userspace/boringtun; throughput targets derived from "10 devices streaming video comfortably" (100Mbps aggregate Ethernet, ~33Mbps aggregate WiFi failover, both worst-case peak not continuous); no vendor secure boot / no vendor Android BSP given Allwinner's historical BSP-backdoor precedent (confirmed not H616-specific); the eFuse chip-ID treated as a traceability liability, never exposed; 1GB RAM / 8GB soldered eMMC (not microSD) with signed A/B updates and no swap-on-flash; and the Allwinner FEL USB recovery mode as the hardware-level factory-reset path. §5 lists what still needs a real hardware benchmark before any BOM/schematic is finalized.
- [UbuntuHostLabControlFindings_2026-07-23.md](./UbuntuHostLabControlFindings_2026-07-23.md) — **live capability-parity verification of `ubuntu-kvm-1`**: confirms the KVM host has agent-driven VM status, per-guest IP, source sync, on-guest release build of `rustynetd`+`rustynet-cli`, and install — all working. **But a full live-lab run on box guests is still BLOCKED**, and the doc logs **5 pre-existing, non-box-specific bugs by severity**. Headline: BUG-BOX-5 (**HIGH** — the `--node` orchestrate pipeline's first node-readiness gate (`mod.rs:12222`) is UTM-only, so a run on libvirt guests aborts at stage 1 with *"local UTM discovery did not report the selected aliases"*; confirmed live, this is why the box's ledgers have zero `linux-x86` rows). Then BUG-BOX-1 (MEDIUM — `get_vm_diagnostics` calls `vm-lab-collect-artifacts --report-dir` but the CLI requires `--output-dir`, artifact collection always exits 64), BUG-BOX-4 (MEDIUM — `launch_live_lab_on_host` omits the required `--known-hosts-file`, a node run self-destructs at arg-validation), BUG-BOX-2 (LOW-MED — the manual `restart-runtime` bootstrap phase assumes a systemd unit no phase installs → status-5 on a fresh guest; the orchestrate deploy path is unaffected), BUG-BOX-3 (LOW — MCP tool calls time out client-side at ~60s on slow ops, CLI-direct is the documented workaround). Companion to `LinuxVmHostPlan_2026-07-14.md` (the owning program).
- [UbuntuHostLabControlRemediationPlan_2026-07-23.md](./UbuntuHostLabControlRemediationPlan_2026-07-23.md) — **DESIGN / PATCH PLAN (no code written)** for the 5 findings in the doc above: per-bug current-behaviour (file:line), fix options + tradeoffs, recommended fix, test, blast radius, and a sequencing/verification plan. Headline BUG-BOX-5 fix = controller-kind dispatch in the `--node` readiness gate (`orchestrator/readiness.rs:57`/`:165`, NOT the legacy-bash `mod.rs:12222` gate — location corrected after an adversarial review that caught the original draft citing a path `--node` never executes), reusing the already-libvirt-wired `observe_local_utm_target_ready` observer so the `--node` pipeline stops calling UTM discovery unconditionally. BUG-BOX-4 = inject `--known-hosts-file` into the launch template mirroring the existing `--ssh-identity-file` default. BUG-BOX-1 = one-line MCP flag fix (`--report-dir`→`--output-dir`) + rebuild. BUG-BOX-2 = install the systemd unit in the manual chain via the existing `install_rustynetd_service` bin + fail-loud. BUG-BOX-3 = doc/description containment. §8 has the owner-decision queue. None are security-sensitive; full §7 gates apply.
- [LinuxVmHostPlan_2026-07-14.md](./LinuxVmHostPlan_2026-07-14.md) — **stand up a dedicated x86-64 Linux box as a second, larger VM host (~10 guests) driven by libvirt/QEMU/KVM, integrated with the run-on-host `--node` orchestrator.** Owner-ratified **Tier 2** (full UTM-parity lifecycle: start/stop, ship code, compile, run the live-lab suite); §11 tracks the gated increment plan. Records that **no prior dedicated-Linux-host plan existed** and the whole program assumed the single macOS+UTM host. Key finding: the `--node` orchestrator is **SSH-throughout except a thin `utmctl` power+IP readiness gate**, so the libvirt work is confined there and Linux guests need no guest-agent channel. VM-lifecycle was hardcoded to `VmController::LocalUtm`/`utmctl` (no libvirt/virsh anywhere); **increment 1 landed** — the `VmController::Libvirt { domain, connect_uri }` variant + parser + fail-closed power/IP gates (virsh power = increment 2, IP discovery = increment 3). Strongest motive: an x86 KVM host does **nested virt**, unblocking the Windows **WinNAT/exit** parity cell the Apple-Silicon UTM lab structurally cannot prove (§1.1). Includes the OS recommendation (Debian minimal + libvirt/KVM primary; Proxmox VE alternative), a flash/host-setup runbook, the networking model, security baseline, and the owner-decision queue
- [AnchorNodeRoleDesign_2026-05-21.md](./AnchorNodeRoleDesign_2026-05-21.md)
- [NodeRoleTaxonomy_2026-05-21.md](./NodeRoleTaxonomy_2026-05-21.md)
- [NodeRoleTaxonomyExtension_2026-06-11.md](./NodeRoleTaxonomyExtension_2026-06-11.md)
- [NasNodeRoleDesign_2026-06-11.md](./NasNodeRoleDesign_2026-06-11.md)
- [LlmNodeRoleDesign_2026-06-11.md](./LlmNodeRoleDesign_2026-06-11.md)
- [ServiceHostingRolesDeltaPlan_2026-06-11.md](./ServiceHostingRolesDeltaPlan_2026-06-11.md)
- [ServiceHostingRolesRoadmap_2026-06-11.md](./ServiceHostingRolesRoadmap_2026-06-11.md)
- [CrossNetworkRemoteExitNodePlan_2026-03-16.md](./CrossNetworkRemoteExitNodePlan_2026-03-16.md)
- [CrossPlatformSecurityGapRemediationPlan_2026-03-05.md](./CrossPlatformSecurityGapRemediationPlan_2026-03-05.md)
- [DiagnosticFunctionsRoadmap.md](./DiagnosticFunctionsRoadmap.md)
- [HeterogeneousLiveLabEvidence_2026-04-28.md](./HeterogeneousLiveLabEvidence_2026-04-28.md)
- [LinuxUserspaceSharedLiveLabReadinessDelta_2026-04-02.md](./LinuxUserspaceSharedLiveLabReadinessDelta_2026-04-02.md)
- [MagicDnsSignedZoneSchema_2026-03-09.md](./MagicDnsSignedZoneSchema_2026-03-09.md)
- [OsAgnosticOrchestratorAndWindowsPeerDeltaPlan_2026-04-27.md](./OsAgnosticOrchestratorAndWindowsPeerDeltaPlan_2026-04-27.md)
- [RustNativeMultiPlatformOrchestratorPlan_2026-04-28.md](./RustNativeMultiPlatformOrchestratorPlan_2026-04-28.md)
- [VmLabCapabilityCookbook_2026-04-14.md](./VmLabCapabilityCookbook_2026-04-14.md)
- [VmLabCapabilityReportingPlan_2026-04-14.md](./VmLabCapabilityReportingPlan_2026-04-14.md)
- [VmLabCapabilitySources_2026-04-14.md](./VmLabCapabilitySources_2026-04-14.md)
- [WindowsExitAndRelayDeltaPlan_2026-05-10.md](./WindowsExitAndRelayDeltaPlan_2026-05-10.md)
- [WindowsExitNodeRunbook_2026-06-04.md](./WindowsExitNodeRunbook_2026-06-04.md) — how to run + prove a **successful active full-tunnel Windows exit** live lab. The `active_exit` orchestration stage (drives `route advertise 0.0.0.0/0` over the daemon control pipe → asserts forwarding + NAT + client-egress `Get-NetNatSession`) is built, unit-tested, and fail-path-validated (r54). The ONLY remaining gate is a Windows guest with the WinNAT/HNS stack — the current `windows-utm-1` lacks it (`MSFT_NetNat` class absent). Includes the WinNAT-readiness verify one-liner, the default-route prereq, the exact run command, and the deliberate `role.rs` support-flag promotion step for after a green run
- [WindowsLabVmStabilityAndSessionModel_2026-04-30.md](./WindowsLabVmStabilityAndSessionModel_2026-04-30.md)
- [WindowsLiveLabReadinessPlan_2026-05-31.md](./WindowsLiveLabReadinessPlan_2026-05-31.md) — focused readiness ledger for running live-lab **on a Windows node**: `restart-runtime` GREEN; **N1.3/N2/N3 done + merged** (first WireGuard-NT tunnel bring-up, then killswitch + DNS fail-closed in protected mode); **G8 IPv6 fail-closed mechanism validated live** (apply/block/rollback under the killswitch; full leak-proof deferred — the lab has no global IPv6); **N4 (two-node mesh) blocked on lab networking** (Windows is LAN-only and no peer currently shares its subnet). Ordered path N1-N6 (single-node tunnel smoke → killswitch/DNS fail-closed → two-node mesh → roles → full matrix) + enablers E1-E3 (Windows CI gate, native WFP killswitch, gate-timing CSV); cross-links the detailed Windows/connectivity/role ledgers
- [WindowsUtmTransportArchitecture_2026-04-30.md](./WindowsUtmTransportArchitecture_2026-04-30.md)
- [WindowsVmLabAccessOrchestrationRecoveryPlan_2026-04-16.md](./WindowsVmLabAccessOrchestrationRecoveryPlan_2026-04-16.md)
- [WindowsWorkingNodePlan_2026-04-17.md](./WindowsWorkingNodePlan_2026-04-17.md)
- [MacosUserspaceSharedBackendPlan_2026-05-08.md](./MacosUserspaceSharedBackendPlan_2026-05-08.md)
- [OvernightAutonomousBugHuntProposal_2026-06-08.md](./OvernightAutonomousBugHuntProposal_2026-06-08.md) — **Overnight Autonomous Verified-Plane March** (v2, agent-driven rewrite). Proposal for an unattended overnight system that marches the verified-working plane (every role × every OS) toward green with **no human in the loop**. Reframes the goal as net-new *implementation* (Windows relay SCM adapter, anchor deferred substages, macOS dataplane depth), not bug-patching, and leans on the **live lab as an objective oracle** to bound the blast radius to a throwaway branch. Three layers: a thin Rust **driver** (`ops vm-lab-overnight`) owning a typed **frontier backlog** (verified/red/flaky/unbuilt cells) + scheduler/branch-isolation/anti-tarpit; a **fresh tool-using agent spawned per work-unit** (solves context exhaustion) that implements/fixes a cell using the existing `rustynet-repo-context`/`rustynet-lab-state`/`rustynet-gate-runner` MCP servers, the durable loop journal, and the `overnight-live-lab-loop` playbook (all already in-tree, bc9753b); and shared filesystem state. Safety envelope: dedicated `overnight/<date>` branch never `main`/pushed, security-crate denylist + adversarial second-agent review, `git reset --hard && git clean -fd` (+ optional worktree) between units, mandatory gates + oracle re-verify, autonomy dial. Supersedes the v1 "standalone binary + one-shot LLM JSON patch" design (deletes the fabricated `NodeAssignment`, the `rustynet-cli`-vs-`rustynet-mcp` mislabel, the `mod loop;` keyword bug, the incomplete revert, blind_exit in rotation, and round-robin re-running greens). blind_exit permanently excluded (irreversible identity wipe, `role_presets.rs:438`)
- [ProductionTransportOwningWireGuardBackendPlan_2026-03-31.md](./ProductionTransportOwningWireGuardBackendPlan_2026-03-31.md)
- [RustyfinExtensionTrustPlan_2026-05-10.md](./RustyfinExtensionTrustPlan_2026-05-10.md)
- [RustynetComparativeVpnExploitCoverage_2026-03-14.md](./RustynetComparativeVpnExploitCoverage_2026-03-14.md)
- [SecurityAuditLedger_2026-06-18.md](./SecurityAuditLedger_2026-06-18.md) — **review-only**, file-by-file security audit ledger (RSA-#### namespace) against `SecurityMinimumBar.md` and the audit charter checklist. **COVERAGE COMPLETE: 594/594 tracked code/config files have a dated row** (Tier 0–4 + vendored), audited highest-blast-radius-first across 8 multi-agent fan-out batches with first-hand auditor verification of every load-bearing finding, **then an adversarial re-verification pass + theme sweeps (truncation / `#[allow]` / verification-bypass).** **76 findings raised → 2 withdrawn as false-positive → 74 standing: 0 Critical / 2 High / 15 Medium / 34 Low / 19 Info / 4 Question.** The two Highs: RSA-0009 (membership reducer non-determinism → revoke/key-rotation can't apply; AUDIT-040) and RSA-0063 (macOS bootstrap leaves `NOPASSWD:ALL` sudoers on failure → local privesc; AUDIT-045/RN-32) — both survived adversarial re-verification. Re-verifies + extends RN-* / AUDIT-* / HB-* (incl. AUDIT-006 now remediated, RN-03/04/10/N1 confirmed fixed); supply chain clean (`cargo audit` 0/210, `cargo deny` OK); includes a SecurityMinimumBar Critical-control trace, an adversarial-re-verification log, and Appendix A sources. **All findings are proposals awaiting human approval — no production code/crypto/config changed.**
- [SecurityRemediationPlan_2026-06-19.md](./SecurityRemediationPlan_2026-06-19.md) — sequences the audit ledger's 73 findings into remediation **waves** (P0 = the 2 Highs RSA-0009/RSA-0063; P1 = Mediums grouped by 7 systemic themes A–G; P2 = Low/Info by category) with per-item proposed fix + verification test + effort estimate, plus a 6-item owner-decision queue for the Question findings. Planning only — no code change; flip ledger rows `open→applied` as fixes land.
- [SecurityAndQualityAudit_2026-06-10.md](./SecurityAndQualityAudit_2026-06-10.md) — fresh full-repo security + correctness audit (every workspace crate, vendored boringtun, all scripts/CI, the agent tooling), 14 independent deep reviews + first-hand verification. **53 net-new findings (AUDIT-001..053): 0 Critical / 11 High / 19 Medium / 16 Low / 7 Info**, each with CWE, file:line, exploit scenario, confidence, and a fail-closed fix; plus a full reconciliation of RN-01..RN-38 / HB-1..HB-7. Verdict: **NO-SHIP until the P0 set closes.** Top risks: the fail-closed killswitch can fail open (RN-03/04/10), the uncommitted overnight driver's live path is destructive + commits to `main` (AUDIT-017/018/019 — do not run without `--dry-run`), the `lab_state` MCP server has an unconfined `report_dir` (arbitrary host read/delete, AUDIT-006), the membership reducer is non-deterministic so revocation/key-rotation cannot be applied (AUDIT-040), Windows encrypted-key custody ACL is a no-op (AUDIT-027/RN-33), a relay pre-auth DoS (AUDIT-031), and the macOS bootstrap leaves a `NOPASSWD: ALL` sudoers file on failure (AUDIT-045/RN-32). Good news: the prior backlog's headline open High **RN-06 (Windows killswitch IPv4 LAN leak) is now fixed** (WFP egress allow-lists). Includes cross-cutting themes, trust-boundary threat-model results, verified-solid controls + a SecurityMinimumBar control trace, a P0/P1/P2 remediation roadmap, and a 100% coverage ledger
- [SecurityHardeningAudit_2026-04-28.md](./SecurityHardeningAudit_2026-04-28.md)
- [SecurityHardeningBacklog_2026-06-01.md](./SecurityHardeningBacklog_2026-06-01.md) — actionable hardening TODO tracker (not a fresh review). Section A: net-new items on the single-node Windows smoke/harness code added 2026-05-31..06-01 (ephemeral cleartext key uses plain unlink not secure-scrub; `write_runtime_private_key` `0o600` is unix-only so smoke `%TEMP%` keys rely on inherited ACLs; bare-`netsh` recovery paths; Drop guard skips the IPv6-block rule; ipv6-smoke probes a hardcoded public IPv6; vm-lab PowerShell-string surface). Section B: the highest-priority OPEN findings from `SecurityReview_2026-05-24.md` **re-verified first-hand on `main` `e01dd64` (2026-06-01)** with current file:line (P0 RN-03 discarded force_fail_closed 10/44 sites, RN-04 pre-killswitch opt-in+Linux-only, RN-05 non-`node:` selectors bypass revocation, RN-06 Windows killswitch IPv4 LAN egress allow-all, RN-07 IPv6 leak [G8 partial], RN-11 permissive-on-empty) plus a best-practice assessment — the review's directions are sound, but the strongest Windows-killswitch fix moves the egress policy to **WFP** (not netsh rules, per E2), the pre-killswitch should be mandatory+cross-platform, and empty membership/context should deny. Records that G8 partially remediates RN-07 while RN-06 (IPv4 LAN-egress scoping) remains the larger open piece
- [SecurityReview_2026-05-24.md](./SecurityReview_2026-05-24.md) — firm-grade security review across six domains (privileged boundary, crypto/key custody, trust/anti-replay, untrusted-input/IPC, dataplane fail-closed/killswitch, secret-hygiene/supply-chain). 38 findings (7 High / 9 Medium / 17 Low / 5 Info) with CWE, file:line, exploit scenario, and a prioritized P0/P1/P2 roadmap; load-bearing findings verified first-hand. Now also includes scope/asset/trust-boundary mapping, a threat model (10 actor profiles), methodology limitations + residual-risk statement, indicative CVSS, compound exploit chains, a CLAUDE.md/SecurityMinimumBar compliance matrix, a master finding-status tracker, reproduction notes, and references/glossary. Remediation log RL-1..RL-8: **8 findings fixed + tested** (RN-01 membership-decoder DoS, RN-14 unsafe-lint enforcement, RN-22 ed25519 verify_strict, RN-24 zeroize, RN-23 keychain id, RN-19 symmetric argv gate, RN-17 helper peer-cred TOCTOU check, RN-15 CI --locked), RN-21 accepted (fail-closed). Headline open P0s: dataplane fail-open paths, Windows killswitch/IPv6 leaks, policy default-allow (revocation bypass)
- [FullRepoAnalysis_2026-05-24.md](./FullRepoAnalysis_2026-05-24.md) — full-spectrum repository analysis (9-agent two-pass review, all passes complete): 66 findings across security (1 critical crypto bug — AlgorithmPolicy inverted guard; 3 high policy-engine issues — empty-context wildcard, rollout validation bypass, membership bypass-on-empty), architecture (IPC versioning gap, relay diagnostics trait gap), code quality (panic paths, allocation inefficiency, async safety — RwLock across await, fire-and-forget tasks, HTTP body unbounded), shell-to-Rust migration status and TIER-1/2 outstanding work, documentation gaps (4 critical release blockers, 8 high-priority missing runbooks), lint/cfg/unsafe audit (clean; missing Windows SAFETY comment), IPC/input validation audit (strong; HTTP body size limit missing); 43-item prioritized remediation plan
- [TestCoverageImprovementPlan_2026-05-24.md](./TestCoverageImprovementPlan_2026-05-24.md) — owning ledger for the test-coverage track; prioritized P0/P1/P2 workstreams (roles.rs zero-coverage, signed-state/crypto/dns-zone negative tests, sysinfo parse/IO split, dataplane default-deny truth tables, gossip anti-replay persistence) plus tooling levers (llvm-cov ratchet, proptest, expanded fuzzing, broadened coverage-gate floors)
- [SerializationFormatHardeningPlan_2026-03-25.md](./SerializationFormatHardeningPlan_2026-03-25.md)
- [ShellToRustMigrationPlan_2026-03-06.md](./ShellToRustMigrationPlan_2026-03-06.md)
- [StartShOperatorUxRustMigrationPlan_2026-05-24.md](./StartShOperatorUxRustMigrationPlan_2026-05-24.md) — implementation guide for migrating the remaining `start.sh` logic (operator UX, config management, role policy, validators, arg parsing, egress detection, dependency bootstrap) into a new `rustynet-operator` Rust crate; continues ShellToRustMigrationPlan (privileged/secret flows already migrated) and closes the last two direct privileged shell ops (config chmod, binary install); motivated by cross-platform (Windows) support and security. Includes **Appendix A: ready-to-paste reference implementations** — complete, tested Rust ports of the pure-logic shell functions (role/preset normalization, launch/LAN/exit-chain validators, arg parser, egress/endpoint parsers, config allowlist/parse, atomic 0600 config persist + file-security gate)
- [UdpHolePunchingAndRelayTraversalPlan_2026-03-07.md](./UdpHolePunchingAndRelayTraversalPlan_2026-03-07.md)
- [UdpHolePunchingHP2IngestionPlan_2026-03-07.md](./UdpHolePunchingHP2IngestionPlan_2026-03-07.md)
- [UdpHolePunchingImplementationBlueprint_2026-03-07.md](./UdpHolePunchingImplementationBlueprint_2026-03-07.md)
- [FableForkConsistentMembershipTransparency_2026-07-01.md](./FableForkConsistentMembershipTransparency_2026-07-01.md) — **SPECULATIVE R&D deep-dive — UNSCHEDULED, self-authored: not in-flight, not in the live-lab acceptance matrix.** Single-topic expert design study closing a currently-unaddressed trust-state attack class: **membership equivocation / fork attacks.** Today every check in `apply_signed_update` (`crates/rustynet-control/src/membership.rs:693`) is a *local linear* guard (`prev_state_root` link, epoch chain, `MembershipReplayCache` monotonic-epoch) — none detects a signer (stolen/coerced admin key, malicious anchor MITM on single-source bundle-pull, or concurrent-signer race) handing *two distinct-but-each-internally-valid* epoch N→N+1 histories to different nodes, silently forking the mesh. Proposes a three-layer defense on top of the existing Ed25519/SHA-256 primitives (no new crypto): (1) promote the already-hash-chained update log (`append_membership_log_entry:808`) to an **append-only Merkle history tree** (RFC 6962 / Crosby–Wallach) giving O(log n) inclusion + consistency proofs and a Signed Tree Head; (2) **gossip-based fork detection** (SUNDR fork-consistency, CT-gossip) — nodes exchange STHs on existing gossip, demand a consistency proof, and **fail closed** (freeze at last-consistent epoch + persist offline-verifiable fork evidence) on divergence; (3) opt-in **decentralized witness cosigning** (CoSi / Sigsum / Parakeet) to escalate detection→prevention, where witnesses hold **zero capability** (can only refuse, never grant — fits the advisory-never-grants rule). Includes grounded citations, explicit CLAUDE.md §3/§4/§8 constraint check, Pi-class cost analysis, honest scale-gating of Layer 3 (detection valuable at ≥2 nodes, prevention needs ≥5 independent witnesses), a 4-phase report-only-bake build path with an equivocation-injection live-lab acceptance stage, and self-assessed weakest points. Composes with (does not duplicate) FIS-0002's local-correctness formal spec by adding a `NoUndetectedFork` invariant
- [FableIntelligentSystemsProposals_2026-07-01.md](./FableIntelligentSystemsProposals_2026-07-01.md) — **SPECULATIVE R&D proposal set — UNSCHEDULED: not in-flight work, not in the live-lab acceptance matrix.** Eight adversarially-verified intelligent-systems design proposals (FIS-0001..0008, ranked): continuous path-quality EWMA scoring + make-before-break migration (boringtun already computes per-peer rtt/loss and rustynet discards both today), TLA+/stateright formal spec of the membership apply/anti-replay protocol (operationalizes the parked AnchorLiveLab §C6 line), Plumtree digest anti-entropy gossip, loss/RTT-variance congestion signal → relay preference + pacing, advisory role-placement scoring, SPRT/CUSUM live-lab flake-vs-regression classifier, relay DRR fairness + load-aware selection, nas/llm multi-instance service routing — each with grounded file:fn citations, an explicit CLAUDE.md §3/§4/§8 constraint check, a phased S/M/L build path, and a recorded independent citation-fact-check + adversarial-review verdict; plus 2 fully-designed-then-REJECTED entries (enrollment Bloom-cascade — RN-26 already prescribes the dominating exact fix; multi-anchor DNS quorum read — targets the SR-001-disabled fetch path) and an honest coverage note listing unverified side-findings routed to the audit pipeline

## Active Lab Assets

- [UTMVirtualMachineInventory_2026-03-31.md](./UTMVirtualMachineInventory_2026-03-31.md) — VM list, SSH key fingerprints, **probe-and-recover runbook** (`scripts/vm_lab/probe_and_recover_local_utm.sh`) for unsticking stale-killswitch lab guests
- [../LiveLabRunMatrix.md](../LiveLabRunMatrix.md), [../live_lab_run_matrix.csv](../live_lab_run_matrix.csv), and [../live_lab_node_stage_results.csv](../live_lab_node_stage_results.csv) — aggregate run ledger plus normalized exact distro/version node-stage proof; update after every run used as evidence
- [rustynet_blind_exit_pcb_report_2026-05-10.docx](./rustynet_blind_exit_pcb_report_2026-05-10.docx) — external/AI-generated analysis, not native to this repo; cites a hardware source file (`rustyjack-h616-pcb-architecture-v0.md`) that doesn't exist anywhere in this repository, and its network design assumes NAT is present, which contradicts the actual no-NAT `blind_exit` dataplane. **Superseded by [BlindExitPcbHardwarePlan_2026-07-22.md](./BlindExitPcbHardwarePlan_2026-07-22.md)** — treat this docx as historical background only, not a spec.
- `vm_lab_inventory.json` — VM topology + role assignments
- `vm_lab_readiness_check_2026-04-28.json` — dated VM lab readiness summary (schema_version=1)
- `netns_transit_migration_evidence_2026-07-10.json` — Tier 1 live evidence for the netns transit migration to `198.18.0.0/15` (build/reachability/isolation + NAT mapping 3/3 + filtering 9/9 + clean teardown on debian-headless-2)
- `vm_network_evidence_postmigration_2026-07-10.json` — post-migration read-only network audit after the Slice B live application moved debian-headless-4/Windows/Fedora/ubuntu from Bridged to Shared (`mgmt_shared_smoke_v1`, txn `txn-1783721025-63223`); all bridged findings cleared; residual findings are the host Tailscale/mesh route collision + stale network_group labels
- `windows_utm_1_runtime_acls_2026-04-28.json` — windows-utm-1 runtime-ACL collector evidence
- `windows_utm_1_service_hardening_2026-04-28.json` — windows-utm-1 service-hardening collector evidence
- `windows_utm_1_validate_2026-04-28.json` — windows-utm-1 validate-bundle output

Rule: these JSON evidence files are dated artifacts from specific lab runs.
Add a new dated file when a new run produces fresh evidence; do not edit
existing ones in place.

## Rules

- Keep status and evidence in the owning ledger or plan.
- Do not add standalone prompt documents here. (Existing prompt documents are listed in the section above for discoverability only.)
- If an active file becomes historical, move it or reclassify it honestly and update the indexes in the same change.
